---
id: 867
title: "Dynamic Entities in .NET: Building Low-Code Systems on Top of Entity Framework Core"
slug: dynamic-entities-in-net-building-low-code-systems-on-top-of
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Languages & Runtimes"
type: "Keynote/Talk"
stage: "Stage 6 - powered by Microsoft"
tags: ["Low Code/No Code", ".NET"]
speakers: ["Halil İbrahim Kalkan"]
speaker_companies: ["Volosoft"]
day: 2
starts_at: 2026-07-10T08:20:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=g-VnWn3P-lI
video_id: g-VnWn3P-lI
session_page: https://app.wearedevelopers.com/events/16/session/867
transcript: true
---

# Dynamic Entities in .NET: Building Low-Code Systems on Top of Entity Framework Core

**Halil İbrahim Kalkan (Co-founder — Volosoft)**

`Track: Languages & Runtimes` · `Type: Keynote/Talk` · `Stage: Stage 6 - powered by Microsoft`

`#Low Code/No Code` `#.NET`

[Watch the recording](https://www.youtube.com/watch?v=g-VnWn3P-lI) · [Session page](https://app.wearedevelopers.com/events/16/session/867)

## Abstract

Designing a truly dynamic data model is one of the most challenging tasks in modern application development. Many teams fall back to traditional approaches like Entity-Attribute-Value (EAV) or overly generic schemas, only to struggle later with complexity, performance bottlenecks, and maintainability issues. In this talk, we will explore the landscape of dynamic entity systems, compare alternative patterns, and highlight their strengths and tradeoffs, before zooming in on how Entity Framework Core can be leveraged to build something both flexible and efficient.

Drawing on real-world experience from building the low-code capabilities in ABP Framework, I will share the key architectural decisions, pitfalls, and solutions that made our dynamic entity system robust and performant. You’ll see how EF Core’s extensibility can be pushed to its limits, how to handle querying and schema evolution at runtime, and how to avoid common traps while maintaining developer productivity. By the end of this session, you will have practical insights and patterns you can apply in your own projects when facing the challenge of designing dynamic yet reliable data models.

## Speakers

### Halil İbrahim Kalkan

*Co-founder — Volosoft*

I am a software architect and developer. I love to work on open source projects, producing articles, videos and giving talks on software development.

I'm the lead developer of the ABP framework (see https://github.com/abpframework/abp and https://abp.io/)

I'm mostly interested in developing reusable frameworks & libraries, distributed systems, multi-threaded and scalable solutions. I'm talking at local and international conferences about multi-tenancy, microservice architecture, domain-driven design, modular systems and developer tools.

Visit my website to see a list of my previous talks: https://halilibrahimkalkan.com

## Transcript

*4,213 words · source: kome (en)*

**[0:00](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=0s)** Welcome in the third session of this track the Microsoft track which is uh obviously because all the talks about net uh today we have Halil Hal is one of the the lead the maintainer of the APB for AB AB framework I actually know it I've never used it myself is from Turkey. He's going to be talking about yeah, how do I explain this even building low code systems on top of entity framework? Okay. He's the co-founder of Photooft and I want you to give him a big applause. Welcome, Alil. >> Thank you, Denise. Uh hello everyone, welcome. Uh I suppose all of you are net developers. I think yes. And mostly working with entity framework core for RM purposes. I think yes. Okay, great. Uh you know I am also developer and um since from C 1.0 I'm working with net.

**[1:02](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=62s)** So uh you know someone tells me us what to do and we create applications actually what they want you know every time they say make it dynamic. So we make it uh make some change on the fly without redeploying or without requesting change from you. So it starts from simple configuration like dynamic configurations that uh change the behav behavior of the application. Uh but all this kind of people business people's dream is to do everything themselves without asking you create new entities on the fly add some properties if possible create some workflows um some maybe page themselves without requesting from you. This uh probably this request comes very frequently to every developer in some level. Today uh I will talk about how to build this kind of dynamic systems maybe uh we can say

**[2:02](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=122s)** low code systems uh using the entity framework core as the database layer. So first I will start with a what is a low code system uh then I will talk about how to design a low code system. So what uh which considerations we should care about and um what are the design design tradeoffs and so on and then I will implement uh show how we have implemented the data layer with entity framework core and finally I will talk about how to deal with runtime chains. You know if your model chains if your entities changed on runtime how you model it and how you make it practically working. Anyone heard about ABP framework before? A few. Okay. ABP framework is an open source framework. I am the lead developer of the this project. Actually, I have created it uh more than 10 years

**[3:05](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=185s)** ago and it is you see it's has uh 14K stars on GitHub and uh we have been developing it very long time. Recently I started to create a low code module based on this ABP framework uh to make to this module is very flexible and reusable because uh we are not developing it for internal purposes but for other developers that we don't know what they will build on top of that. So it should be flexible and we selected entity framework core because when we use entity framework core we don't uh need to deal with low-level SQL statements based on different providers like it works with every provider that's supported by entity framework core um so I will talk about this real implementation what we did how why we gave this decisions and so

**[4:05](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=245s)** So what is a uh low code system? We can say that it's it creates your application behavior from a model on runtime. You know normally our model is maybe C# code with some SQL business logic everything is hardcoded and they they are not so flexible. they you know when they request some change we make some development redeploy it and so on but for a low code perspective it's everything is data even your application even your business logic entity definitions um database integrations every should everything should be data and uh doesn't require any redeployment when you change your uh model when you change your business logic uh okay when You define your data some runtime you should build that interprets this model and executes it in back end and front end. Of course this is the

**[5:05](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=305s)** main purpose. Uh the main advantage is no recompilation, no redeploy and uh of course it provides faster delivery because when you want to add a new field to a entity you just make it in a few seconds and it is live. uh unfortunately it uh reduces dependency to us. You know in these days everybody tries to do that with AI uh to reduce developer dependency. So this is one of the things uh it provides flexibility and also domain experts uh that has you know directly have knowledge about this business can directly make their change without needing a developer. Of course as developer we are still uh behind the scan and uh create this application and continue to develop this low code system still we are exist but uh they can build their own page they are own workflows

**[6:07](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=367s)** and so on. So when we we can use this kind of things because it's not proper for every scenario. First of all, datadriven applications mostly crude page uh maybe uh light business logic is proper for this kind of applications. Some internal tools for example you may have a CRM tool HRM tool uh for internal company purpose is good candidate for this kind of applications and uh if your requirements are rapidly evolving changing and every time you need to deploy your application change this is also can be a solution but it is not for replacing everything like especially complex system that requires for example good architecture like domain-driven design, maybe micros service architecture, these are another story. But for datadriven internal applications which needs to rapidly change, that's good way to create some flexibility to

**[7:06](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=426s)** users. Okay, how to design this kind of systems? Let's see the big picture. First, we have of course a model. This time model is not C# classes or this kind of things. We have some entity definitions, maybe relations between these entities, workflows, maybe some kind of business logic, interceptors, this kind of validation, authorization rules. These are our model. Actually, it was before with normal C# code now with some kind of definition, data definition. And based on this module, everything shapes based on this module. We have a back end runtime typically which executes backend logic based on this model and the front end runtime which creates dynamic UI based on your model. So when you create these runtimes you have no idea about which type of application will be built based on that. So it should be generic.

**[8:08](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=488s)** It should provide all the flexibility. That's the hard part of course especially uh UI part is really hard for this kind of system building. For back end side, we have in this case entity framework or uh for mapping your entities to database still while we have entities on the runtime and of course database tables and typically this kind of systems has a designer. So um your customers doesn't use uh like development tools. They have a UI they can create entities, relations, these kind of things. So uh first question is where to store this model because we store data in database we know it very well we have orders customers products they have over tables fields this kind of things okay that's good but where to store model there are two common ways when it comes to this topic first option is you can also create tables classic tables entities relations I I don't know maybe

**[9:16](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=556s)** UI definitions in your database table. Second way can be using a more flexible HML like a JSON document or XML document. The second way is more common because it's more flexible and more proper for uh evolving and uh for storing this kind of information. uh it's easy to version this kind of single document or separate document doesn't matter but uh so it's more proper to create this kind of shema definition for modeling your application we prefer the JSON approach in this uh in our implementation so it's simply like that you have entities customers properties some options like is this um field is required unique it is displayed like that on UI. This is just a simple demonstration. It's not the exactly real case. You may have some

**[10:13](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=613s)** interest interceptors like for example when creating a new customer, you can write some JavaScript code which is executed on runtime on the back end uh to check authorization rules and this kind of things. So this is this can be imagination of a simple model uh which is coded as a JSON document. Okay, we have a model in a JSON document. Of course uh our customers will use some designer to manipulate this JSON without uh touching it. But when it comes to database layer, we should have three considerations. First of all, how to design this shemma? You know you have new entities on the fly and how to store it in a single datab database table or separate database table. There are a few different approaches. When it comes to entity

**[11:11](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=671s)** framework mapping we have used some advanced techniques which will be our focus in this topic in this talk and of course we should deal with the some kind of database migrations because our shema can be changed on the fly also we should handle this kind of things. Uh okay. When it comes to shama, you can think that this e a v model is the simplest one and most of the uh teams uh fall into this trap actually because they have they think that okay we create some entities table some properties table and some values table that's all and it will solve really all the problems of uh migrating um tables or creating new tables maintaining Shma it solves all these kind of problems because it's very flexible. However, it pushs all the complexity to the runtime especially for the querying

**[12:14](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=734s)** because when you have this kind of shama you cannot easily index your tables. You cannot make join queries eas easily. you need uh you need some custom indexes based on this shama and so on and it finally becomes much more complex than in the beginning. So this is not a way we suggested this. Of course we have considered this also because it's easy when you uh model your system it's easy to model but when you want to write performant queries it's almost impossible. Okay, we in our implementation we prefer a hybrid approach for first of all we have a separate database table for each entity that is created on runtime. So that means when a customer creates a new entity, a user creates a new entity, we create a physical database table in the database.

**[13:14](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=794s)** And also in this database, there are some standard fields like ID, creation time, maybe moderation time, uh concurrences, stamp, these kind of standard fields that is um used mostly by the framework uh automatically and fill it and these kind of things. There are also options we provide to field physical database tables to your entity even if your entity uh exist only on the runtime and also rest of the properties for most of the properties we store as JSON. I will show exactly uh so you will understand it better what I mean. So this approach makes it easy to filter, easy to order, easy to make joins, database level indexing is also possible. So this approach is more balanced approach compared to pure three table approach. Let's see an example. We have a product defined in the dynamic model and with a

**[14:17](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=857s)** few properties name, product code, maybe description and so on. You see that one P field here name is method to the database field. When we do that we create a real database table like that with ID creator id is deleted. These are standard columns but also name is directly mapped to a field. In this way we can easily index we can easily join and this kind of operations database level operations are easier. And for rest of the fields which are mostly for read purpose or not needed to index or these kind of things more flexible fields are stored in a single field called it data as a JSON object. Uh fortunately in SQL server and poster SQL and this kind of major database systems also allows you to query in this JSON fields. So it's also

**[15:17](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=917s)** possible to make joins some level of indexing and ordering and these kind of things. So how to model this kind of flexibility with entity framework core. This is the main purpose of this talk as I said. So I will introduce a few less known entity framework feature that makes this kind of applications possible. First feature is shared type entities. Have you ever heard it shared type entities? Okay, great. You know when you create a such a dynamic system, you cannot know which entities will exist in on runtime. You may have products, customers, orders, I don't know many different entities. So you cannot hardcode these entities in development time. In this approach, we create a single class that maps multiple tables. This is the purpose of entity frameworks shared

**[16:15](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=975s)** type entities. So table name becomes the entity name on runtime and you can use just normal I queryable just normal entity framework code based on this shared entity type. Let's see an example. This is a simplified example of uh the dynamic entity class defined in the ABP framework low code module. So it basically have ID tenant ID to support multi-tenant scenarios. These two properties are critical. The first property is for indexer properties. I will demonstrate later. Uh and this data property is for JSON stored properties. So you can see with indexing standard index syntax we can get or set properties and also this is a kind of dictionary actually but uh serialized as JSON to the database. So when we create such a single entity class we can use entity frameworks

**[17:13](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1033s)** shared entity type entity builder we give this entity name. You see this shared entity type syntax is special because you we normally write like builder entity like that but when we do it we should provide a name because every entity in the database every database table is method to single C# class. So we should distinguish somehow uh and we provide entity name. Then of course we can configure other uh table properties or other entity properties as normal. And when we want to query this kind of entities you know entity framework has a DB set like a DB context set method to get an entity type. Here we should also provide the entity name because it's dynamic don't know on runtime. So it's probably will be a variable coming from

**[18:11](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1091s)** somewhere and we can pass to the set method. In this way entity framework gives us the same uh data set entity or sorry object with configured only work with this table. So then we can just use order by I don't know where conditions this kind of things as usual. And uh also when you have a single entity class assume that you have an object a dynamic entity to object in your code and you don't know which table this is mapped. So there is a solution for this problem and the framework core uh has some interceptors that is executed when you query your data or save your data to database. this kind of in this kind of operations you can um write interceptors and execute some code here the code is easy you see uh I am taking the entity name and set this

**[19:15](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1155s)** entity name to the entity object so in this object then I can check which entity type is this I may write conditional expressions I don't know what we we need to do anyone use it materialization interceptor here not very use it uh feature but it's very valuable. When you query u object from database you can pre-initialize your uh class for example you can do everything whatever you want also there are some uh more interceptors when you are saving the database and this kind of things. Indexer properties is another uh important feature we have used it for um this kind of application low code application you know we don't know the all properties on the dynamic entity class we don't have because it's appears on the runtime but we may want that this

**[20:13](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1213s)** property exist in the database field so in this case we can map it as indexer property and we can write normal query where conditions order by joins it will be indexable you know normal field in the database but mapped to a collection property or dictionary property on runtime so you can easily add properties to dictionary get data set data it's is easy to use but definition is a little different when you create a uh you know property define a property you just write builderproperties like that but this time you should use the indexer property property method you should provide type and name of the property. The name of the property is string because we don't know it's on runtime uh development time. So when you query you can use just use uh entity frameworks property helper method uh this a static method and just

**[21:18](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1278s)** pass the property name and you can use in order by where or whatever you want. Again it's just standard property. Another feature we have used uh which comes probably as I remember entity framework 7 or so JSON directly JSON column mapping. So you can have a single column single field in database as a JSON formatted and uh a corresponding object or dictionary in your entity. So all mapping is handled by the entity framework automatically. So to map these kind of uh properties we just write to JSON extension method. When we use that entity framework of course expects some kind of dictionary or a class that is derived from dictionary object and then we can use normal indexer property or property or any kind of mapping code it will be mapped to a single field.

**[22:17](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1337s)** Another problem here you know in entity framework core we have a very famous method on model creating. In this on model creating method uh normally it runs only once for an application lifetime for your DB context object. So if you want to change your model later it will not be possible normally. But anti framework core also provides a way to invalidate invalidate this cache and allows you to redefine your model when your dynamic model changes. So the way is create a class that is derived from imodel cache K factory. This just waits an object from you as the K of your cache. So here uh in our implementation we use context object and dynamic model version. So we have a version property that is increased on every change. It's simple integer property actually. So whenever a version

**[23:19](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1399s)** change and the framework calls this method and we return a different cache object. So it invalidates the cache and calls the on model creating method again. So we can define new entities or just not define old entities. So it knows the latest version of our model every time. Another problem when we create such a dynamic systems to create database fields or database tables on runtime you know when we use entity framework or we use code force migrations or is it still supported database first approach? I don't use for a while I don't know maybe okay code first approach is used it for defining migrations but the problem is it's for design time that means development time there are more libraries like fant migrator anyone knows fant migrator okay it was a really uh useful library but entity framework

**[24:20](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1460s)** core now supports almost all scenarios now I think it's not needed so uh but it is also for uh design time on runtime you cannot use its features. Our solution actually we investigated two ways. First um make our own migration tool on runtime that just write some SQL to alter database tables create fields and this kind of things. Also we check it if we can reuse entity framework course migration features on runtime somehow and we found that it's possible it's a little dangerous but possible dangerous because it's internal feature and entity framework core team may change it in the future and it breaks our application but we take this risk because maintaining other side is also harder so we use entity framework course internal APIs to create uh the database

**[25:18](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1518s)** chains on the runtime time I cannot explain all the solution now but just I want to say that it's possible if you if you ask the ch it will say okay [laughter] [gasps] um one thing one feature we also created in addition to this low code system that everything works on runtime we decided that maybe we can mix low code with normal code just normal C# entities and dynamic entities you don't know it will be created on runtime maybe they can somehow communicate so we decided to uh create a system that we can add some attribute to a standard entity class it can be derived from entity base special entity base or not it doesn't require it so we can create uh we can make this customer entity in this example as a part of the dynamic system. So we can

**[26:21](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1581s)** join them, we can query on runtime, we can mix this code together. So it's interesting approach. So first layer was that the second layer was a JSON file. Maybe we thought that developers may want to create code on run u create JSON files on development. And the third layer on runtime it stores everything on the database. So runtime on the database overrides the uh model JSON file and it overrides the C file which is hardcoded. So three layer model. It's a little complicated but to give you an idea it's also possible to mix normal code with somehow low code system. I think this is this hybrid approach is proper for much more scenarios because most of the time maybe 20 or 30% or your application requires high business logic while rest of the application is just crude pace.

**[27:23](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1643s)** So this crude pace can be dynamically created on runtime while still you have some page or UI component that works with your handwritten code or AI written code. Okay, as a summary, I have introduced shared type entity. This uh allows us to map C# class, a single C# class to multiple database tables. Indexer properties that allows us to not define a property on a C# class but define a field in the database and map the them together. JSON column approach makes single field in the database u stores multiple field multiple values as a JSON object and map it to the database uh map it to the entity framework class and model cach factory to invalidate the cach when your entity uh definitions or database mapping change on runtime. Okay, thank you very much all from me

**[28:23](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1703s)** for now. If you have any question, I'm here. >> That's awesome, man. This is probably the most advanced usage of anti framework I've seen. >> Probably. Yes. We have investigated too much time to explore very high high level details. This kind of hard details. >> Clearly. Clearly. >> Thank you. >> Cuz I've been using an hibernate and that's something was possible. This I didn't know. So there's one question from Alexe. When using JSON properties, how does the framework materializes older JSON properties which did not not did not have newly added JSON fields? It's a long question. I guess he means when you're read it yourself, >> which did not have newly added JSON fields. Ah, okay. Actually, I understood the question. Uh, when you design such a low code systems, of course, you should

**[29:23](https://www.youtube.com/watch?v=g-VnWn3P-lI&t=1763s)** think about everything as backward compatible. When you have a new field in JSON, new records will have this field in the JSON. Others will not be. So you should always define some fallbacks or default values. That's the solution. Second solution can be of course migrating all the u all the existing field. But what you will do? You will just set a default value. So it's best to assume this default value. If it is not defined, it's better. >> I think I understand you. I hope Alexa also does that. Thank you. Big applause and see you in 10 minutes in another talk on this amazing track. >> Thank you very much.
