---
id: 731
title: "Docker build without Docker"
slug: docker-build-without-docker
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "DevOps"
type: "Keynote/Talk"
stage: "Stage 8 - powered by Red Hat"
tags: ["Containers", "DevOps", "Docker", "Linux"]
speakers: ["Oliver Seitz"]
speaker_companies: ["Accenture"]
day: 1
starts_at: 2026-07-09T12:50:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=AVr_6VN1-24
video_id: AVr_6VN1-24
session_page: https://app.wearedevelopers.com/events/16/session/731
transcript: true
---

# Docker build without Docker

**Oliver Seitz (Associate Manager — Accenture)**

`Track: DevOps` · `Type: Keynote/Talk` · `Stage: Stage 8 - powered by Red Hat`

`#Containers` `#DevOps` `#Docker` `#Linux`

[Watch the recording](https://www.youtube.com/watch?v=AVr_6VN1-24) · [Session page](https://app.wearedevelopers.com/events/16/session/731)

## Abstract

Ever wondered what a Docker image really is? How do layers work? Why are images content-addressed? And how does Docker turn a sequence of filesystem changes into something a container runtime can execute?

Let’s continue the "Docker * without Docker" series and take a deep dive into Docker image builds - without relying on Docker itself. We use docker build every day, but what actually happens under the hood?

We’ll explore the Linux and OS concepts that make builds possible: root filesystems, filesystem diffs, OverlayFS, tar archives, hashes, and image metadata. Step by step, we’ll reconstruct the core ideas behind docker build directly from the terminal, to see how much of Docker is clever orchestration rather than magic.

Join me for a hands-on exploration of what Docker images truly are - and why understanding the build process changes how you think about performance, image size, caching, and container behavior.

## Speakers

### Oliver Seitz

*Associate Manager — Accenture*

I've been a software developer at heart for ten years and am interested in everything related to software development, like containerization and orchestration, machine learning, security, and the cloud. I work as an Associate Manager @ Accenture in Munich, Germany. I love fishing, Microsoft Flight Simulator, and TikTok.

## Transcript

*4,359 words · source: kome (en)*

**[0:00](https://www.youtube.com/watch?v=AVr_6VN1-24&t=0s)** All right. Welcome back everyone. How are you enjoying the conference? Yes, awesome, right? Okay. Next we up Next up we have Oliver Zeits, software developer at Accenture. We're going to be talking about Docker build without Docker. Everyone give a round of applause. >> [applause] >> Yeah, well, thank you so much for joining this talk. Um, this is my maybe fifth or sixth time at WeAreDevelopers and third time speaking. Fun fact, it's also the third time speaking about Docker and the third time this year my application for Docker got rejected. Um, but this is a different story. Um, I love these um, really technical deep dives um, about uh, Docker. I don't know how I got to that. Um, I think it was one of my lectures at the university and then I started with explaining how namespaces and C groups work and now we are at um, how Docker images work and how Docker build works. Um,

**[1:03](https://www.youtube.com/watch?v=AVr_6VN1-24&t=63s)** and I want to assure you that apart from the nine other talks right now that are about AI, this talk does not mention the word AI in any sense. So, um, this is your non-AI safe space for the next 30 minutes and I want to jump right into it. Um, So, a Docker image is really not that special. It's just a tarball. Um, so a tar archive that contains JSON files and tar files. And what these files actually mean is encoded in the so-called media type. So, each and every of these files has a certain media type. There are um, Let's start with that. There are the media types index and manifest and config. Those are the ones that are represented as a JSON. Um, the index um, file type that represents a more like a, um, a pointer to the certain manifest for a specific target architecture. So, for example, if you're running on a, um, ARM CPU, or if you're running on an

**[2:10](https://www.youtube.com/watch?v=AVr_6VN1-24&t=130s)** Intel CPU, so this is, um, done via the index, and then the interesting thing starts at the, uh, the manifest. Um, the manifest, um, talks about where the configuration JSON lies, and where the actual layers, so this is the file system of the container, is, um, and the config JSON then defines, like, environment variables, the entry point, um, which ports are, for example, exposed by the container, and then we have the actual layers, the file system of the container, and this file system is represented by, simply, um, folders that are compressed as a tar archive. Um, we will tear that apart in a second, um, the other important thing that we're talking about today is a lot of, um, hashes, because these files are not named by config.json or by manifest.json or layer one or layer two. No, they are named by their hash value, and this is a pretty interesting thing, because, um, the content matches the hash, uh, or the

**[3:15](https://www.youtube.com/watch?v=AVr_6VN1-24&t=195s)** content matches matches the, um, the hash, and the name is the hash value. That way, you alway- always have the, um, the checksum of this file combined with the file itself. And this is also how Docker registries, or container registries, work. Um, they are content addressed, meaning if you, uh, you have to know the hash of the file, and if you do that, you can use the simple get endpoint to get whatever layer, or manifest, or config, you want. And then, of course, you have to send like an authorization token, stuff like that. We will do that in a minute. Um, however, so, basically, a Docker registry is just a key vault or uh not a key vault uh a key-value store um from um pointer given by um the hash to the actual file. Um and I think best is to understand this

**[4:14](https://www.youtube.com/watch?v=AVr_6VN1-24&t=254s)** if we do it in a live fashion. Let's check if this Hope it works for the font size. Um, I have prepared this as a repository. You can also Um, you always just have to look at my command line down here. I will just copy the things. Um, first thing that we have to do is we have to get a token for the Docker registry and then we can start pulling things. So, what we now want to do is we want to look into the Alpine image. Very famous image um containing basically just um low-level um tools like cat or ls often used to build the most simple images. And we can look at this by using a simple curl that we use to query the registry. And the first thing that we are looking at is um the index. We see this if we go on top here. We have If we go far on top um we have a section of manifests and we want to use the

**[5:22](https://www.youtube.com/watch?v=AVr_6VN1-24&t=322s)** manifest that is appropriate for my machine, which is an arm uh CPU. And so, we're using um this manifest here. We have some annotations here. It tells me it's an Alpine 3.24.1. Um it tells me um what architecture this is for. And what I'm interested in is this reference here to the manifest. And what I have to do now is I have to do I have to look into um this hash, get it from the Docker registry, and then look into this file, and then I have the manifest. Um I think I might have prepared this in advance and we can look into um the actual manifest. I will format this for you. Um so this is this is basically how the uh manifest looks like and here you find several things that are probably very

**[6:18](https://www.youtube.com/watch?v=AVr_6VN1-24&t=378s)** well known. You have the environment variables that you can set in a Docker file. You have the command that you also set in a Docker file, the working directory. Um we have a history. And we have something that is called the root FS with several diff IDs. And those IDs are um references to the actual layers that we used to build up the file system. For this Alpine image is just one layer. And this is um a hash of the Now this is a bit of a special part. This is not the hash of the compressed file, but this is a hash of the uncompressed file. Um and if we look into the manifest again, which I have to query from the registry. I think I have it here. Download the manifest really quick and look into the manifest.

**[7:14](https://www.youtube.com/watch?v=AVr_6VN1-24&t=434s)** This is that one. We can see that it also has a section about where it references this config that we just looked at, right? The um 1991, which is this one. And then we have the layers and those layers are tar gzip files. Um and this is the hash 5DE, which references this file here. And if we uncompress this file, um so remove the gzip, we get the tar file. And this is the one that is referenced in the config. So and we can do that and look at how this image file system thing actually looks like. Um I think this is the right command for that. Um No, it's not. Perfect. What are we doing? We are We can also just do this manually. Copy path. And oops, get this up. tar minus tf and the blobs. Oh, no. I'm in the wrong directory, I see. So one Oh, this one. Ah, that works. Perfect. So, now we have our container file

**[8:20](https://www.youtube.com/watch?v=AVr_6VN1-24&t=500s)** system. And we see this is really just the thing that I would see if I just do a Docker run and look at what is actually inside the container. So, nothing very special about that. Where it gets interesting if we add multiple layers. Um and this is what we're going to do next. Um So, the next thing that we're doing is um we want to add a file to an existing Docker image which is which is essentially what we would do if we, for example, have a web application that we successfully built and now we want to run it inside an Nginx um container. So, what we want to do is we want to add this index.html file that contains the text built without Docker inside a container and then somehow run this container and see this text printed out on a web page. And the way we do this is we have to create a new layer.

**[9:24](https://www.youtube.com/watch?v=AVr_6VN1-24&t=564s)** So, we cannot touch the base layer of Nginx um because if we would touch this layer, the hash would change and then it wouldn't be the actual Nginx image, meaning we don't have all these cache efficiencies because if you download the base image of Nginx one, you don't have to download again just because you add a file on top of it. So, we want to create a new layer and this layer is then just compressed and zipped. Um and then, of course, we have to add it to the config.json as a new layer. Then we have to because the config JSON the the content changed, therefore we have to rename this file. Yeah? Hash the content of the file, rename it, generate the manifest JSON with the new reference to the config, right? This is just the re- hashed uh content here. And we of course have to add it to the layers here so that Docker

**[10:28](https://www.youtube.com/watch?v=AVr_6VN1-24&t=628s)** knows which files to download. And then basically we're done. And if we run that, we should be able to see something like build without Docker on our web page. So, the way we're doing this is this time by changing into the right folder. And there is up a bit. Close this. Um nginx demo. So, I have prepared some things in advance. I downloaded the nginx image and all the layers that are inside the nginx image. We have the index.json and I have the manifest laying around here. And now we want to fiddle around with those files to create the new layer. So, the first thing that we're doing is we create a working directory and write the file that we want to have inside the image into this folder, right? I have this path here, user share nginx HTML. index.html. This is what I want inside the container. Next thing I have to do is I have to create my archives, the tar

**[11:33](https://www.youtube.com/watch?v=AVr_6VN1-24&t=693s)** archive and then the the zip version. Um just plain command line uh using tar and then gzip. And all the other stuff is just to get a pretty print of the hashes that I now have to plug into the config and to the manifest. And we do this by um looking for the right config that we need. So, remember we need the V8. Right here, this gives us the manifest. Then I could look into the manifest. However, I think I also downloaded it here and we have to look into the config. And we have to look into the layers. So, first thing we can do is we add a new layer here. Actually, the size attribute doesn't really matter if you execute it locally. We just put something in here and for the hash we use the compressed file. So, the one that would be downloaded from the Docker registry is always the compressed one.

**[12:36](https://www.youtube.com/watch?v=AVr_6VN1-24&t=756s)** Um and then we're done with that. Next thing we have to update the config. So, this is the hash 229. We search for this file here. We find 229. A little formatting. Um and we we don't want to change anything of the config. Our Docker file wouldn't change any environment variables or commands. We would really just want to add a new layer down here. And we do this this way. This time we use the uncompressed version of our archive. Plug it in here. And now since we changed the content of the file, the hash of the file changes. So, we have to regenerate the name um for this file. Um we also have to um move the custom tar.gz, which is our new layer, inside this working folder. I have this command for it. So, we plug it in here. I think it's the um the AA file. So, this one is now inside this blobs folder, our tar.gz file. Um now we have to regenerate the config.

**[13:44](https://www.youtube.com/watch?v=AVr_6VN1-24&t=824s)** Um I think we have it. config.json Uh, we have to execute this. We already found our config. Now, we rename it. So, new hash for the config is now 73d6. Um, and we have to copy that one and plug it into our manifest so that we're actually referencing the new config.json. Otherwise, we would run basically the same image. Um, so we updated config, we updated layers. Um, we can now move the manifest also into our blobs. The name, again, is just the hash. We have that. Um, so the hash of this new manifest is uh d78. Um, here we have our manifest. And now, we are almost done. The only thing we left to do is we have to update this index file because we don't want to point to the old manifest, we want to point to the new manifest. To do

**[14:42](https://www.youtube.com/watch?v=AVr_6VN1-24&t=882s)** that, I have prepared a little template to make this a bit more easy because inside this inter index, I can also rename my image because when I load this in Docker, I don't want to to be named nginx, I want to name it my nginx. Um, and of course, we have to plug in the hash of the manifest over here. So, this references the right manifest. Um, and then we have to generate a file that is just telling Docker basically, "Hey, we're using OCI version one." Um, this has to be there. And now, we can create a tar archive out of No, not that. Out of um, this folder here. Um, it's now the my nginx.tar, and we can load this into Docker. It says, "Right, we loaded my nginx:latest into Docker." And the ultimate test now is if I use a Docker Docker I have to wait after some time. Yeah, we run this. And if we do a

**[15:47](https://www.youtube.com/watch?v=AVr_6VN1-24&t=947s)** curl, we see if we build without Docker. Great. We build a Docker image without using any uh type of engine or demon or tools, whatever. We just plugged in a file. Pretty impractical to do in the CI/CD pipeline because you have to update like 3,000 hashes and whatever. Um but basically that's um the whole mystery behind it. Um now the interesting thing is what happens if we delete a file. Because if we delete a file, um we cannot mutate the base layer. We have to somehow do it in the the upper layer. Um and this is now the the next thing that is very interesting about Docker. Um what happens if we delete a file? So, we have our base layer here. We have an index.html file and a build cache.tar. This happens pretty often if you do um for example, you do a npm build of something, then you have a 2 GB node_modules folder,

**[16:50](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1010s)** and then you just delete that node_modules folder um because you don't want to ship it to production uh um and your image is done, right? No, because Docker has a concept of layers. So, everything that happens is we create a second layer, and it marks this file as deleted. So, we have these files called whiteout. A whiteout is just a marker that this file should not appear in the final image. Um and the actual file will be shipped. So, this is why your image is still 2 GB in size. Um and this is why we have multi-stage builds because if you build a fresh image and just copy the actual build outputs into this image, um we're good. We can see these whiteout files. They are pretty um, easy to understand. Um, we have a little docker file here, um, where we just um, add a file 1.txt and 2.txt. We make this based on the Alpine image,

**[17:54](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1074s)** and then we remove the 1.txt file. And if we then, um, do all these unpacking of the layers and so on, um, we end up with an image that has, um, just three layers, um, I think. Well, okay, let's do it from the uh, from the start. Um, we Where's my Yeah. We do We build this image and we save it as a wh.tar file. Ah. Switch into the right directory. Do this again. So, we have this, um, tar file and then we unpack it into the image folder here. And now we have to read through this index to find the right layers. Right, we want a 24841. Uh 4841. Then we want the, oops. The, arm image that we have, 029. 0 Where is it? 029. Yeah. Um, here we see three layers. This is the Alpine base.

**[18:50](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1130s)** This is the one that contains one and two txt. This is the one that contains the whiteout. And we see even deleting a file has some space requirements. And, um, we can also look at to the, uh, layer where this file should be deleted. I'm using tar TF inside image blob sha this. And here we have the whiteout file that now notifies Docker when it's loading these layers again that this file should be deleted. Now, this is not how it works when we actually instantiate the image um because Linux does not understand what a dot wh. file is. Um for that we have something that is called overlay FS, um the overlay file system. And this is something that you probably get along if you use Docker. We have overlay one and overlay two. And how this works is a very interesting functionality of the overlay file system which consists of a lower folder and then upper folder. The upper one is

**[19:56](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1196s)** read-write. The lower one is read-only. And the lower one contains everything that is in the base image and the upper one contains everything that is um well, that gets created or deleted within the current session that we're running. Um so if I run for example in Docker run Alpine and I do an exec to get into the container and create a new file, I will end up um with the file in the upper layer. And if we delete a file then we get a a whiteout file, but this time it's a character device which is a special type of inode in the Linux kernel um that marks the file as deleted. So in the lower layer it will still be there. Um and we can also do that from our command line to look at this this overlay um file system. Um one side note, um if we have multiple base image layers like we had

**[20:59](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1259s)** um here, right? We have three layers. We can even tell OverlayFS to stack these layers together as a um lower one single lower layer. Um and this is the improvement from overlay to overlay two. Overlay two supports up to 128 um base image layers, and therefore we have to do much less um configuring our like here with the old overlay, we had to copy those files into one directory um to mount it, and here we can just say, "Hey, there's all my three base image layers. Take them as the lower layer." Um and now we want to do this on a command line. I have to do this in a little Linux virtual machine. And the first thing I'm still copying my commands over. Um the first thing that we're doing is we create all the required folders for

**[21:56](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1316s)** this. So, we need a lower folder. This will be our base image. We have an upper folder. This is our working folder. We have a work. This is a something the kernel needs. I don't know what gets in there. And then we have merged. This is our container file system. So, that's what we would see if we would be inside the container. Then we create a very sophisticated base image with this base.txt file. Um and now we can mount this. So, we have to tell it, "Hey, we want to create an overlay um file system. Our lower um folder is lower. Our upper folder is upper. This is our work folder, and the the final product should be inside merged." So, now since we have inside our lower a base.txt file, we expect this uh to be inside our upper folder. Uh not in the upper,

**[22:55](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1375s)** inside the final container uh image inside merged. Upper still empty. If you want to get something into upper, we could simulate that we, for example, write something inside our container, right? So, I add this now to merged. So, this is inside my container. And if I now look into upper, of course, I have this file there. And if I look inside merged, I also have this file here. And I still have the base image. Um And if I now want to delete, for example, the base txt file, the overlay file system implements what is called as a um copy on write. So, in the moment that we write to a file inside the base, this file gets copied over to the to the upper layer because we changed the content of this file. Um So, if I, for example, do an echo new into merged um base, I can do that. Um if I catch merged base, new. If I do that with the uh lower,

**[24:02](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1442s)** from base image. Because we cannot change base layers. So, this will cause the file to be copied over. Um meaning if you have also in a Uh well, we get to Docker files in just a minute. Um I want to show you what happens if we delete the file. Um so, we delete the merged base txt file. I can do that. Um it's still inside the base. However, if we look inside the merged one, we don't see it. If we look inside the upper one, we see this character device that will then be transformed into a whiteout file, which then notifies inside the star archive that we we pull from the Docker registry that this file should be deleted in this layer. And this is basically all the magic that happens in Docker images. And if we want to talk about what

**[25:00](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1500s)** happens if we build a Docker container, um this slide is pretty good, I would say. Um we have a little basic Dockerfile here with some run commands, some copy commands, and some commands that for example set environment variables. So, and if we remember what different files we have inside this layer, um we see that for example from Alpine is really just configuring what base image we're using. Then we have something like env or workdir or command, those are only config changes inside the JSON files that we had. So, no impact on any layers. Then we have run um and copy, and these mutate the container file system. And those are executed in a way that um we create a container and run it up to the point before this command, right? So, we mount the file system, we set up a completely container, we run this command, something happens that changes our overlay file system, and those changes are written into the

**[26:05](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1565s)** upper folder. And then we make a tar archive out of this upper folder, get our new layer, and then we do this again. So, we mount basically um again everything up to now this point, and we execute the copy command. So, we mutate the upper layer, and we mutate our container file system. This mutates the upper layer. We zip this all together, have a new layer. Meaning um what I already told you, if you have things that you don't want to have inside your container image in production, you should not delete it because it won't be deleted, it won't will just be invisible. Um you have to you start from a new from maybe down here. Um and then you have a new um stack of layers that has built up. And that basically is everything that happens if you run

**[27:02](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1622s)** Docker build. Um there are tools like the build uh build a tool, however it's pronounced, that do this in a demonless way. Docker does it with a demon because it basically execute a Docker image. Um but for example, if you just want to add a simple file, um it's just file system changes and zipping something as a tar archive. Um little summary of all what I said you. And now if you want to have the slides and the code and the demo, you can just scan this QR code. It's should hopefully link to my GitHub. Um and well, then thank you very much for joining today and have a great rest uh day at this conference. Great day tomorrow. See you. Thank you a lot. >> [applause] >> Awesome. Great internals on Docker. That's a fantastic deep dive.

**[27:59](https://www.youtube.com/watch?v=AVr_6VN1-24&t=1679s)** Let's see if we got any questions. Of course, now the iPad is not working. Um we have 3 minutes for questions. Um and this is obviously not working. Excellent. Well, >> they can also just meet me. >> Yes. So if you have any questions, if you had anything, you can definitely meet Oliver right outside over here. But yeah, give Oliver a nice round of applause one more time. Thank >> Thank you so much. >> [applause]
