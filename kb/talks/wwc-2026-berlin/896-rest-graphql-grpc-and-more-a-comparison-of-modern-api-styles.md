---
id: 896
title: "REST, GraphQL, gRPC, and more: A comparison of modern API styles"
slug: rest-graphql-grpc-and-more-a-comparison-of-modern-api-styles
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Backend & APIs"
type: "Keynote/Talk"
stage: "Stage 7"
tags: ["APIs", "ASP.NET", "Software Architecture"]
speakers: ["Florian Bader"]
speaker_companies: ["Lunaris Digital Solutions"]
day: 2
starts_at: 2026-07-10T09:40:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=zYqhUmZLbu4
video_id: zYqhUmZLbu4
session_page: https://app.wearedevelopers.com/events/16/session/896
transcript: true
---

# REST, GraphQL, gRPC, and more: A comparison of modern API styles

**Florian Bader (CEO — Lunaris Digital Solutions)**

`Track: Backend & APIs` · `Type: Keynote/Talk` · `Stage: Stage 7`

`#APIs` `#ASP.NET` `#Software Architecture`

[Watch the recording](https://www.youtube.com/watch?v=zYqhUmZLbu4) · [Session page](https://app.wearedevelopers.com/events/16/session/896)

## Abstract

The API landscape is diverse: REST, GraphQL, OData, gRPC, WebSockets, server-sent events, and more. This presentation will demonstrate live the advantages and disadvantages of each approach and when each style is appropriate. You will learn whether it makes sense to provide several in parallel and how common logic can be used across different interfaces. Practical examples with ASP.NET Core will show you the concepts in action. By the end, you will know which API strategy suits your project and how you can switch flexibly between the options.

## Speakers

### Florian Bader

*CEO — Lunaris Digital Solutions*

Florian is a Solution Architect at Lunaris Digital Solutions and a Microsoft MVP for Azure IoT and DevOps with many years of experience in DevOps, Cloud, and Digitalization. He helps companies develop efficient and effective solutions that sustainably lead their digital projects to success.

## Transcript

*5,457 words · source: yt (en)*

**[0:03](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=3s)** Thank you. Nice to see every one of you. So many people joining for a topic like this. It's always exciting to have like these kind of basic topics and then still seeing so many people in there who maybe need some introduction into these basic topics. So what we are looking at today is disclaimer before we start. There is no AI in this talk. So if you want to leave now, feel free. >> [applause] >> And we also we also talking about modern API web API style. So if you here for like older styles soap or Corba or something like that, I also need to disappoint you. Thank you for the introduction. I don't need to talk too much about me. If you have any questions, feel free to connect on LinkedIn. Feel free to send me an

**[0:50](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=50s)** email. I'm solution architect. So I'm working mostly with customers. I'm also one of the founders of Lunar Digital Solution. So we're based in Stuttgart in Germany. We're doing development software development and also consultancy with customers building their software products with them helping their development teams get more efficient. And all the learnings that we have we are sharing with with you guys. We are sharing on conferences. We are sharing also on our blog. So if you're interested, visit our blog. It's in German, but I mean there's translation tools out there. So what's the key takeaways for the talk today? I mean we have 30 minutes. So it's going to be an overview. So we're not going to do a deep dive on the topics. But what I want you to take away is what web API styles are out there. What are they what is available?

**[1:39](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=99s)** What is each API style also to try to solve? What kind of problems is it trying to solve? So when should you actually use it? What's also kind of the negative? What are the trade-offs that you might face if you're introducing that one? And also what are you facing if you introduce not just one of them but multiple in parallel. And how can you make a good decision? So, which one to choose depending on your needs. So, I brought up nice scenario kind of coming from the IoT space. That's kind of the basic scenario that we're dealing with with a lot of customers. So, they have some kind of IoT devices out there sending telemetry data and now you need some kind of configuration platform in the cloud for example. And you have a web front end you have a web API that accesses data on your database. Pretty straightforward. Keep in mind

**[2:28](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=148s)** there's some further requirements later that might be useful. We need to show KPIs in some kind of dashboard of course. There's alarms, notifications that need to be sent out. Motor temperature too high. Someone needs to be alerted on that one. And of course the data needs to be exportable. So, it needs to be there for further analysis. Might be interesting later. First thing, REST API. The one thing probably everyone of you has done. Who is honest enough to say they are doing REST but not actually doing REST? Thank you. So, kind of REST I mean there's this whole thesis from Roy Fielding on this one. I don't want to go too deep into that one. You all know what it's about, but it's often also misunderstood how it's actually how it should be used. And if you're doing some kind of web API

**[3:16](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=196s)** everyone calls it a REST API in the end. Kind of the central idea is you're talking about modeling resources and each resource has their own endpoint. And then they have their stable URL path where you can access resources and also the details behind it. And then the clients are working with that representation of the resources. You can use HTTP for semantics. So, HTTP methods for getting data, updating data, deleting, inserting. So, simple CRUD operations. And you have response codes that indicate like is everything working? Did the client make a mistake? Did the server make a mistake? Pretty basic. In the end, REST API in terms of schema, in terms of describing what is available, you have, for example, Open API that gives you a schema that you can also consume on your

**[4:03](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=243s)** client to know which API endpoints are available, what's the data type for request and response. Um and also kind of how to further navigate on that one. Kind of going further, there's also this thing um I guess also in the REST specification already, which is called HATEOAS or however you want to um call that. Um which pretty much basically links in your response where you say you have hypermedia uh that gives you links in a response for the client to consume, and you can make your resource endpoints in the end navigatable for the client. So, you wouldn't even need Open API for a schema because if you're just introducing that one, the client by just making a request and getting a response already knows where it can go next, what kind of additional data it can uh can

**[4:52](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=292s)** use for that one. So, that's something not everyone is maybe using uh which is a good introduction of also uh giving you more kind of documentation for the client, if you want. Uh other than Open APIs the most widely spread where you can base your documentation out of it, you can generate your clients in different programming languages, you have testability based on the specifications, and you can do also governance like naming, checking if all the response codes are correctly chosen, and so on. So, you take just your Open API specification, you put some kind of linter on top of it, and you can regularly check if your teams are following uh whatever you declare as your standard. You might have some additional stuff like querying, which is mostly needing pagination in some kind of form because you don't want to always

**[5:40](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=340s)** return all your data, you need filtering, sorting, searching, all kinds of that stuff. And most of the challenges you will face with REST is over fetching, under fetching. So, you either get too much data or you get not enough data, and you need to make multiple more requests to get all the data that you need. Um or you have all the bandwidth problems, maybe even on mobile devices or IT devices, uh where you get too much data that you don't really need. You might have specialized endpoints, like specialized endpoints that are just giving you less data for mobile dashboard, for example, which is not really nice. And you might have also inconsistent query parameters for stuff like filtering, sorting, searching, if you don't kind of standardize this, especially over multiple APIs that you might be creating. Uh if we're looking at kind of a final

**[6:27](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=387s)** assessment, and this is on the final slide, so I'm going over this one real quick. Um it's very easy to introduce, it's hard to master. You have a good framework with OpenAPI. It's kind of flexible, it provides you the resource with all the data it has for that. You cannot just get more or less on that one. Performance is nice. I mean, you have client-side HTTP caching that you can use, compression. Um JSON is easy to debug. Um you can just query with your browser and see what kind of response you're getting. And you have for evolution and breaking changes versioning that can be introduced on the endpoints, however that looks like. And on opera- operability side, you can easily log what kind of request was made to your REST API for uh yeah, debugging reasons, latency checks, and so on and so forth. So, pretty easy to introduce that.

**[7:17](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=437s)** If our platform now grows a bit more and we get from our product owner, product manager and then you require me that we now need a mobile application that shows KPIs as a dashboard, uh which was our further requirement, maybe you need a bit more. Um and this is where GraphQL comes into play. So, in then GraphQL was exactly born out of this need of having um mobile application, different clients that might need data in a different shape, in a different way. Maybe more, maybe less data. Kind of fixing that issue in REST with with over and under fetching. So, initially developed by Facebook, now open sourced or in developed in the open. Um the idea behind GraphQL is that the client basically tells the server what kind of data they need, in what kind of shape they also need the data. And the server

**[8:06](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=486s)** delivers. Which is nice, because it's not just about querying, as you can see on the left side. Um there's also the concept of mutations, so you can also mutate data, which is basically insert, update, delete, if you want. Um so it's pretty much something that you can also use instead of REST to have the same CRUD operations available for your client applications. Uh in the end what gets a bit harder on that side is uh you kind of putting a bit more pressure on your back end on all your back end team, because now the client dictates what it really needs. And often when GraphQL is introduced, it's kind of misused as a database browser. So, you kind of one-to-one mapping your database tables, and you're kind of directly querying against the database, and that's not really a nice idea, because this might

**[8:55](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=535s)** still have business logic behind this. You might need still need validation rules, uh and it's definitely a better idea to expose data transfer object DTOs instead of your domain entities or your database tables directly. Um kind of the problems with GraphQL uh because the client kind of can decide what they need, and kind of the client could be also malicious actors or malicious users, um they can also query a lot. They can do deep object graphs, and kind of hammer your system. So, we're talking denial of service here also, if you're not too careful. Uh but we can also have problems like um N+1 problem. So, you see on the left side a query where we have devices, and for each device we're getting telemetry data which might be in another database, might be on another API, so there's another request for that

**[9:42](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=582s)** one. And depending on you how it how you implement data receiving in the backend, it might be that this one is going over all your devices and for each device single-handedly querying then the telemetry data for that device which is not really performant. Um so [snorts] that's kind of where that problem comes from uh which can be solved. So there's a concept of data loader where you can basically batch together multiple requests of telemetry for devices into one big request and then uh kind of actively work uh on that problem. You also have the concept of cost. Um so you can for example say how much does it cost to query certain data or to query certain uh branches of data if you want. And then you can give kind of a a budget to your client and say if it goes over the the budget in terms of cost, you're not allowed to query or you have

**[10:30](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=630s)** operation timeouts to kind of restrict that access. Kind of issues with with GraphQL on that side is also caching. I mean, you see on the left side that's basically one post request with a big payload. Um so there's nothing like in REST where you query where you cache based on your endpoint and maybe even query parameters. And a query can look different every time. So there's concept of for that one where you can have plan ideas when uniquely identifying um the query that you're executing that you can then reuse, you can cache that also in the backend. And you of course always can do server-side caching. Operation is also a bit harder because it's not like one endpoint you're making a request against that you can then identify in your tracing tool of choice. It's again a post request against the same endpoint

**[11:18](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=678s)** every time. So for this one you can use things like passing an operation name with your query. So you can easily also identify which query is taking how long, which query was failing, which queries used how often, for example. Quick comparison. Uh for developers, definitely uh a higher learning curve that you're seeing. Um you're putting a bit more work on the back-end team here because they need to implement all those different data resolvers, data loaders, and stuff like that, dealing with all the clients going rogue uh situation. But you're really flexible in that case because you can easily put new clients on there. They can decide what they want to see, especially if you have also external parties that need to access your application. They are free to query the data in whatever shape they want. So, that's very nice thing. In terms of

**[12:08](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=728s)** performance, a lot of things to watch out for, like the N+1 problem that we saw with querying telemetry data for devices. Um but it's also how you in the end, based on a query that is passed by the client, uh shape the database query behind, how you choose the indexes on your database so that uh data can be also queried more efficiently. In terms of evolution and breaking changes, um because the client only requests what they really need. If you're adding new stuff, it's pretty easy. The client never sees that because they don't request the new data. So, you can easily add changes to that one. And there's also a concept of deprecation. So, you can actually tell the client if certain fields are getting removed in the future, and then the client can change based on that one. So, kind of API versioning on that one also

**[12:55](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=775s)** gets a lot easier. And in terms of operability, you also saw taking operation names makes it also traceable on what kind of request that you're doing. So, enterprise, of course enterprise. Someone wants to have Excel access your data because enterprise. Um So, what should we do? Um we could either use REST or GraphQL and somehow integrate that into it, or we can use OData because Excel loves OData, and probably you also have Power BI for some reason, then you also love OData. Um and OData is basically kind of a standardized rest data access protocol. Um so you also model kind of your different entities that you're having that you want to query. And it's highly optimized on how to query the data. So you see on the

**[13:43](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=823s)** left side you have filter criteria that are standardized on how to query certain fields to get only the data that you really need. You can do the over under fetching issue with a select query parameter to only select the fields that you really need. There's expand in terms of uh resolving nested properties and crafts and so on and all kinds of different stuff. So this is nice because you kind of get um very standardized way also standardized in terms of ISO from uh Oasis um in terms of standardized way that other applications can just say if they support OData, just go against that OData endpoint and you can easily fetch the data that you need. And most of the time kind of the big pain on this one is really on how to model the data because when I said GraphQL you shouldn't use it

**[14:30](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=870s)** as a database uh database browser, this kind of goes into that direction of actually accessing the database objects or tables directly. Um so you can do that. You can define your own entity data model that is then available for the client to query. There's also meta metadata that you can then receive from the at the entity data model. Um so the client actually knows what is available, how to fetch it. And it's also create update and delete operations. So you cannot just receive data, you can also now in Excel do batch operations for example if you want. So if you're too lazy to implement that at rest, go with OData and you can have batch inserts um directly out of the box. So um when should you actually choose OData? Um I mean normally rest is fine if you can also standardize on your query

**[15:19](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=919s)** parameters. If you can also standardize on the implementation of how they will be passed through to your database access layer, however that looks like. Um so it can be executed directly on a database. We don't load all the data in your backend and then you query in memory, which is uh from a performance perspective not very good to do. Um so definitely OData is something either you choose it and you use it as your only one or it's a good addition to what you already have. So you kind of fit that requirement that you might get from from enterprise requirements. If we're looking at the final assessment, it's very efficient. Uh developers can learn it really quickly because there's frameworks out there that are basically doing all the job for you, providing the endpoints for you. You just need to

**[16:05](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=965s)** define your your entity data model in the end. Um and most data access layers can also pass through the filtering, sorting, and so on. So you don't even need to translate that into some kind of database query. It is flexible in terms of the data that's being returned. Um maybe not for the crafts that you've seen in GraphQL, so be aware of that one. Um in terms of performance, we're not in GraphQL like in GraphQL um passing the whole query as a as our payload, so this can be also cached on on client side with um caching headers if you want. Evolution breaking changes, same as with REST API. So you basically, if you change something, you need to version that in your entity data model or you version your OData endpoint and then you can use that. And for operability, the same as with REST.

**[16:54](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1014s)** Next requirement. Um we wanted to send out alarms if something changes in terms of telemetry. Um so we created a new alarm service and in the background background service um that is sending out emails, not by itself, but actually leveraging our web API. So would be nice if we can call the web API from the alarm service somehow. Could be REST, could be GraphQL, uh but could be also maybe something more optimized for that use case. And this is where often GRPC comes into play. Uh, so GRPC makes it very efficient to do remote procedure calls. I mean, that's the name, GRPC remote procedure calls from Google. Um, so you can talk between services um, using HTTP/2 as your protocol

**[17:40](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1060s)** and protobuf as your serialization format if you want. So, it's so it's a JSON, it's protobuf, uh, it's also more optimized because it's just binary over your wire. Uh, so this is why it's often used also in in microservice architectures or even in simple cases like the one I showed you where you have another service needing to call your web API and you don't necessarily want to go over the rest API, but you want something different. Maybe because also authentication is a bit different. Um, and you have the service-to-service authentication directly built into your GRPC endpoint. Um, on the left side you see the the protobuf schema for my device service in that case, not the alarm service. So, uh, basically you define different types on this one. You can, uh, do RPC method signatures and then

**[18:30](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1110s)** you can just use uh, generators to generate the client and the server code for you. So, that's pretty neat because you just need to define your contract in protobuf and then all your client and server code is generated for you. And on the server side, of course, you need to then just plug in whatever the the method should be doing, which is pretty nice. In terms of limits, what we're having, um, definitely GRPC is not automatically the best best answer because it's faster in terms of binary and how it uses the protocol. Um, it is service-to-service. So, if you want to use this from your front-end browser client to back-end, you can buy implementing a rest API in between. So, there is a REST facade or some kind of gateway that you can plug into, which is translating then the the the front end

**[19:19](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1159s)** REST API call over normal web to a service-to-service gRPC method call. Um which is then maybe not giving you the performance benefits that you're hoping for, but it's it's possible if you want to do that. Uh debugging, definitely something different, especially if you're starting new, if that's something new for your development team also. Uh you need to deal with Protobuf. It's going as binary over the wire. You need to know the right schema to decode whatever comes as payload, uh which might be interesting. It's also in terms of uh logging a bit more difficult because it's not pure JSON that you have the payload that you can look up if you want. Uh that's something that you should be looking into how that really fits into your dev teams if you want to introduce that. On the other side, what gRPC does very nice

**[20:08](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1208s)** because it has that very schema definition up front, versioning is pretty pretty easy on on that one. So, because you see the numbers on the left side for each field, for example, each field gets their fixed position, so you can add new stuff, remove stuff, and it gets just skipped. You can also version by and you see that at the very top by using packages, for example. So, by referencing then the right package version, you can just um yeah, go between different versions of the schema that you're providing if you want to. So, quick assessment on that one. Uh definitely for developer productivity, for internal services, it's nice because you can also based on a Protobuf easily generate your client and server code out of this one. And compared with REST, you don't need open API schema, whatever, because Protobuf

**[20:56](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1256s)** already defines the schema. You're going schema first, you define what your contract, what your types are, and then everything else in terms of code follows out of that one if you want. In terms of flexibility, I mean, your schema is fixed. You can version it if you want, but it gives you exactly the data that is specified there and in the way that it's specified there. In terms of performance, yes, it's a bit faster depending on how you judge faster. Definitely something that you can take a look at. Again, recommendation is service to service on that one. Breaking changes we already talked about and operability is something where you can measure for example each request by the method called that is being done and then looking at however you want to put the payload in those trace calls or whatever

**[21:46](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1306s)** you're using for operability. So, last update. I mean, it still stays the same, but the errors are a bit different because now we want live updates. So, the front end dashboard on the mobile, but also the web front end now need live updates if new telemetry data comes in. And at the moment we're just querying data once, so I mean the easiest solution on that one is do polling. I mean that's the easiest solution. You already have the endpoint. Just poll it on a fixed interval and you're getting the the newest data and you just need to update. But of course it's not the most performant way. You end up putting a lot of pressure on your on your on your back end. And we had customers that were introducing polling and then users and requests scaled up and then their database got hammered and you have

**[22:33](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1353s)** all kinds of problems with that one of course. So, best way to do that is if an update is received, you push out the new changes and if nothing changes, no update needs to be pushed out. And for that there's different options in the web that you can use. One is server sent events, which is purely server is sending something to the client as an update. You have web sockets, which is mostly used also for used for bidirectional connections, so the client can use the same connection to talk to the back end and then get information on the same channel back on that one. Or for kind of legacy stuff, you can also use a long polling, like polling, but maybe even every 2 minutes, so you're not putting too much pressure. Or what you can also use is if you already have GraphQL, and I showed you query and mutations,

**[23:22](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1402s)** there's also a concept called subscriptions in GraphQL, which is basically exactly for that one. So, you subscribe on certain data that you want to receive, and you see that on the left side how that looks like in uh .NET and ASP.NET Core with Hot Chocolate as the GraphQL framework. Um you can subscribe on certain data that you want to receive, and then behind the scenes it also, most of the time, just uses WebSocket to push the changes to your client. And of course, the client also knows how to deal with subscriptions, so everything is built in into uh the GraphQL for you. In terms of specification for all these live updates, AsyncAPI is something that you can take a look at, which is for event-driven stuff, if you want. Um also, if you message brokers, for example, and you want to specify what

**[24:10](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1450s)** kind of messages are um sent there, what kind of data is being sent there, definitely give that a look. Um often, you don't want to deal with WebSockets and server-sent events and also kind of fallback mechanisms on the transport level by yourself. Um so, on the on .NET, we have something called SignalR, um that is basically abstracting that away for you, so you have a so-called hub, which can be called from the client, so that's kind of the client calling server-side, which you can, for example, use for the client to subscribe to certain data, because you don't want to push all your data to all the clients. They should be subscribing to whatever is relevant for them. And then, on the code example, the the bottom statement is how you can then push out from your back end um data to

**[25:00](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1500s)** to the client that it can then use to update the UI accordingly. In the end, that's a nice way because it again abstracts away all that transport and fallback and authentication. And it also abstracts away scaling for you if you want. So, you don't have it just hosted on every back end. You might scale over multiple back ends horizontally. You might even have a different service just for your live updates. You're not hammering your API service for that one, but there's actually something different that can be used just for those live updates, that uh live communication. So, we saw a lot. Um how can you decide now what what should be the API style or maybe multiple API styles that fit your needs, that fit your project, that fit your problems? Um first criteria, definitely look at

**[25:47](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1547s)** your consumers. So, who is consuming your API? What kind of data do they need? Um what kind of flexibility do they need on the data? Um can they only speak REST and GraphQL is maybe not even an option? Can they only speak OData? The other things are not an option. So, that's something that gives you a good idea. Also, looking at the future, do you expect new clients to come into into your project and needing different kind of views on your data? Communication model is always nice. So, how does the data flow? Do you only need to request data? Do you need the server to also send out data? Do you need service-to-service communication, um maybe even bidirectional service-to-service communication? That gives you a good feeling if you need to go in the direction of gRPC, for

**[26:34](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1594s)** example. Uh in terms of data and interface, how stable or flexible does your schema need to be? So, going from gRPC, which is this fixed schema, which you have to GraphQL, which is very flexible even in the kind of data that is being requested. You can deprecate fields. You can add fields, pretty nice. Looking at your non-functional requirements, stuff like caching, latency, easy to debug, security, all these kind of things come into play if you look at all the API styles that we that we that I showed today. So, what is being really being required as non-functional requirement in your project? That's definitely also something that tells you which API can fulfill those non-functional requirements for you.

**[27:22](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1642s)** And last but not least, operational reality or reliability. What can your dev team actually sustain over time? So, what's the learning curve? Does your dev team already know about these kind of API styles? What's the adoption in the programming language that you're using? What if new members are joining? Do you need to train each new member on that new stuff or is it basically just rest that kind of everything knows, but you need to teach them standards or stuff like that. So, all of these can help you make a good decision in the end, but always do a spike first, of course. So, if you test something and you see it fits your needs, definitely try it out first and see if it actually fits your needs. So, to recap on that one,

**[28:10](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1690s)** there's different API styles, each tries to solve their own problems, so there's no go-to API style that you should always choose, that's always the best. Definitely before you start, see what fits best your needs. There's always trade-offs and definitely trade-offs are more important than any technical hype. So, just because everyone is using gRPC or GraphQL or whatever and it's pretty nice, it solves under and over-fetching and stuff like that, it's client flexibility, maybe it's still not worth like deprecating your existing REST API and moving to GraphQL just because of that one. Definitely look for standards in terms of schema, in terms of how to use the API in town, in terms of how to teach your devs on how to implement it. So, you avoid single-purpose approaches.

**[29:01](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1741s)** And last but not least, even if you go in the direction of having multiple different APIs like kind of having OData and GraphQL in combination for example, look into how you can share logic because most of them, most of the frameworks that allow you to use for example OData or provide OData and and GraphQL endpoints for you, they still need to fetch some kind of data in some form of way and some are even overlapping. So, you can share a lot of these data res- retrieval logic also of your business logic that can be then easily put behind another API facade if you want. There's also a lot of tooling out there, a lot of services in the different cloud platforms that kind of does that for you. So, you're just providing for example a REST API and you can put some kind of data

**[29:50](https://www.youtube.com/watch?v=zYqhUmZLbu4&t=1790s)** OData facade in front of it. So, if you want to do that, maybe that's also something. So, that's my conclusion. I hope I gave you a quick and broad overview of what is available there. If you have any further question, feel free to come to the front or also ping me afterwards. Thank you. >> [applause]
