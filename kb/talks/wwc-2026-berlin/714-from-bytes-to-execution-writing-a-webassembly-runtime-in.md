---
id: 714
title: "From Bytes to Execution: Writing a WebAssembly Runtime in Rust"
slug: from-bytes-to-execution-writing-a-webassembly-runtime-in
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Languages & Runtimes"
type: "Keynote/Talk"
stage: "Stage 10 - powered by TikTok"
tags: ["Rust", "Runtimes", "WebAssembly"]
speakers: ["Gaurav Gahlot"]
speaker_companies: ["IONOS Cloud"]
day: 1
starts_at: 2026-07-09T12:10:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=wRBDzv9Ai7E
video_id: wRBDzv9Ai7E
session_page: https://app.wearedevelopers.com/events/16/session/714
transcript: true
---

# From Bytes to Execution: Writing a WebAssembly Runtime in Rust

**Gaurav Gahlot (Staff Software Engineer — IONOS Cloud)**

`Track: Languages & Runtimes` · `Type: Keynote/Talk` · `Stage: Stage 10 - powered by TikTok`

`#Rust` `#Runtimes` `#WebAssembly`

[Watch the recording](https://www.youtube.com/watch?v=wRBDzv9Ai7E) · [Session page](https://app.wearedevelopers.com/events/16/session/714)

## Abstract

WebAssembly runtimes power everything from serverless platforms to container sandboxes — but their internals often feel opaque. In this talk, I demystify those internals by presenting Whisk, a minimal WebAssembly runtime I built entirely in Rust.

Rather than focusing on a production-grade engine, Whisk intentionally strips the runtime down to its essential components: module parsing, validation, memory handling, and instruction execution. Walking through these pieces makes the architecture of a WASM runtime clear and approachable, while also showing how Rust’s enums, traits, and safety guarantees naturally support VM design.

We’ll explore how an interpreter is built step-by-step, what trade-offs arise in a minimal design, and how Whisk differs from larger runtimes like Wasmtime or Wasmer. I’ll also demo Whisk running real WebAssembly modules (non-WASI), illustrating where the boundaries of a tiny runtime lie.

Attendees will learn:
- How a WebAssembly runtime works from the inside out
- How to design a small interpreter in Rust
- Why Rust is well-suited for building VMs and sandboxes
- The trade-offs between minimal and production-grade runtimes
- How lightweight engines fit into today’s WASM and cloud-native ecosystem

This talk offers a practical and accessible deep dive into WebAssembly internals — with Rust as the guide.

## Speakers

### Gaurav Gahlot

*Staff Software Engineer — IONOS Cloud*

Gaurav Gahlot is a Software Engineer passionate about building low-level systems, Rust, and Cloud Native ecosystem. He loves to share his learning via his blog, and is also a maintainer of CNCF’s Akri project.

## Transcript

*4,193 words · source: yt (en)*

**[0:10](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=10s)** Uh so, we have Gaurav with us, uh staff software engineer at IONOS Cloud. There's a silent uh presentation, so that uh you'll need uh headphone to hear yourself out. And if you have any question, just please feel free to post them on the app, and we can check them out later. Thank you. >> Okay. I hope you can hear me because I have no idea if you guys can hear me. Okay. This is so weird. Um okay, let's get started. I hope the font is visible, and if everything is okay, I will zoom in if

**[0:58](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=58s)** required, but I hope it's not hurting your eyes and it's visible. Awesome, thank you. This is so weird. >> [laughter] >> All right. Um so, I'll be talking about WebAssembly. It's a small project that I did for fun and learning, and let's jump right in. My name is Gaurav. I'm a staff engineer with IONOS Cloud. I recently moved to Berlin, so I'm super new, and I'm liking it so far. I do have a Twitter or X, whatever you call it, but I don't really tweet. Um this is my GitHub handle, and I'm a maintainer for Akri, which is a CNCF sandbox project. It's about um making your edge devices available inside a Kubernetes cluster, just so you can use them or request them very much like you request uh CPU, RAM, and all those things. I'm also working on a

**[1:45](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=105s)** project called KubeDRA. It's about writing device plugins um uh dynamic resource allocation drivers in Rust. And Tinkerbell, if you know about it, it's a bare-metal provisioning engine that I contributed to like a few years ago. So, uh let's get it started. Uh so, WebAssembly is actually neither web nor assembly. Why is that? Because it was initially started as a project that was aimed to only deal with web, but eventually it turned out that super useful and it's super tiny that it's got uh more and more adoption and towards edge specifically. It's not assembly as well because what we do with web when we write some code in a higher level in a higher language, it then compiles into a bytecode, which eventually can run on any platform, any um operating

**[2:34](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=154s)** system, and all that stuff. So, it's it's not something that you run directly on your machine. Let's take a closer look uh and everything will start to fit in and make sense when we look at an example with let's say Java. Uh so, Java has uh has been known for its JVM environment. What you do is you write Java code, you compile it, and it gets in it's into bytecode format, which you can run with a JVM. The same idea was taken and uh borrowed. So, you write a program in any language, be it Go, be it Rust, be it C, uh Zig, or whatever. You compile it into a WASM bytecode. Now, this bytecode cannot run directly onto um hardware. So, you need a runtime. That runtime could either use interpret work in an interpreter mode,

**[3:23](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=203s)** which Wasm does. It could also do jit compilation. Um there's some call optimized jit compilation. It could also do AOT. And then you execute the same code that you wrote in a higher language eventually as a WASM module on any architecture, be it ARM or x64. So, it's the same idea that JVM was carrying, but it's sandboxed by default. When you write a Java program, compile it to a bytecode, it's open by default, but WebAssembly gives you closed by default. So, everything in a wasm module is denied. You only get access when you really need it. So, it's just another bytecode. It's not specifically designed for web. It was started as in that idea. But, um it's a bytecode that can run on any platform. It's secure by default. Like I

**[4:11](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=251s)** said, it's sandboxed, which means it's uh it's not accessing your host. It's not accessing any thing outside in the outside the boundary of that wasm module. It only and only it extends request to the host. And if the host permits that you okay, yeah, you can do that. For example, let's say if my wasm module wants to write to a file, it cannot do it until and unless it has some functions that are provided by the host, which is the runtime. And only then it can write to that file. In terms of performance, it's near native, but it's only near native when it comes to uh AOT or JIT compile. Because the penalty that you take uh in terms of compilation is higher as compared to the runtime penalty, which is higher in um interpretation mode.

**[5:01](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=301s)** It's polyglot. Of course, you can write in any language and that it compiles to uh wasm bytecode. And eventually that can be run using any runtime. A few famous ones are wasmtime, wasmcloud. They We also have uh Spin Cube, which is uh again built on top of wasmtime. I think wasmtime or it's wasmcloud. Portability, of course, uh you can write in any language and your code can eventually run on any platform. So, we do have the portability. But again, I'll show in the demo that the the modules are super thin. They are super light. And it's very easy to uh move them around. So, WebAssembly comes in two flavors, or you can say two formats. What you see, this is the text format. And primarily you don't really end up using WAT, which is

**[5:49](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=349s)** a WebAssembly as text format. You don't write WAT by hand. This is more of a debug tool that you can use when you are Let's say you write some code, you compile it, but it's not doing what it's expected. So, you can use some other tools to convert that WebAssembly module into a WAT representation, and then see, okay, what's happening behind the scenes. There's this um cool uh WebIt, that's how they call it. This is a uh This is a repository which provides you a lot of tools. So, you can use uh WAT to WASM, so you can write a module in WAT just to for learning purpose. And you can also use WASM to WAT, which where you can have a WebAssembly module converted into the text representation and see what's happening behind the scenes. It's super useful.

**[6:36](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=396s)** But, you don't do it by hand, usually. Um what you do see is more like this, where you have um a program that is written in a higher language, and then it's compiled into a WASM, which is a byte sequence. Now, it at first, it seems to be like something gibberish, and it it does not have any structure to itself. But, it actually does. And we will see what the structure of those um these bytes form, because it's compact, structured, and that's why it's fast. Uh it's it adds to its fastness, basically. So, every WASM module starts with a header, and we will touch the header in just a bit. But, every WASM module has sections. It's like different layers put together, and eventually forming um a

**[7:26](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=446s)** WASM module. A WASM module at the when I started working on this project had 12 sections, but I think they recently introduced two more, so we have now have 14. Uh the recent two additions are primarily with relation to WASI, which is um for providing you standard implementation of some interactions that you want to do with the host. Uh the the ticks that you see is what Wisk implements because it was done primarily for a very small scope. That's why you I only implemented five. And these sections are done are implemented by any runtime that's production grade. Every runtime has four stages. It decodes your Wasm module because need

**[8:14](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=494s)** to understand what's happening inside it. Then it does some validations. It also does some instantiation because everything requires memory and it also has some globals. And eventually you want to execute it. Every runtime, Wasmtime, WasmEdge all those included and so does Wisk. However, Wisk was done as a very small project, a play toy and that's why it has a very thin layer on all these four stages. Now, just a quick intro to Wisk. I've been calling Wisk so far. So, not so long ago in past, there was a time when we wanted to learn something, we would just build things, not just prompt to AI and ask for a huge document that we may or may never read. We We We were to just try things on our hands,

**[9:02](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=542s)** burn our hands. That's how Wisk was born. It was the It was written in four days. Uh and I submitted it as a as a talk to a conference. So, it's a very small. It does not have any dependencies. It's written in Rust and it only supports uh five sections that we just saw and it only supports eight opcodes. And these opcodes are assembly opcodes that you have to implement as a runtime. It only supports uh i32 values. WebAssembly in itself supports i32, uh i64, f32 and f64. Everything outside these uh scalar values goes into memory into the linear memory. And it does not have any unsafe code. And there's a reason for it, which we will see in just a bit. So,

**[9:51](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=591s)** let's get into the meat of the talk. Um when you look at the byte codes, uh our Wasm module has a structure as I said. The first thing that you need to know is this 8-byte pattern. This These 8 bytes are actually the first four bytes give you the preamble or the awesome thing of a null value followed by a a static ASM, which makes sure that this byte code is actually a WebAssembly module. The next four bytes are the the version number. At the moment when I take this, uh it was version one, but I think it's progressive now. The next byte, which is the ninth byte, which is this one, uh 01. From here it starts the sections. And each line

**[10:38](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=638s)** uh shows you a section. The next one is a type section. The first byte, 01 and so on, 03 07 08, gives you the byte uh of the section ID. Each section has an identifier, and that can be stored in one byte because it gives you 127 options for sections. So, 01 tells me it's a type section. What this type section has, it has a signature of a function. So, 01 is the ID, which is right here. 07 is the length of that section. And eventually the next seven bytes are the payload, which is the section itself. Uh so, 01 uh uh 07 size and 01 and so on so forth, 60 tells me it's a function. And 01 tells me how many entries do I have in

**[11:26](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=686s)** this type section. So, I In this one, I have one entry of 60, which means a I know this because I have implemented by hand. Otherwise, you don't need to really know this. When the when you look into a code of any runtime, you will you can see how they are interpreting it. Next one is a function section, which has a function zero at index zero. It's It's what it does is It's basically a a middle layer that you can think of between the type section and the code section that we see at the very bottom here. So, there's a type where we say this is the function that we are looking at. It has It It expects two parameters and it returns an i32. Now, in the code section, there is There's a function body, which actually

**[12:15](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=735s)** does whatever it needs to do with those three things. And what it It connects to is the The code looks to the function. Function says, "Here is the the definition of the function itself." That's in the type section. So, any runtime does uh some validations at some stage. Risk does a very thin layer of it. The validation itself, when it comes to a complete full runtime, it's a production runtime, you do not just make sure it's a WebAssembly module in itself, but you also do that um the types, the the types the type section has a matching code section, and it also has a function that maps to it. You also want to make sure that all the memory allocations that you do at runtime will

**[13:03](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=783s)** be safe, and there is no violation of the memory at least. So, all those checks are done at validation point, especially in JIT compiled AOT compiled runtimes. It's well-typed instructions. Um Eventually, what you want to make sure is that during execution, you just have um a machine code if you are doing jet, and you don't have to do at uh any sort of validation at runtime. That's what the validation basically does. So, executor doesn't have to worry about anything. It just blindly executes. So, Wasm only does eight bytes and just takes a leap of faith. Okay, I trust I trust you, bro. Uh so, it only checks for the first

**[13:50](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=830s)** eight bytes. It says, "Okay, there's a magic number to it, which is again hardcoded as a constant in the in the code itself." And it then just compares the version because I only targeted version one, so I everything is hardcoded. If I were to improve the validation, the next layer will be to compile on a compile, but validate whether the type section has a matching function section, and it has a an equivalent code section. That's how it would compare. And then you improve those layers by checking is the memory allocated the linear memory is allocated properly or not. Is So, when a function executes, will it have all those parameters that is expected on the stack? All those things. But Wasm does a very thin layer. That's all the validation that Wasm does. Again, Wasm was never meant to be

**[14:38](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=878s)** another runtime. It was just a hobby project that I did to understand what's happening when I run a Wasm module. It was a learning project. Before the age of AI. Now, instantiation, allocation of memory, it's very important because as I said, WebAssembly by default only supports i32, i64, f64, f32, and f64. Anything outside it, let's say strings, or let's say if you have slices of array of data, it all goes into the linear memory. What eventually you get during the runtime is the index where that in the memory where it's stored, and and the length of the data. And eventually, your runtime will go to the memory index,

**[15:25](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=925s)** fetch those many bytes, and give it to you. That's how it works. There is no concept of arrays within WebAssembly. So, the host is responsible for resolving all the imports. And what import is as a For example, let's say I want to print something onto console as a WASM module. I do not have that capability inside of WebAssembly. What I have to do is rely on the host providing me with the the right functions like FD write on or whatnot, so that I can as a as a WASM module, I call out to the host and say, "Okay, this This is the address of the string which I want to write. This is the length of the string that I want to write. Could you write it for me?" And that's how it goes to from the WASM module, which is the guest, to the host, and then it's

**[16:14](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=974s)** eventually gets printed. RISC declares the shape, but it actually does not do anything with memory. I never went to that stage of actually implementing the linear memory. I just ran out of time, and other things got priority. So, RISC actually does not do any memory management. It just declares it. So, if a module has something, it can just instantiate, but it does not really do much with the memory itself. Um As a final step, we actually start executing. Any runtime walk the instructions in a stack machine model. Now, we we know that WASM is stack-based. A lot of people think that WASM is fast because it's stack-based, but that's not

**[17:03](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1023s)** true. Uh stack-based machines give you an option that you can interpret, or you can jit compile, or you can AOT, but those decisions are the ones that make your execution near native, not just being stack-based. What stack-based basically means is let's say we have a function called run, and what we are doing is we are simply taking two parameters, uh five and three. Then the first thing it's because it's a stack stack-based machine, you do not get registers. So everything lives in a stack or in the local store that the function gets. So when we do a local get zero, it only straight away goes to and check, okay, there's a five, and it pushes it onto the stack. So there there's always one entry on the stack. The next one is we do a get one. So this

**[17:52](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1072s)** zero and one is basically the index or in the local store which uh the runtime will fetch, and it believes that since we are here, it's already validated uh that the values will actually be present, and it can be pushed onto the stack. So we get zero, we get one, we push it onto the stack. The next function add simply pops those values, adds them, and straight away uh pushes the result back to the stack. It does not return anything. In the end, which is the return call, simply goes to the stack and pops the last value and returns it. So here it's it's a stack machine. If it were to be something else, we would have registers and whatnot, but because it's WebAssembly is uh stack-based, everything works on opcodes and

**[18:39](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1119s)** operands. Now Wasm being a tiny interpreter, and the reason I say it's an interpreter, it's uh because it does not do any jit compilation. It does not do AOT. Uh more uh sophisticated runtimes do jit compilation. They also give you an option to AOT if you can. So we only implement eight opcodes, and opcode meaning uh like we have local get. These are the opcodes that you have to actually implement. So let's take a example. And one more thing that that's worth highlighting is um Wasm at the moment does not support and it's probably never going to support uh loops or if conditions conditional navigation through your code. It's just

**[19:26](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1166s)** works in a straight line. So, it reads the bytes and uh it just executes them. But, at the same time, let's say if you were to write a a loop where you want to print a few things, that it's not possible with Wasm at the moment. So, here are a few um Here are the opcodes that are supported. And I used Rust because it makes sense. Uh enums are very powerful in Rust and I did not want to do uh try any other language because I know it's it's good it's a good suitable language. And ISA being the instruction set architecture is a finite set of instructions that you want to support. It's highly possible to um uh put everything together in an enum. Eventually, when you go through each opcode, um you do a match, which is again a Rust thing, but you see, okay, there's an i32 constant, you put it uh

**[20:16](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1216s)** onto the uh stack. This is something that you have to do for each opcode. So, it's up to you. So, let's say if you want to take Wasm and do improve something and get a feel of what exactly it is to like implementing um a runtime, you just add an opcode and because it's Rust, everything will break at compile time and then you fix all the matches or all the compilation errors and you will improve the runtime because it's uh it will keep failing until you fix it. So, it's slow, but it's simple. So, how exactly do we make fast uh how the uh traditional runtimes make it fast? By using um JIT and optimized it. Uh uh JIT basically means is um uh let's say you have your whole code. When you run it for the first time when you run your Wasm module for the first time, it will

**[21:03](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1263s)** transpile it into an intermediate format that the runtime itself understands. It optimizes the order in which you want to execute and move things around. Uh and eventually the first run will be time-consuming, but the any follow-up runs in a for that will be much faster. Optimizing jit is a much better where you get um registers and also the runtime does a lot more in terms of uh it directly compiles into the machine code that you can run on the target itself. That's a lot also goes in line with the AOT. Uh and I'm running out of time. Here's a quick example of the same um function that can be implemented in what? Rust and go. Um so it's just a simple addition, which uh is possible to

**[21:53](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1313s)** run in both the languages uh all three of the languages or any other language. And once you compile it into WASM, you can simply execute uh let's say Wasmtime or any other runtime. You do an invoke because we're exporting the function run here and you provide the arguments that you want to run. And these will be executed as uh the one that we saw earlier, local.get and all that stuff. So as I said, a module cannot I have to rush because I'm running out of time, sorry. Um a module itself cannot do IO itself. It needs Wasmtime and Wasmtime gives you the the standardized implementations of certain functions that you can use um instead of relying on the um host itself, which also means when you add support to uh of Wasmtime into a Wasmtime module, the

**[22:41](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1361s)** size of the module itself grows. Um let's move forward. Uh this is I think the important one. A lot of people think that uh WebAssembly is still confined to web, but that's not true. There's already a shim layer that's available in containerd, which allows you to run Wasmtime modules alongside your containers in a Kubernetes cluster. Uh there's a spin and spin cube which is also again a CNCF based project which allows you to have fast but powered by WebAssembly. So function as a service but using WebAssembly. WasmCloud is super famous already, no need to talk about it. Component model and wasm wasi 2 02 is something that's combine coming together for combining multiple modules but using a well-defined interface, not just

**[23:31](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1411s)** piping bytes through them. I do not have much experience. I would prefer not to beat the bush around it. I just know that it's for type safety, but I don't know and all the whole works how it's working. So I'll not talk much about it. So WebAssembly itself gives you a much better cold start and it's super helpful because it's tiny. Here's a the four key takeaways. WebAssembly is based on stack machine. It's a binary envelope on the state machine uh stack machine. It's a runtime. Every runtime has to go through those four stages. It's small and that's why it's super useful in edge devices. And you could build a thin slice over a weekend. Um here are a few resources that you can check. Uh and if you want to take the slides, that's they're also available.

**[24:19](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1459s)** And since everything was in a terminal, uh it's all markdown. So you can just take a look and try things out. Uh before I drop, I have a few seconds. I can quickly show So if we take I have a these the same files that I showed. I will just quickly compile them. And uh let's see if I can type. Uh hello. star.wasm. So you see the size of these modules are so small. It's a function, but if you compile it as a binary, it will be lot larger. But if you do it with web assembly, super small. And we can run it. Let's take this example. So, it just it

**[25:07](https://www.youtube.com/watch?v=wRBDzv9Ai7E&t=1507s)** works for sure. But yeah, I'll stop here probably just have any questions if there are any. Thank you. >> [applause] >> Uh Thank you so much for this enlightening session. Uh if you guys have any questions, uh Gaurav is still available on the sidelines and he can address them. Okay. Thank you so much.
