---
id: 733
title: "Extreme Rules Lawyering with the ECMAScript Specifications"
slug: extreme-rules-lawyering-with-the-ecmascript-specifications
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Languages & Runtimes"
type: "Keynote/Talk"
stage: "Stage 10 - powered by TikTok"
tags: ["JavaScript", "Web Standards"]
speakers: ["Peter Kröner"]
speaker_companies: ["Storyblok"]
day: 1
starts_at: 2026-07-09T12:50:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=yP-uaoQ6ga0
video_id: yP-uaoQ6ga0
session_page: https://app.wearedevelopers.com/events/16/session/733
transcript: true
---

# Extreme Rules Lawyering with the ECMAScript Specifications

**Peter Kröner (Senior Developer Relations Engineer — Storyblok)**

`Track: Languages & Runtimes` · `Type: Keynote/Talk` · `Stage: Stage 10 - powered by TikTok`

`#JavaScript` `#Web Standards`

[Watch the recording](https://www.youtube.com/watch?v=yP-uaoQ6ga0) · [Session page](https://app.wearedevelopers.com/events/16/session/733)

## Abstract

Nerds all over the galaxy agree: TECHNICALLY correct is the best kind of correct! So why not mine the ECMAScript specifications for some technically valid JavaScript programs that “work”, but only by exploiting a wide variety of obscure semantic loopholes on the way? This talk goes far beyond mere syntactical obfuscation and explores the absolute fringes of JavaScript—for fun and for very, very little profit. Let us summon forbidden objects, unearth unholy functions that are not meant to be developer-accessible, and abuse features that TC39 would REALLY like to sweep under the rug.

Anybody can follow “best practices” and write “readable code”, so how about learning something actually new and unique? This session will instantly turn you into a living and breathing JavaScript infohazard, uniquely able to conjure up valid JavaScript programs that even ChatGPT could not hallucinate.

## Speakers

### Peter Kröner

*Senior Developer Relations Engineer — Storyblok*

Developer Relations Engineer and unrepentant web technology trash panda Peter Kröner reads web standard specifications for fun and writes about frontend tooling since the early 2000s.

## Transcript

*5,790 words · source: kome (en)*

**[0:00](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=0s)** Uh, hello everyone. My name is Satej. I'll be the moderator for this session. Hope everyone is having a very nice time in this conference. Today we have with us Peter Groner, senior developer relations engineer at Storyblok. Uh, and today he'll be speaking on the topic of extreme rules layering layering with the ECMAScript specifications. So, definitely something with ECMAScript and JavaScript, uh, Node and also I see also that he'll be sharing with us something with even ChatGPT wouldn't be able to hallucinate. So, definitely looking forward to this. This is a silent stage, so everyone would need a headset to listen to the speaker. And if you have any questions, those could be addressed towards the end of the session if there is a time. So, please use the app, um, to post your questions. Uh, please enjoy the session. Thank you. Now, I'll welcome >> All right, ladies, gentlemen, and non-binary nerds. Welcome to extreme rules layering with the ECMAScript specification. Technically correct is the best kind of correct. Now, I also probably should welcome you like as a professional to stage 10 powered by

**[1:02](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=62s)** TikTok for whatever that is worth. I don't really know, but I like the slogan, "Engineer responsibly and inspire trust." That's not what I'm about to do. Um, hi, I'm Peter and I'm a senior developer relations engineer for Storyblok, the greatest headless content management system the content management system in the world, but I'm also kind of a web technology trash panda. By that I mean that I really like to read web technology specifications. I'm serious. If I'm in the like in the queue at the grocery store, I don't open TikTok. I'm sorry TikTok. I open web technology specifications, have a scroll, and read whatever I come across. This could be the HTML specification, could be CSS, or could be JavaScript. And trust me, this can actually be fun, and you never know what you find. But in this case today, you will know what I found because we are going to talk about a few interesting aspects of the ECMAScript specification. Specifically,

**[1:57](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=117s)** we're going to fact-check what counts as common knowledge among JavaScript developers. First off, um you may be under the impression that since 1999 new keywords have been introduced to the JavaScript programming language, which is kind of true, but also in a way technically not really true. And we are going to investigate how this is only true if we attach a giant number of asterisks. Then, um classes you might um assume are a thing, and I'm going to argue that they actually aren't. Now, this is not the greatest surprise to people who are really deep into JavaScript, but I need to talk about this to set up my final point, which in my opinion is the most interesting one, namely that private properties on classes don't really exist. Or really, they don't exist in the form of properties. They are some weird grafted-on extension, more working like a weak map, and this has actually observable side effects. And we are going to dive deep into how JavaScript objects work to uncover the truth behind this. If you have any questions at any point, then that's great. Once I'm done,

**[3:01](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=181s)** you will have many more. Well, those came from. Okay, then let's get started. New keywords have been introduced since 1999, and in reality, that's not quite the case. Now, what is a keyword in JavaScript? If we go by MDN, the Bible of web developers, then it says a keyword is a special token that looks like an identifier, but actually has a special meaning. And the example given in this paragraph is the async keyword that you can slap in front of functions. And you know, in theory I can work with that. If I have a bit of code, if I have a function, and I slap async in front of there, then it works as a keyword, turns the function into an asynchronous function, and it looks like a keyword, it is highlighted like a keyword, it makes a whole bunch of sense. But do you know what's not a keyword? Async. Because async is only a keyword in a specific position, namely in front

**[3:57](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=237s)** of a function declaration, or in front of a method declaration. In any other place, it's just a plain old variable that you can use to make your programs read more interesting, I guess. Okay. What's this all about? Who cares? Who actually cares about any of this? Well, I do, because this actually tells us something really interesting about how web standards work. And what this tells us is that browsers always keep existing code working. The async keyword word is a conditional keyword that only works in front of function declarations, simply because somebody might have written some code way in the past, where they used async as a plain old variable. And we don't want to break existing code simply when we release a new browser version with a new JavaScript version with new features being supported. And so, every new keyword that has been introduced to the language essentially ever only works in specific positions, and in all other positions doesn't work as a keyword, but can be used for other purposes, which can lead to rather interesting code. My favorite example for this is the for of loop.

**[5:01](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=301s)** This is taken straight from the specification and tells us how for in or for of loop work. And you might see that where the arrow's pointing to, this of keyword is only a keyword in this specific position. This means if we take my very normal list of numbers and do what programmers do, namely create a loop over this list of numbers, then of in the middle there is a keyword, is highlighted as a keyword, and works as a keyword. But because if you are as close to the grave as I am, you might still remember a time before for of loops. You know that you can still use off as a variable name. It works as intended. Now, this poses an interesting question, right? Does this thing need to be named the value or could this be also called off? And you already know the answer. I wouldn't bring it up otherwise. This can be called off and so this loop now

**[5:56](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=356s)** starts to look way more interesting, but we can go even further. What about list? Well, surely this cannot also be named off, but I've got a surprise for you. It actually can be. The reason for this working is that a for of loop doesn't operate on the object itself, but it operates on an iterator of the object, which means that once the loop starts running, it doesn't really need the object that it's iterating over anymore because it already collected the data. It already collected an iterator over the object and it keeps working even though it looks like it shouldn't. But if you go deep under the hood, then this actually works. And again, this is true for every keyword in the language. You cannot just invent a new keyword out of whole cloth because to do this you would have you would have made sure that nobody on this planet ever used this specific token for any variable and you can't. So, this is something that you

**[6:53](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=413s)** can discover if you again poke at the edges. This works for many new keywords. For example, this guy also a personal favorite of mine. The thing to remember is all new keywords are context dependent and because in most contexts most tokens aren't keywords, I would say that we can around this towards there are new no new keywords, right? Right. Good enough. Come on. Now, there's way more in the realm of keywords that we could talk about. For example, people usually perceive undefined to be a keyword, but it's not. It's just a variable name that happens to point to a global variable called undefined, but it's not really special. It doesn't have any special meaning. You can reassign it to something. I don't really know why you would. Maybe if you get replaced by AI, you want to, you know, as a last commit, as a parting present, maybe. I don't know. But again, we are not going to talk about this. This works for undefined, works for not a number, and many, many other features, but that's not important. What is important that classes also aren't really a thing. Um

**[7:58](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=478s)** let me explain. If you have a class, your regular object-oriented basic example like this one, then you can use it like a class, like real programmers would do. You can instantiate an instance of the class using the new keyword, and you get back an object that essentially follows the building plan that was created in the constructor. Okay, great. Um this works. And our uh Toby is a cat. This also works. But if we apply the type of operator to the class object itself, then we get back function. Because classes are in essence just functions. If you are as close to the the grave as I am, you even may remember the bad old times when you actually had to write your object factories not as as classes, but as functions. So, this would be essentially the class of yesterday. Still works, still follows the same procedure, has a bunch of safety gear taken off it compared to a class, but

**[8:53](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=533s)** this still works. A class is just a function with a little bit of extra features grafted onto this. Now, of course you can write a um a function, and if you know JavaScript, you know that you can apply it with the new operator. But the important question is, how does line six still work? How does an object come out of this function? I mean, there's no return statement, right? But there is an implicit return statement. Every function that is invoked with the new keyword has an implicit return this attached to its very end. But it's a return statement in a function. That's implicit. But why be implicit when you can be explicit? I have always learned that my code should be explicit and not rely on any magic. So, we also can write this out in line four like here, and it still works. But remember, this is JavaScript. There are no types, there are no rules, so we can return essentially whatever. There's no

**[9:49](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=589s)** type checker to, you know, stop us from writing this code. The only question is, in line nine, does this actually work? Does this return this array of numbers and does it simply swallow my cat? And the answer is, of course it does because it's JavaScript, there are no rules and there are no types. Yeah, this works. Classes and uh functions are the same thing, which of course by extension means that I can also return whatever from a class class constructor instead of this and then can instantiate my class and get back an object that's completely unrelated to the class. Isn't that great? Okay, yeah, you might say um this is not really important. But let's circle back to the ECMAScript specification. The ECMAScript specification, first of all, clearly states that what I just said is true. The arrow points to the return function object, which is what's returned when the runtime evaluates the class definition. Okay. This works just fine. But again, who cares? Who actually cares

**[10:46](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=646s)** about any of this? Why would you do this? This has no real-world use case, right? Well, I would beg to differ. Um at least in my world, there is a use case. Consider the following. Here's my class example that has some field called data. Data isn't initialized, but it can be initialized, um for example, in a constructor or using an initialization method. And in my case here, it's actually important that this data field gets initialized because my method do stuff depends on data actually containing a value. This method will fail when data isn't set to anything important. So, no problem, you say. Let's just create an asynchronous initialization method that we can call that performs some asynchronous magic and then populates this.data. And bam, we can use this class just fine, right? We um create um an instance using new example and then we have to be extremely vigilant because if we now just go straight to do stuff, then we will get

**[11:41](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=701s)** back inscrutable errors because remember do stuff is written in a way that it only works when this.data has been populated. So, the entire class can only be used if we first create the instance, then remember to call the init method, await the result of the init method, and only then do what we actually want to do, namely do stuff. Now, this might be fine, this might be acceptable, I don't know, but I would really like to get rid of this. Can we actually make this work automatically? Can we enforce that the initialization logic, the asynchronous initialization logic, happens every time and that calling do stuff before calling init and waiting for the result gets to be impossible? Well, of course we can. We just have to rules lawyer our way to what a class actually is. So, your first idea might be we need a constructor to solve this problem. And it's kind of true, but it's not that as straightforward as

**[12:37](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=757s)** you might think because you would, as a second step, take the initialization logic and shove it into the constructor, which kind of works, but the problem is that you cannot use await in a constructor. The constructor cannot be decorated with the async keyword, keyword, and therefore you also can't await load stuff async in there. But, remember that browsers never break existing features, which means we can still unwrap the promise that we get from load stuff async using the old bad way of dealing with promises, namely by calling the then method and supplying a callback function. This is how we did it back in the day and it still works because nothing ever goes out of style. In this case, this first of all works, this waits for load stuff async to deliver a result, the result is called data, gets passed as an argument to the callback function, and then we can use it in the callback function itself. What's also important at this point is that using then on a

**[13:35](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=815s)** promise always returns a new promise. It's always a chain of do something, then this, then that, then the other thing, and every call to this returns a new promise. In this case, we get back a promise of undefined because we don't return any value from the callback function itself, but we can change it. But, first of all, we should probably do the thing that we are actually here to do, namely populate this.data. And now it works. Now this is as good as it had been previously when we still had our initialization method explicitly. Okay, fine. And now? Well, now this is still a promise for undefined, and I kind of don't need a promise for undefined. I can just get undefined at home. But, what I can do is return a value from the callback function. For example, I could return this. This, of course, in the context of a class in an arrow function inside of there refers to the instance itself, and I can then return this from

**[14:33](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=873s)** the callback function. This turns the resulting promise, the result of the entire expression from line five to line eight, into a promise on the instance, and because I'm in a constructor, and a constructor is just syntax syntactic sugar for a function, and JavaScript has no rules, I can return this promise from the constructor. So, instantiating my class now doesn't get me doesn't get me an instance anymore, but it gets me a promise of an instance, and the promise only resolves once the entire initialization logic has resolved. I mean, this looks weird, this shouldn't work, but it does, and I find this to be extremely useful. Because if I now call new whatever, I get back not the instance, but a promise for the instance, and I know what I can do with a promise, right? I can unwrap it using await, which means I cannot get access to the class instance without making sure that the the initialization logic

**[15:27](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=927s)** has run to completion. It's now impossible to call do stuff at any point where the initialization logic hasn't completed because, you know, how could it? We don't get access to the instance. So, this function is now impossible to misuse. And sure this looks strange. Sure you need to beat TypeScript into submission to make this work. But you can. And I think you should. Um so, we can see classes um are just functions. And because functions are functions and classes are functions, classes themselves aren't really a thing in my mind. Uh I I see the, you know, the rise of, you know, um questions, but still let me talk about classes that don't exist some more. Talking about another feature that doesn't really exist, private properties. Now, please hold onto your seats because now we are going to go off the deep end. You might say classes are very much a thing because there's at least one specific class feature that doesn't exist anywhere else, and that is, of course, private properties. Where you put up a little fence before your class field and then nobody can exit

**[16:31](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=991s)** access this except for the owner of the fence, which is the class the class itself. Now, what if I told you that this all um didn't exist? Remember how I redacted this entire green note in the previous slide when I showed this? Well, I redacted this for one specific reason. This note talks a whole bunch about something called brackets brackets private elements brackets brackets. And we need to unwrap this. What is private elements and what's this entire note about? The TLDR is as follows. Private properties um aren't really properties like any other property. If you have a class and you plug um a property called data on there, then it's literally an object property called data, but private fields are something that's completely different. Private fields are implemented in an internal slot named brackets brackets private elements brackets brackets. This notation with brackets brackets is how the ECMAScript specification denote what's called internal slots. These are

**[17:24](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1044s)** specification devices that only exist in the ECMAScript specifications and you can think of them as a sort of super private field that JavaScript objects only have access to on the level of the JavaScript engine. I mean, to be more precise, it's really just something that exists in the ECMAScript specification. This entire note, if you read it to completion, talks about how private elements is an internal slot that exists on objects, but actually that engines don't have to implement something that's actually a private element slot. So, this thing doesn't have to exist in your implementation of a JavaScript engine, but your JavaScript engine has to behave as as if this did exist. Does this make sense? It's entirely a whole bunch of vapor and it's not a concrete object property. It's something completely different that, depending on your JavaScript engine, doesn't even exist. The other thing that's important about private elements is that by the letter of the specification, this exists in every object. Every object, when it gets created, should get this slot, private elements. It's always there and it's

**[18:26](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1106s)** always not a back of properties. Now, I admit that class declarations are the only way to actually get some like data in there, but it's still something that's in theory available on every object. So, you know, classes can suck it, if you ask me. And the general behavior of private elements is completely different from how objects behave. If you want to break this down into a sort of TypeScript-like syntax, it's kind of like like if there was a giant weak map where all classes put their private fields into and the fields are not identified by a name, but the name of a private field is somewhat like a descriptor for a symbol. So, that two private fields, even though they have the same name, but exist on different classes, kind of work like symbols, so they are not equivalent, even though they share the same ASCII letters that they are constructed from. So, in essence, this is all smoke and mirrors and doesn't even exist, if you

**[19:23](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1163s)** ask me. Now, again, who cares? You wouldn't believe it, but also I didn't care. I've known this in broad terms for ages, but a few weeks ago at this point, I actually wrote a JavaScript program that, you know, failed to work as I intended because this is all true. Because private fields aren't fields and they don't exist and it's all a giant lie. Let me explain. Um okay. Let me explain in excruciating detail. You might be aware, or you may not be, um that every JavaScript object object implements a whole bunch of internal methods. You already know the notation, brackets brackets something brackets brackets. These are the internal um methods that exist on JavaScript objects and the way to think about this is whenever you write any syntax, it boils down to an operation on an object with a method called brackets brackets something brackets brackets. This means that every object can implement every relevant operation on its own terms for itself. So, two

**[20:19](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1219s)** objects could have two completely different um interpretation of interpretation of what it means to delete something because it's implemented on a sort of super private method on the objects themselves. Does this make sense? I hope so. Let's take a look at the um specifications for the the delete operator, the simplest operator that you can think of, right? Delete a.b deletes b from a. Makes sense, but if you take a real close look at how it's specified, then you, you know, see a whole bunch of code for error checking and special treatment of properties in strict mode, but when you get it down to the end where it's highlighted in yellow, you see that you go base object.brackets brackets delete and then uh you pass in the reference, the property that you want to delete. This is what's what's actually going on. Deleting something means getting past a whole bunch of sanity checks and in the end, calling a method on the object that

**[21:14](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1274s)** implements the actual act of deleting something. Um, is this relevant? Yeah, kind of. I mean, you can actually program with this. If you use the regular delete operator, you use everything that the specifications define. Error checking, throw errors in strict mode if you try to delete something that's non-configurable, and so on. But, you can also use the reflect API to essentially do the same thing, but circumventing all the details. This is more or less a straight way to go to the internal method, and circumventing all the sanity checks, all the error checks, all the exceptions, all the strict mode shenanigans. This is directly calling the internal method, and this is a roundabout way of achieving the same thing. Okay, why is all any of this important? Well, it's important because there are two types of object in JavaScript, the ordinary objects and the exotic objects. Um, the dif- the division works like this. Ordinary objects implement a specific set of internal methods, that's what you would usually expect from a

**[22:12](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1332s)** JavaScript object. Because, let's be real, for most objects, defining a new property or deleting something is all the same. Most objects aren't really strange. But, there are few, for example, regular old arrays, that count as exotic objects. Why? Because their internal methods have some deviations from the regular um set of operations. Arrays are special in that they have the length property, which kind of works like a getter setter, but is actually a magic key that has internal logic in it that, you know, tracks the array's length or truncates the array if you shrink the length. And, there's many more many ways to get a hold of, for example, ordinary objects. You invoke a class constructor, or maybe you create an object literal. This all works. But, if you want to do to to create your own exotic object, then you need a proxy. Who has used proxies before? Yeah, wow. Yeah, of course. They gave me the smaller stage because they knew that

**[23:08](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1388s)** only the few, the proud, the select nerds will want to learn more about this kind of stuff. And for the rest of you, um let me just explain very quickly what a proxy is. A proxy is an exotic object. So, it has all the internal operations with some deviations. And the terminology works like this. A proxy is a user-defined exotic object, something that you can just write in regular JavaScript using the proxy API, invoking the proxy constructor. You always base your proxy on a base object. The idea is always that your exotic object is composed from an ordinary part, which is your target object, and an exotic part that's defined by the handler. Now, your target is supposed to be the ordinary part, but nothing in the specifications um prevents you from using another proxy as a target. Your debugging might get complicated, but you totally can do it. Um okay, what's the handler? The handler defines the exotic logic by way of method definitions. You

**[24:04](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1444s)** supply an object called handler in this case, and on there you define so-called traps. Traps are your implementations of exotic logic. So, for example, in this case, I want to create my own implementation of brackets brackets delete brackets brackets. Makes a whole bunch of sense. In there, I can do whatever I want. Well, not really, there's rules, but in general, I can do whatever I want. Probably, I want to cause um the original invocation of the delete operation, which I do with a reflect API, but then I can add whatever extra logic I want, right? So, in this case, I simply um forward the delete operation to the original logic, and then I add a side effect. Something very simple, I simply log delete to the console, and only delete operations are now exotic. They have behavior that they previously didn't have. In this case, they print something to the console. If I then take my object and delete

**[24:59](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1499s)** something from it, then I not only get the delete operation working as intended, but I also get my side effect, my delete operation. And if I do anything else, for example, if I define a new property, then nothing special happens because I didn't define a trap for set operations, and so I get the ordinary behavior from target. And that's exotic objects in a nutshell. You can write a whole bunch of fancy meta programming stuff, or you can do something very, very trivial like what I'm doing right now. So, continuing on the topic of being trivial, you could write a proxy that intercepts all get operation. All property reads on the target get intercepted. How does this work? It's a prop prop proxy that implements a method called get in its handler. Make sense? This will write something to the console for every object. But, remember that classes don't really exist and that constructors don't follow no rules. So, in this case, I can simply

**[25:55](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1555s)** take the proxy, make it a proxy around this, the class instance, and return it from the constructor. This is actually extremely useful because if you want to create an exotic objects, you don't want people messing around with your proxy API specifics, you just want to give them a class constructor that they can invoke, and they then get an exotic object that they don't have to think about. So, this really works well, and now we have, you know, a constructor that when we invoke it, doesn't give us an instance, but it gives us a proxy for the instance with the side effect that this will log all property reads, right? Right. So, let's try what happens if we compare a private property and a non-private property. Because they are both defined on the class right there, and we can read from them, get the value out of them, and if everything works as I said, then we should get a console log for the first property, data non-private, but data in

**[26:53](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1613s)** private shouldn't give us any console log. So, let's test this. Let's create a static method for reading from an instance. The idea is that this static method gets passed an instance of example and then tries to read. We should get two numbers and two console logs, right? Okay, let's try. What do you think happens? Well, what obviously happens is an exception. I'm just saying that private fields don't exist, but they kind of work in that they prevent access by objects that don't have access to them because the example that I passed to example.read in this case isn't an instance of example. It's a proxy because we returned a proxy from the constructor. And the proxy for an object isn't exactly the same as the object. So, if this was able to exfiltrate private data, this would be quite a disaster because then private wouldn't work. It's bad enough that it doesn't exist. If it also didn't work, we would

**[27:47](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1667s)** be in real trouble. So, um what's my point? My point is that we can fix this real easily. Let's say we create a base class and return our proxy proxy concoction from there. This means that from the perspective of example, which now extends base, this has always been a proxy. It understands itself as a proxy by definition and so there's no more mismatch. This code will now work and this code will, in this case, actually give us two numbers but only one lock. Why does it give us only one lock? Well, because private fields aren't properties. If you take a look at the ECMAScript specification, then you find the abstract operation get value. No prizes for guessing what this is for. Um and you can see that there's a clear delineation between handling actual properties and handling private elements. Private elements don't go at any point through any of the internal operations. They are special case because they obviously, at some point, have been grafted onto JavaScript, which isn't really laid up laid out to support private properties, but it still

**[28:49](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1729s)** sort of does even though this all doesn't make any sense, and again, this has observable effects. If you try to write any logic that relies on intercepting every operation um that happens to an object, you cannot do this for private elements. This simply doesn't work and is fundamentally impossible. It's not you, it's the specification. This cannot ever work. So, in summary, we've seen quite a bit that, you know, doesn't quite match the engineer responsibly and, you know, build trust vibe of the stage, but, you know, they didn't tell me that this was going to be on there. So, you can now take this information and do with it whatever you like. I mean, you now know that you can write truly inscrutable for loops simply by abusing the technicalities of newly introduced keywords. You can also be really annoying about classes, and every time somebody comes to you and says, "Well, we in the back end write real classes like real programmers." You can tell them that you in JavaScript unfortunately can't do this because it's functions all the all the way down, and

**[29:51](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1791s)** you might as well commit to functional programming, right? Right. And of course, private properties don't really exist, which might be useful for your, you know, inner boomer who simply wants to reject all new technology and stick what has worked previously. This is all just pri- um special case scoped weak maps. Um there's way more nuance in there, but I'm running out of time, so we have to, you know, uh come to a conclusion at this point. So, any more questions? Um well, if not, then congratulations. You are now um I don't know. A walking JavaScript info hazard, I would say. I should probably um say at this point, like, for my employer, Storyblok, I don't have write access to any repositories. I do developer relations. I don't write code. Okay? So, you can still try out Storyblok if you really want to, and otherwise remember that you shouldn't lean too hard on your tools. Knowing the stuff, knowing how the language really works, and knowing where

**[30:48](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1848s)** all the edge cases are is what it's what it's really So, don't get confused by all the people trying to sell you stuff, except for me. Try Storyblok. It's a great content management system. Thank you. Have a nice conference. >> [applause] >> Uh thank you so much, Peter, for the awesome session. I think everyone can take uh their learnings from the specifications, especially I do. I think there's one question from Morris that there was an announcement that promises.then is outdated. Doesn't await replace promises. >> Yeah, okay. Why did I say that then is uh outdated um and does it replace promises? Well, it's not outdated um because there's an obvious use case for it. Um you may have gathered from my talk that I don't take myself or my topic or anything really that seriously, and you know, being a bit hyperbolic makes for good fun. That's it. >> [laughter] >> Uh thank you. >> Thank you. >> And if you guys still have any other

**[31:45](https://www.youtube.com/watch?v=yP-uaoQ6ga0&t=1905s)** questions uh Peter would still be available for some time on the sidelines to answer you guys. Uh hopefully you guys have uh the a better day and uh rest of the sessions at the Thank you. Thank you.
