---
id: 736
title: "JSX to Live Activity: The Story of Voltra"
slug: jsx-to-live-activity-the-story-of-voltra
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Frontend, Web & Mobile"
type: "Keynote/Talk"
stage: "Stage 13"
tags: ["Cross-Platform", "iOS", "React Native"]
speakers: ["Szymon Chmal"]
speaker_companies: ["Callstack"]
day: 1
starts_at: 2026-07-09T12:50:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=Qat0JURTkXU
video_id: Qat0JURTkXU
session_page: https://app.wearedevelopers.com/events/16/session/736
transcript: true
---

# JSX to Live Activity: The Story of Voltra

**Szymon Chmal (React Native Expert — Callstack)**

`Track: Frontend, Web & Mobile` · `Type: Keynote/Talk` · `Stage: Stage 13`

`#Cross-Platform` `#iOS` `#React Native`

[Watch the recording](https://www.youtube.com/watch?v=Qat0JURTkXU) · [Session page](https://app.wearedevelopers.com/events/16/session/736)

## Abstract

What if you could ship native iOS Live Activities and Dynamic Island experiences without writing a single line of Swift? This talk tells the story of Voltra, a library born from a viral tweet and a collaboration between Saul Sharma and Callstack to bridge the gap between React and native iOS extensions. We'll go behind the scenes of building a custom React renderer from scratch to translate JSX into SwiftUI primitives, bypassing the traditional "Swift wall." You will learn how we tackled the extreme technical constraints of the platform: from the tiny 4KB data limit to server-side driven UI that doesn't require a pre-shipped implementation. Join us for a deep dive into Voltra's architecture and learn how you can start shipping React to a brand-new platform today.

## Speakers

### Szymon Chmal

*React Native Expert — Callstack*

Creator of Rozenite, Harness and Voltra. The kind of person who dives into code and somehow makes the "impossible" happen. Focused on building the next generation of developer tools so you can ship your apps even faster.

## Transcript

*5,971 words · source: kome (en)*

**[0:00](https://www.youtube.com/watch?v=Qat0JURTkXU&t=0s)** Thank you. Uh so this presentation is supposed to give you a glimpse uh on how I was able to bring live activities which are native functionalities of iOS and how I was able to expand this approach from live activities to for example widgets and from iOS to Android because the library we are supposed to talk about today is Voltra which supports both Android Android and iOS u both live activities and vidgets. But before we go into the topic itself, before we dive into the code, because we will have some code snippets included, uh let me say a couple of words about myself. So I'm Shimon or Simon if you don't if you cannot pronounce the name in Polish. Uh so I came here from Poland from Toruni uh a nice city in the northern Poland and my work revolves around opensource development open source software because I'm currently a leader of something we call internally called stag incubator which is a space a group of people

**[1:01](https://www.youtube.com/watch?v=Qat0JURTkXU&t=61s)** playing with technology creating new libraries and this is basically our day-to-day work we don't work for uh for clients on some apps on projects we try to push the boundaries of react native we are uh contribute contributing to react native itself and that's pretty much my work and during my time in this incubator I was able to work on a couple of projects I created something I called Rosenite which is a way to add plugins to react native dev tools if you are a react native developer you know that there are no plugins so you cannot for example use query dev tools for react native with Rosenite you can h then I created hardness which is a good way for testing native modules so if you write some native module for React Native in for example nitro module system or turbo module system you can use hardness to test it in end to end fashion by writing JavaScript code just like you do with just and the last thing well not the

**[2:05](https://www.youtube.com/watch?v=Qat0JURTkXU&t=125s)** last thing but uh the big thing the last big thing is vultra so the library we are supposed to talk about today but the idea doesn't came doesn't it didn't came come from me it uh it came from Saul uh who is the original creator of Voltra. So he had this idea how to merge both worlds, how to merge React Native with uh live activities, how to make sure that we can uh have the same great developer experience with hot reloads with changes being applied automatically with no need for any native code. Uh but unfortunately he he had some troubles with finding time to finish the work. uh so we as call stack we decided to reach out to him and offer our services because we were also interested in this uh in this technology and that's why I'm here today because I was able to help soul and then take over uh the maintenance and uh future improvements of the library. So just to make sure you all understand,

**[3:08](https://www.youtube.com/watch?v=Qat0JURTkXU&t=188s)** Voltra gives you ability to create live activities and vidgets with no native code. You only write JavaScript/typescript. Uh we support both iOS and Android. So you can create widgets for iOS, you can create widgets for Android. Uh and we support something we call push and pull updates. So for example, if your live activity is displaying information about some kind of delivery, you can use the same Voltra JSX, the same React principles to render uh a new state of live activity on the server and then push it via push notification to uh to a device and vidgets can pull updates via HTTP periodically from your service running with Voltra. And how does it look in practice? So if you pay uh if you pay attention to this part you will see this is a basic react component. Okay, it's not basic because it's coming from Voltra name space but well it looks like text, it looks like view. It supports style property. It has

**[4:09](https://www.youtube.com/watch?v=Qat0JURTkXU&t=249s)** some custom spacing property but in core is basically the same thing you use in your day-to-day work when creating uh UIs for React Native. And we also have something like voltra text which is well a text component you also use in React Native. uh we have some custom components like vstack hstack but the important thing is that if you change something like I just changed this text in vultra text the change will be automatically propagated to the native word so you don't need to rebuild the app you don't need to like wait for the for the change to happen you don't need to do it imperatively it happens out of the box and it seems like something magical it seems like I had to write a lot of code something like super uh complex, a lot of native, a lot of JavaScript. But in reality, I was able to achieve that with a clever composition of existing

**[5:07](https://www.youtube.com/watch?v=Qat0JURTkXU&t=307s)** elements of React Native and React ecosystem. Because Voltra under the hood uses React because you write React code, you write JSX, which is basically React. And then on the native side, we have Swift UI, which is existing native framework for building uh UI in Swift for iOS. And Android side we have jetpack compose which is again built-in way for building UIs with native. So the only part missing is this way of translating react into something that can be interpreted on the native side to rebuild the same UI but with those uh native frameworks and today I want to go through a minimal implementation of Ultra. So technically after this talk you will know how to build it yourself. Of course we will know go we will not go through the whole code. We will go only through a limited set of examples. But thanks to them, you will know how it works internally and how to basically create your own vultra.

**[6:07](https://www.youtube.com/watch?v=Qat0JURTkXU&t=367s)** But before we go any before we go any uh further, we need to know the requirements because without requirements, we cannot write anything. Uh so first of all, we want to focus on swift UI. So we need this mapping from JSX from React components to Swift UI. uh we also need to support dynamic island because that's how Apple forces us to use new features of iOS. So if you have live activities, you need to provide some variants, some UI to be displayed in dynamic island, the space visible on top of your screen if you own an iPhone. Uh because if if you don't provide any any UI variant, you will end up with just a blank space. No rebuilds and no user on Swift because we don't want users to uh learn something new. We want to use React Native principles. So people feel like at home and we need to push be able to push updates because live activities are supposed to uh represent some action

**[7:09](https://www.youtube.com/watch?v=Qat0JURTkXU&t=429s)** that's going to change uh during some time like delivery status for example and our app may be killed may be suspended. So we cannot rely on the app pushing the updates to like live activities. So fundamentals if we built our apps react we built a tree. This tree is composed from components, custom components created by us by writing functions and some like leaves which are basically host components, host primitives, the most primitive views of given platform. And we can say that if we have this app component and we say to react please render me this app, React internally will provide some kind of representation in the form of some object hierarchy. So we can say that this view and text will be represented by something like this type props children and this will go on until all the uh all the elements are represented in the in the memory and then we need to convert this representation to something that's understandable by a given platform. So for example if we have react dome we

**[8:14](https://www.youtube.com/watch?v=Qat0JURTkXU&t=494s)** will get this description and we will produce HTML strings. Well, technically we will produce the HTML HTML nodes hierarchy, but you get the point. You are going from this abstract representation to something that's understandable by given platform. Of course, we can we need to do some u conversion migration let's say uh because styles on the left side this is an object on the right side this is a string. Uh so this transformation can include some uh some changes in the uh in the structure. And Voltra does the same thing. It takes this representation given by React and then on the native side it reconstructs the the UI described by uh by this big object uh by using native primitives. So we are doing the same thing that React DOM does under the hood. We are rebuilding the hierarchy of UI elements. Um but how can you build a custom React renderer? It turns out there's a like existing solution used by React DOM itself. It's called React reconciler. Uh

**[9:16](https://www.youtube.com/watch?v=Qat0JURTkXU&t=556s)** and you can install it from npm and then if you want to uh use it, you need to provide this host config which is about 40 meters long. It's super long. Uh the problem with this approach is that documentation is not really great. It exists. It tries to tell you what you need to what you need to know. But to be honest, I learned more by going to React DOM source code and reading the code and trying to understand why exactly given property given method is looking like uh like it looks like in the code instead of instead of reading the documentation itself. But in the end, if you manage to to create this host config thing and then you you run the reconciler method to build the renderer object, you can render React. Uh, and this is great because it supports hooks out of the out of the box. This support concurrent mode. This supports suspense. This

**[10:15](https://www.youtube.com/watch?v=Qat0JURTkXU&t=615s)** support pretty much everything React supports. We get all of that uh thanks to using this reconciler. So we are set up we our setup is basically giving us the JSON tree of uh of the primitive elements. We have this text for example uh which we then pass to native and on the native side we need to convert it back to something like text but uh but correct for a given platform. So in Swifty this is basically text. So for every type of primitive you want to support, we need to create this swifty wave wrapper with some props and then uh basically rebuild the same element but with native and we have to do this for every primitive we support for text for vstack for h stack for example for image and then we can expand to some graphs charts uh symbols and the list goes on and goes on and goes on. you end up with some nice looking switch case with 40 elements. But this is what it is. There

**[11:19](https://www.youtube.com/watch?v=Qat0JURTkXU&t=679s)** is no other way uh to implement that. And in the end, if you run this, you will see that if you display some live activity, you will see this hello world because well, it works. Nice. Cool. But we have no styling. We have no support for any like complex elements like images for example. Okay, image was there, but I didn't show you the code. But let's say there was no image. Um, and there are more problems with this approach. Uh, but I don't want to I don't want to spoil the fun. So, let's move to styles first. And styles are also interesting because it turns out that React Native styles and Swift UI modifiers which which are used to uh to style our uh our views, they are not the same. They are not even compatible by default. And why? Um it's because padding is the same. So padding is is simple because uh on the left side we have padding. It's the last property. On

**[12:20](https://www.youtube.com/watch?v=Qat0JURTkXU&t=740s)** the right side is the first property. That's important and you will see why in a second. So padding basically makes the bounding box bigger by 60 pixels in this case. Then we use background which is equivalent of background color. So we will say that okay this bounding box should be red. And now it turns out that there's no margin in Swift UI but in ra native you want to use margin because that's something you are used to. Uh so in order to simulate the same uh the same modifier on the swift UI side it turns out that you can apply the padding second time because it's not like on on web with box layout system that like padding always can be only once applied once and this will make the bounding box bigger by the padding. It turns out that this action can be uh can be applied multiple times to make the bounding box bigger. When we apply uh when we apply it first time, we'll make the bounding bounding box

**[13:24](https://www.youtube.com/watch?v=Qat0JURTkXU&t=804s)** bigger. Then we'll make it red. And then when we apply the padding second time, it will make the bounding box bigger. But because background was already applied as a modifier, it will stay at the previous uh previous uh previous boundaries. Therefore, we will create a fake margin. And things like this are all over the place in Voltra. uh because again we don't want to make users learn something new APIs we want them to use the same knowledge they have when working with react native apps so now let's talk about variance because this is the place when I thought vultra will never be published and will never work because it turns out that we have at least four variants for our UI to be uh to be provided because well Apple forces us to provide uh variants for Sorry [clears throat] for dynamic island. So we have the first one which is lock screen the default one. Then we have the

**[14:24](https://www.youtube.com/watch?v=Qat0JURTkXU&t=864s)** expanded state when the dynamic dynamic island expands. Then we have this uh com collapsed uh compact compact state on the left side. So you have a little window circle on the left side and a little circle on the right side and then you have minimal which is only this icon on the right side. So we have one two three uh one two three four five five varants to be to be defined and displayed. So if we assume our API look like this, we can just add an object like this. So we'll have a separate JSX3 for every variant. Sounds good. Looks good. But it turns out that there's a limitation I wasn't aware. Uh and the limitation is that you can only pass a certain amount of data between your app and live activity. And this limit is only 4 kilobytes. In reality, it's even less because it's 3.2 kilobytes. For for a comparison, MP3 file can be more than 1 megabyte. This is only 4 kilobytes. This is not much.

**[15:27](https://www.youtube.com/watch?v=Qat0JURTkXU&t=927s)** This is super small number. And this is the point when I thought that okay, that's over. We cannot really do anything like we need to pass information about every node. uh we need to pass information about styles. We need to pass the text given in text elements. Uh but then I realized wait text compression is well-known problem. We have solutions for that. So let's try and use them. So the first thing I did is I applied compression. I ch I chose broadly because well it's supported by NodeJS. It's supported by iOS. It's supported by Android. it gives better uh compression ratio than gzip. So, yep, it's it's shrunk the payload size by 70%. But then I realized that uh I need text back because I can only pass text. Uh so I need to I had to use B 64 which inflated the the payload size by 30% more or less. So let's say I was down to 70% of

**[16:27](https://www.youtube.com/watch?v=Qat0JURTkXU&t=987s)** the original size. But still it worked for some time. Then I added some new features. I tried to recreate some more complex UI. The same error was uh was back. So then I realized that okay compression should uh should short should [clears throat] shorten things like type like props like font size for example because those words are uh frequently used in the payload but still I decided to use some custom dictionary. So I replaced props with P, font size with C. For example, I just created a dictionary of short shortened names for all the properties. And I even uh even decided to switch from string literal for type to number uh indices in order to save space. This again allowed me to push more data into live activities, recreate even more complex UIs, but still after I don't know playing with it for an hour or so, I was back at the same error saying that

**[17:25](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1045s)** I cannot push any more data. So then I realized okay I have styles and sometimes we use the same style objects for many elements but I was always uh I was always uh stringifying the style object for every element. So I was wasting a lot of space. So then I realized I can compare by reference the style object provided by style prop and if it's the same I can use some kind of registry and then put only the reference in place of the style uh style property and this allowed me to again shrink the payload size by some percent but still there was my uh there was friend of mine who decided that he will build a GitHub contribution graph you know those rectangles in lines like four lines of rectangles. Each rectangle is supposed to show how many comets uh were done in given week I guess. Um but he was only able to create a single line of those

**[18:25](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1105s)** rectangles because 4 kilobytes limit. uh and that's the point when I realized that okay in this case I can apply the same the duplication but for elements because again if I create an element once and store the uh the reference in some global context and then embed the same element like five six seven times I can pass only one definition and then instead of passing the same definition over and over again I can just provide a reference which made it possible to display not a single row of of rectangles but to fill the whole available space because uh if you again uh pay attention you will see that there are only four types of elements uh dark one lighter one lighter one the lightest one so in the end I have four elements duplicated by I know 40 uh 40 times which uh because of this element duplication system is represented by four definitions and uh property references. But then it worked for client side rendering. So when my app is running, I can update the

**[19:30](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1170s)** live activity. The UI works. Now we need to push the same information, the same payload via push notifications. It means that we need to use the same code, the same renderer, but on the server side. On server side, we don't want any hooks to be executed because if somebody is um let's say um intelligent enough to use for example use effect and then reach out to some globals, it will work on react native side but it will crash on the server side. So we need to turn off all the hooks. We don't care about them. We don't care about any concurrency features because why would we? it it's basically get me the props put it through JSON rendering mechanism of uh of Voltra and then pass the payload to uh to the device. So this is one go operation. There's no need for waiting, no awaits, no uh querying like external sources. This should happen before uh you ask Voltra to render something. We just want

**[20:32](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1232s)** to have a static snapshot of the UI which we are going to send to the device. And the problem is that we cannot really turn off hooks when using React reconciler. It turns out there's no way. Uh I tried many things and in the end it turns out that it's not supposed to work like this. If you use react reconciler you're supposed to have all the hooks, everything in place and everything working as expected on the client side but we need to use it on server side. So then I realized okay React DOM has this serverside rendering solution. You can render via React DOM on server side and then hooks are not executed. So how it's possible? So I went to the code of React DOM and it turns out that there's a secondary super custom written from scratch renderer in React DOM which is only meant to be used on server side. There's client side renderer baked by React

**[21:31](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1291s)** reconciler and a second one totally custom written from scratch just for the server side. So I had to do the same basically. So trash the code I written up until this point and rewrite it from scratch. But it turns out that writing a custom render is a maybe not trivial thing but pretty pretty easy because again this is just a tree tree of components connected via uh ch children relationship between nodes. So if we start with a node we can check if it's primitive. It's a primitive work is done. This is basically text or a number which is for us is the same as text. We need to wrap it in in text element on the native side. So we can say it's a text. Work is done. If it's an array, we need to use the same algorithm but go through every child. So recursively we will call the same traverse function. This is done. Then if we stumble upon some kind of function. So we know that functions in

**[22:35](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1355s)** React means that there is a function component written by us. Now how components are rendered the functions the functions are called with props. So we do pretty much the same thing. We just call the function and then call traverse recursively on on the results. So we will get the children and this continues as long as there are some nodes to be visited. And of course the production code is more complex because there are stuff about sty registries about dictionary mapping about fragments. But this is like the core of the solution. But we are back at hooks now. We don't have them. But we want to have some of them. For example, we can say that use context is is useful because I can believe that there's some theme object that we want to inject into Voltra because people can change the theme of the app and we want to make sure that Voltra components also inherit the same theme. So we need hooks.

**[23:35](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1415s)** How to implement hooks? We go back to React and we go back to source code of React and we see that there's this funny sounding name in React which is secret internals do not use or you will be fired and it turns out that there's this age property and this age property is something called dispatcher for hooks. When you call use effect internally it will reach out to the secret internals. It will look up the age and then on age it will look up use effect and call it. That's why when you are not rendering react and you call your function a function component you will see this error saying that you are breaking rules of react or rules of hook something like that. So we override this age with our custom dispatcher. We call the function and then we we restore the the previous dispatcher. So react native still works correctly. And this dispatcher is basically a giant object with all the

**[24:36](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1476s)** hooks which in our in our case this is uh uh okay let's skip use context for now for use state we don't need state because this is one go operation a snapshot so we just return the initial value call it done for memorization we don't need memorization so we just call the factories that's done for effects no no operational function we don't need any effects that's pretty much done and this continues for every hook that can be present in React application. And as for as for use context, this is also trivial like it can sound complex. We need to support context providers and context consumers. So we need to somehow propagate the value of a context down the tree down the tree. That's the that's the point. So if we go through the tree and we are going through the tree with travelers function. If we stumble upon a context provider type because we can check that we just we have to just see

**[25:37](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1537s)** what the type of the context is provided and the value and then we put this value on some kind of map or some kind of stack. Um and we continue. Then when we stumble upon something like context consumer or use context hook, we look up the context map by the provider context type and if there's any value, we just give the value the last value because technically there can be many context providers of the same type and we only care about the last one. Um if we want to provide the default value of a context, it turns out there's current value property which you can use and this is the value provided by you when you create context. And when we go up, when we exit the node of context provider, we just remove the the type from the map. That's it. We just implemented context in React from scratch in 21 lines of code. And there are many many many many many

**[26:38](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1598s)** many things like this in Voltra. For example, remote images. Technically you cannot use any remote images in live activities but it turns out that you can preload them. So when your app is running you can download the image move it to a special place and then from this special place you can load it in a in a widget or in a live activity. So we have this API to preload images. Uh we are also trying to make sure that you can apply the same okay I'm again saying the same thing but we are trying to make vultra as close to react native mental model as possible. So for example for sweet UI we have this custom flex engine. So technically you can use the same vortra view with flex grow flex string because internally swift UI is using totally different layouting system. Uh but we are giving you this abstraction layer so we can use the same uh the same layouting uh principles. The same goes

**[27:39](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1659s)** with charts for example on iOS we have charts in swift UI that's good on Android there is nothing like this. So what we are doing is we are dynamically creating images with charts on Android on render time and then we are sharing sharing those images with widgets. So you can use the same charts on both platforms. Again, if you write native code, you will need to write a custom rendering solution for that in Voltra. It's already included. And for server driven refresh, it's the thing I I mentioned before. We just can pull from HT by HTTP query from your server when Voltra is running and rendering UI and then replace the native view hierarchy uh automatically. So if you want to use Voltra, there's this QR code leading leading to use voltra.dev dev where uh the whole documentation is hosted. Uh if you are interested in more things like this, some internal things on on Voltra on any other libraries I worked on, you can

**[28:41](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1721s)** follow me on X. And that's pretty much it. Thank you very much for being here. [applause] So I think we have a minute for some questions if there is any question. >> Thank you Shimon. Is there any question in the public here? >> No, I have one question in the end. >> It's about the 4 kilobytes you mentioned. >> Yeah. Yeah. >> Of limitation. So, I'm going to read it all. It's quite long. Well, there are multiple questions in one, but one thing wasn't clear to me. Who sends these 4 kilobytes? Is it a server that pushes that information into the iPhone? And I suppose the phone then has to decode those 4 kilobytes and convert them into native UI. Wouldn't it be possible to just push data in those 4 kilobytes, parse it in the phone and display it, similar to an server SPA that just fetches JSON instead of getting the render HTML single page application. So this question is from

**[29:41](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1781s)** Bernardo. Is he here? Yeah. >> Okay. So uh I will focus on this 4 kilobytes limit because after some weeks uh I know exactly why the limit is here because turns it turns out that the Apple push notification service limits the payload size to 4 kilobytes. they are guarding against pushing more data via local API uh because you cannot push push push notification with a with more data than 4 kilobytes. So they're they are trying to make sure that we are not uh that we are safe basically that's the that's the limitation that's why it's here. Um then about spas and and the other thing like there's a new render in react and sorry in vultra I wasn't talking about because it's it's experimental but the thing we are doing now is we are spawning a secondary JavaScript virtual machine in the vidget process and we are running like JavaScript code react code in this uh in this VM and then we can provide only the props from the server from the

**[30:45](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1845s)** server via present ification and this will be evaluated the code will be evaluated when uh rendering is supposed to happen native site which is something connected with this SPA approach because you no longer need to provide the whole UI you can provide only the props but it has its own disadvantages like you cannot provide any UI you want with voltra this default render you can basically switch the UI on the fly you don't need to provide definition for the UI before in the app. So you can always like I know if you have some limited time offer or limited time event, you can always show something new without making sure that all the users have the definition for this live activity in their uh in their apps. >> Is it clear? Do you have more questions? All right. Anyone has another question? >> Yeah. those those oh sorry so those four kilobytes you send it from server side to the iPhone and then it's being

**[31:46](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1906s)** executed and is it being turned into native code then or what happens exactly to >> the thing you you saw the JSON >> to native this is the JSON with type with props and there is a native equivalent with gets this JSON and uses it to first look up the Oh, it's text. So, I need vulture text component in Swift UI. And then this text component in Swift UI has props >> which are our props from JSON uh decompressed. So, in a normal form, font size etc. and then it just applies them in like native way to the text. >> Okay. So, the dduplication that you did for the elements and for the styles all of it is written then in swift. >> Yep. there is like a switch render on the on the native side and jetpack compose render on the Android side and we just provide this this description of what exactly you want to build on our side. >> Okay. So because I can imagine that that

**[32:50](https://www.youtube.com/watch?v=Qat0JURTkXU&t=1970s)** would even be useful for people that code natively in Swift to have that sort of dduplication, right? Because they would need to take care of that themselves as well, right? Well, when they build live activities, they usually just pass some data like date for example and then they can use this date to display whatever they like. But the thing is that then they need to have the code in the app present when this push notific push notification comes to the device. >> Ah okay. >> Like you have a blueprint which accepts some props. So then when you send the push notification with these props, it can turn them into UI. >> Okay. >> And Voltra goes the other way. So it embeds the whole UI in this small payload and then decompresses it and builds the UI on the native side. >> Okay. >> So >> nice. Okay. So they don't have to send the whole >> Yeah. >> interface. Okay. Okay. Nice. Thank you.

**[33:51](https://www.youtube.com/watch?v=Qat0JURTkXU&t=2031s)** >> I will be still around. So if you have any questions after the >> any other question? No, >> I will be around so we can always like talk. >> Maybe I have a a quick question. So you mentioned that you use like an internal renderer from React DOM. >> So like you >> Yeah, I was looking at it to to understand how it works under the hood because I needed to do so for the second uh volter to be created. >> Okay. >> It was all about the hooks. Yeah. >> Okay. So have you contacted them? So if they could do something like the React reconciler so anyone could reuse it >> like that it's not just stick internally on their code base. Yeah, >> you know the problem is that for service rendering for HTML purposes some constraints constraints are uh are to be enforced like you need to be uh super precise about like maybe the order of the

**[34:51](https://www.youtube.com/watch?v=Qat0JURTkXU&t=2091s)** elements or they're constraints basically and if you extract those constraints then you are left only with this simple logic of going through the tree and and so on. So I believe there's no reason to basically extract this knowledge and put it into npm package because it's technically it's trivial because the complexity starts when you want to build uh when you want to build something for a given platform like convert into this primitives of given platform. That's where the complexity starts to to pop out. But like you saw this going this uh graph thing it's pretty pretty easy to handle like by hand. >> Okay. >> So that's that's why I think they don't they don't expose it via package because it's not it's not that uh that complex >> but rendering hooks stopping the work yielding to to browser like the things that react consiler does under the hood this is complex. >> Yeah. [snorts] >> Okay. Thank you Shimon. So, a big round of applause for him.
