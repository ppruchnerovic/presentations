---
id: 870
title: "CUDA Python: GPU programming for the modern developer"
slug: cuda-python-gpu-programming-for-the-modern-developer
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Languages & Runtimes"
type: "Keynote/Talk"
stage: "Stage 9"
tags: ["APIs", "Best Practices", "Developer Experience (DevEx)", "NVIDIA", "Performance", "Python"]
speakers: ["Paul Graham"]
speaker_companies: ["NVIDIA"]
day: 2
starts_at: 2026-07-10T08:20:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=8bLOMUInqYM
video_id: 8bLOMUInqYM
session_page: https://app.wearedevelopers.com/events/16/session/870
transcript: true
---

# CUDA Python: GPU programming for the modern developer

**Paul Graham (Senior Solutions Architect — NVIDIA)**

`Track: Languages & Runtimes` · `Type: Keynote/Talk` · `Stage: Stage 9`

`#APIs` `#Best Practices` `#Developer Experience (DevEx)` `#NVIDIA` `#Performance` `#Python`

[Watch the recording](https://www.youtube.com/watch?v=8bLOMUInqYM) · [Session page](https://app.wearedevelopers.com/events/16/session/870)

## Abstract

Accessible introduction to GPU-accelerated programming using CUDA Python — no C++ required. Shows developers how to write parallel code for AI, data science, and simulation workloads using Python-native APIs.

## Speakers

### Paul Graham

*Senior Solutions Architect — NVIDIA*

At NVIDIA, Paul has responsibility for supporting customers and partners in delivering accelerated solutions for the Higher Education, High Performance Computing and AI communities in the UK. As well as providing advice on making the best use of NVIDIA hardware, Paul teaches on how to program for GPUs, mentors at hackathons and regularly engages with research software engineering groups. Paul is an advocate for using accelerated computing in HPC, and the use of AI as a powerful tool for researchers.

## Transcript

*5,963 words · source: yt (en)*

**[0:03](https://www.youtube.com/watch?v=8bLOMUInqYM&t=3s)** Okay, hi everyone. So, my name is Paul Graham. I'm a solutions architect at Nvidia. What does that mean? We have lots of different solution architects. We all have different roles. My role is to work with universities. I work mainly in the UK speaking to researchers, seeing what it is they're doing and if Nvidia has anything that can help them do their research bigger, better, faster. I also teach people how to program, so how to program for our GPUs. And I take part in workshops and hackathons and that sort of thing as well. My background is high-performance computing, so um you know, I know AI is a huge thing nowadays and I do know a fair bit about AI, but I'm more old school and give me a big supercomputer any day. Uh I'm going to speak today a little bit about

**[0:50](https://www.youtube.com/watch?v=8bLOMUInqYM&t=50s)** what we're doing in the Python ecosystem to make sure it's as straightforward and as possible for you as developers to make use of GPUs, but also to make sure you're still going to get the great performance that you would expect to see on GPUs. Um So, without further ado, let's crack on. Um we're actually celebrating 20 years of CUDA this year. CUDA was developed round about well, exactly in 2006. That was the first release of CUDA. And CUDA was a a way of programming for GPUs that made it much easier for you to do things outside of doing computer graphics. Obviously, GPUs were originally designed for putting pixels on a screen, but it's a powerful piece of hardware and humans being humans, we're interested to see what else you could do with that other than putting pixels on a screen, so people started developing algorithms for their

**[1:37](https://www.youtube.com/watch?v=8bLOMUInqYM&t=97s)** scientific software and so on. Um but they still had to use the language of graphics and that made it quite hard to get into. So, we developed a thing called CUDA, which is an API for programming for the GPUs. Released in 2006 and then not long after that you start to see this idea of general-purpose GPU computing really take off. And here we are now 20 years later. This is just a small sample of some of the different libraries and frameworks available, which are all built on top of the CUDA platform, uh which allows and I I I'm coming from the world of science, so this is quite science focused, but of course there's many many other uh libraries out there in different industries, um as well. So, it's a huge ecosystem. Now, I wanted to zoom in a little bit on the hardware side of things, and the reason I do this is purely because, um as developers, to have a little bit of

**[2:24](https://www.youtube.com/watch?v=8bLOMUInqYM&t=144s)** understanding of the hardware we're programming to helps us make sure we make the right choices. Um this is a schematic diagram of the Hopper uh GPU. This is one of our big data center GPUs. It's a few years old now. Um but I just want to highlight a few things here. Anything on green is in there is aimed at doing maths. It's aimed at doing compute. Okay? It's not about the you know, moving data around. It's just there for doing the sums. Um and if we zoom in a little bit on this, um this is a small part of that overall GPU. Uh this is called a streaming multiprocessor. Um and you can see again, there's lots of fine details there. There's various different what we call CUDA cores for doing compute at different precisions. Uh something else you need to think about when you're using GPUs. Um and you know, different GPUs are good at different precisions.

**[3:12](https://www.youtube.com/watch?v=8bLOMUInqYM&t=192s)** Um we also have a big chunk of green there, which is the tensor core, and I'll talk a little bit more about that later. Uh but that's aimed at doing matrix multiply and accumulate operations in hardware. So, as you can see, this small part of the GPU is still very complex in and of itself. And the way we program for GPUs, um we have this idea of having many many more threads than there are actual cores to execute on at any given time. It allows us to hide the latency of waiting for data to arrive uh from the main memory. And so, on the Hopper GPU, we have 132 of these streaming multiprocessors. That gives us over 16,000 single precision cores, okay? So, it's quite a powerful piece of kit, but it also makes it a challenge, an interesting challenge to program for and to program for efficiently. And And so, a lot of what we do in our work with CUDA is to try

**[4:00](https://www.youtube.com/watch?v=8bLOMUInqYM&t=240s)** and make it as straightforward as possible for developers to not only get their code working on GPUs, but to get it working well. So, we'll look take a look at some of the tools we have for that. So, CUDA has gone way beyond what it was in 2006, which was literally this API for programming for GPUs. It's now this whole ecosystem, this whole platform that we've built on, going from the compiler stack. You can actually write your own compilers for languages which don't even support CUDA yet, um through to the the um applications and frameworks where you don't need to know anything about GPUs, you just know that your your work is going faster. And this slide I like to show because it shows the kind of philosophy we have in terms of engaging with developers. Um we want to meet you where you're most comfortable at. If you want to write your own

**[4:46](https://www.youtube.com/watch?v=8bLOMUInqYM&t=286s)** compiler, we will support you in doing that. Uh if you're not interested in what the GPU does at all, we give you the frameworks. And we have the layers in between as well. So, we have the accelerated libraries which have APIs very familiar to you if you use things like MPKL and so on. And we have the of course support in various different languages. Now, traditionally CUDA was a C C++ language. Then we had Fortran support for the high performance computing community. Uh and now we have put a lot of effort into making sure that the Python community can also gain from the advantages of being able to use GPUs straightforwardly, and that's the the focus of today's talk. So, let's take a look at um the Python stack and how uh that matches to what we've just seen there. So, this is just a sample of the different Python enabled um tools uh we have

**[5:36](https://www.youtube.com/watch?v=8bLOMUInqYM&t=336s)** available to us. And this kind of matches the the stack I showed you earlier. So, we got the frameworks at the top, domain specific languages, the SDKs, going down through the to the uh device library, so that's programming directly um uh to the GPUs yourselves. Kernel authoring, so write kernels are what we call the functions that execute on GPUs, so you can write your own kernels in Python without having to go out to C++ or anything like that. And of course, down to the runtime tools and the compiler stack. Now, we've put a lot of effort in over the last few years, in particular, to make sure all these things are interoperable. It used to be the case that some of these wouldn't work with others in terms of the way the uh data was stored, the way the data was represented, and you had to do some conversions between these different uh parts of the ecosystem. Now, that's not true. All these are interoperable, and it means we can

**[6:24](https://www.youtube.com/watch?v=8bLOMUInqYM&t=384s)** quickly switch between them, but also means that for you as developers, it makes your life a lot easier as well. And we would say that CuPy has now hit version 1.0. Of course, the idea of programming in Python on GPUs has been around for a long time, uh but in terms of making it uh what we call a first-class citizen, um i.e. something which is comparable and as performant as uh the C++ original sort of CUDA way of programming, um that that's only now happening. Um So, at the bottom, we have things like CUDA core, which allows us to write the kind of basic functionality of of CUDA, which as we would do in C++, so we can create streams and graphs and so on. Uh we've got the CUDA bindings, which are very, very low-level bindings directly from the C to the Python, which allows us to make sure that everything's

**[7:12](https://www.youtube.com/watch?v=8bLOMUInqYM&t=432s)** as performant as it should as it should be. Uh we've got the CuPy CUDA pathfinder, uh which allows you to find the CUDA libraries in your system. Obviously, one of the challenges people have is that when they install their their Python ecosystems environments, uh it's sometimes it's hard to pick up the CUDA libraries. Uh the pathfinder tool will help you with that as well. At the middle level, of course, we've worked a lot on the libraries, and I'll talk a bit more about that in a moment. Uh the NV math uh Python libraries in particular. Um we've got some of the core compute libraries as well, which I'll speak a bit more about. And we've got the communication libraries. Obviously, a lot of the problems people are working on nowadays are require GPUs to execute at scale. So, I'm not I'm talking not just a few GPUs within a server, but multiple servers all working together on the same problem.

**[7:59](https://www.youtube.com/watch?v=8bLOMUInqYM&t=479s)** And of course, that requires parallel libraries for executing that. Uh, in the world of high performance high performance computing, there's a thing called MPI, the message passing interface. And of course, there's there's a Python interface now to that as well. So, that's the um uh, well, it's not actually mentioned there, but it does exist. For us, um, we can make use of some of the topology in our system. So, we've got a thing called NVLink, which is a a very fast interconnect directly between GPUs. Um, and to make use of that, we have a thing called the NVIDIA collective communications library or NCCL, uh, and we have a Python version of that now. So, the NCCL for Pi there. We also have a thing called NVSHMEM. Now, the NVSHMEM's quite interesting cuz it's one of the few um, ways of communicating across uh, large systems where you can initiate that communication directly from the GPU. Most of the other models require the CPU

**[8:48](https://www.youtube.com/watch?v=8bLOMUInqYM&t=528s)** to launch the the communication even if it's, you know, using directly the GPU memory. Um, but with NVSHMEM, you can actually launch the communication from within the GPU itself. So, that allows for further optimizations. Um, so [snorts] again, we have a Python support for that as well. Um, and in terms of kernel authoring, writing your own code, of course, we we support that as well. Um, number CUDA, I'll mention in a moment, is one of the most uh, straightforward ways to do that. Uh, we've also got a new thing called QTile, uh, which I'll speak a bit about as well. Um, I won't speak to the cute DSL, but again, uh, it's the Python version of our CUTLASS libraries. Now, CUTLASS libraries are for programming directly to the Tensor Cores, which I showed you earlier. So, a lot of the work, you know, the message I want to get across today is

**[9:35](https://www.youtube.com/watch?v=8bLOMUInqYM&t=575s)** if you've used GPUs in the past in the C++ world, but you've not gone to Python cuz you think, "Oh, it's not going to be as performant." Uh that's just not the case anymore. Um and also in terms of you might think, "Well, I want to do a certain thing on the GPU. I'm not going to be able to do that in Python." That's also not the case anymore. Python is just as performant, just as capable, just as feature-rich as the C++ world. So, you're coming to program for the first time on GPUs from the Python side of things. Uh you're probably familiar with NumPy. It's a scientific uh uh framework within Python uh for for writing um you know, lots of array operations and things like that. CuPy is the GPU-accelerated version of that. Um and you can see from this example here, well, perhaps you can't cuz it's very small writing. I apologize for that. Um but on the left here is just a a standard NumPy uh bit of

**[10:23](https://www.youtube.com/watch?v=8bLOMUInqYM&t=623s)** Python. Import NumPy as np. Um on the right-hand side, all we've done is import CuPy as cp. And that's pretty much the only difference between those two. Um but the main difference is if you look at the timings at the bottom, this one goes about 10 times faster on a relatively old GPU. So, in terms of code changes as a developer, you don't have to make any. The CuPy model handles all the things like the data transfer between the CPU and the GPU for you. You can think of those things as uh perhaps um something you want to look in as optimizations in the future. Maybe, you know, you can move the data around in a more efficient manner. Um but as a first starting point, this is a really nice way to get going. And it doesn't require you to have any really deep knowledge about the GPU. Going on from that, if you want to start

**[11:11](https://www.youtube.com/watch?v=8bLOMUInqYM&t=671s)** writing your own uh kernels, your own functions directly to the GPU, uh Numba is a great way of doing that. So, Numba has sort of two two modes for GPU programming. One, you can just decorate a function if it's an element-wise function. Um it will just uh put that onto the GPU for you. You don't have to do anything more than that. Or you can actually write your own kernels with if if ever programmed in C plus plus on the GPUs and written CUDA kernels, it will look very very much like this. So, it allows you to do the sort of thread level programming that we're we're familiar with. And so again, that's another way to get going. So, one of the new things we've got is this thing called NV math Python. Now, this is a set of libraries callable from Python. And again, they're framework agnostic. They work with Q pi, they work with Pi

**[12:01](https://www.youtube.com/watch?v=8bLOMUInqYM&t=721s)** torch, they work with NumPy. And they also work with CPU as well. And the idea is it gives you this one sort of interface, one sort of library call. And that you can, you know, set this up depending on your problem size. It might be more efficient to run something on the CPU than the GPU, so you can make that choice, but still have the same code call to the libraries. If it's an arm CPU, it will use our video performance libraries for for CPUs. If it's a X86, it will use the MKL libraries. And of course, if you want to run it on the GPU, it will use our highly performant libraries for that as well. So, it's a really nice way to get going with libraries. And again, it's built all on top of the the kind of CUDA bindings and the CUDA core, which we have at the as the base of all these different approaches. One of the nice things that using the

**[12:50](https://www.youtube.com/watch?v=8bLOMUInqYM&t=770s)** libraries allows us to do is to do a thing called kernel fusion. So, kernels are the functions we execute on the GPU. And there's sometimes an advantage to take the if you have lots of different kernels, to take the work that's happening in each of those and combine them together. There can be efficiencies made. You're perhaps reducing the amount of data transfer both between the CPU GPU, but also internally on the GPU itself. And so using the libraries, here we've got an example using the NV math library for linear algebra. We can do this kernel fusion, and we can see a significant speed up here, which is why I wanted to highlight it. So, on the top left there, we've got a bit of code which is just doing this basically matrix multiplication then you scale that matrix then you add another matrix to it. Um writing the code like that the top left

**[13:37](https://www.youtube.com/watch?v=8bLOMUInqYM&t=817s)** is perfectly legitimate and as a programmer you'd probably write it like that. It makes sense it you know it it reads well. Um so we do our but the way that would execute on the GPU, it would do the map mole first then it would scale the result of that times it by alpha. Um then it would um do the multiplication of the um C matrix by beta then it would do the addition between the two. So there's four different operations. Okay, which would actually mean four separate calls to the GPU. If you just use the library call at the bottom, it fuses all that into one operation. And we can see from this the performance on the right here, we see a significant speed up. It's over three times faster on this this 5080 here. And about two and a half times faster on the Blackwell card. So again

**[14:24](https://www.youtube.com/watch?v=8bLOMUInqYM&t=864s)** libraries are great and we should try and make use of them. And but both of these examples are using the libraries. We're using the map mole library on the first one and the second one however we're using the more capable library which has more richer functions which allow us to to to support this idea of kernel fusion. So again the whole philosophy of of what we're trying to do is we want people to be able to do things easily but to do things as fast as possible. Speed of light as we call it at Nvidia. Um I mentioned the CUDA call. So this is the Python way of doing core basics CUDA functionality. So it's things like you know setting up the device interrogating the device to see what capabilities it has creating streams which are different flows of

**[15:10](https://www.youtube.com/watch?v=8bLOMUInqYM&t=910s)** instructions to put on the device. One of the nice things this allows you to do is to use a thing called CUDA graphs. As the GPUs have become faster and faster a lot of the functions that they run end up being quite short. So, the overhead of actually setting up the the that function to run, the launching of that kernel on the GPU, actually starts to become a significant factor in some cases. With CUDA graphs, what you can do is you can combine uh several different calls um so diff- seven different kernels um and set up the order that they're going to execute in, and then just have one call to launch all those different kernels. And again, similar to the the kernel fusion idea, this can have significant uh speedups because especially if you've got lots of small, fast-running kernels cuz it it it mitigates the overhead of launching lots of small kernels. Uh and

**[16:00](https://www.youtube.com/watch?v=8bLOMUInqYM&t=960s)** we can now do that in Python. We couldn't do that before in Python. We would have to drop out of C++ to do that, but now we can do it in the world of Python. Um we have CUDA.compute. So, this is built on the um the NVIDIA CUDA core compute libraries, the CCCL, which have been around in C++ for quite a while now. Um the idea behind those libraries, so it's things like Thrust, which you may be familiar with from C++. It's a set of sort of higher-level libraries which allow you to do some of the basic functions that you'd expect to do on on data. So, you know, transformations, reductions, that sort of thing. Um now, the nice things about these, they allow you to effectively do some of the um not necessary kernel fusion, but some of the optimizations you might see in kernel fusions yourself but at a higher level. Um so,

**[16:48](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1008s)** uh on the right here, we've got a couple examples. The first one is just showing you how to do a reduce operation. And again, we don't have to write an explicit kernel for this. This high-level call will do that reduction. The second one is is a bit more interesting um cuz it allows us through the use of uh clever use of iterators um to avoid uh doing additional uh reads and writes to memory. Now, why is that important? Although the GPU is very, very fast, any unnecessary reads and writes to memory can slow things down. So, we need to be careful about those. So, you can imagine you have a situation you're adding two vectors together, then the result of that you want to do something else with like reduction. Now, again, logically, that's two steps in your program. You'd write that as two different lines of code. That's fine. But, if you just did that in that naive way on the GPU,

**[17:36](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1056s)** you're creating a sort of temporary array, which would end up going back to main memory to be stored before you then did the reduction. What you can do with this is you create iterators which basically combine that addition and the reduction to one single operation. And that means you don't have to store that temporary array. It's just the individual values are just stored locally on the CUDA cores in the registers. And again, that can have a significant impact in terms of your performance. It's all about trying to go as fast as possible. This is what what GPUs are there for. And this gives you some more tools in your toolbox to be able to do that. We have then have the cooperative libraries. And again, this is building on something from the world of C++. So, the cooperative libraries are where we want teams of threads on the GPU to work together to achieve some results. So, again, a reduction is

**[18:24](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1104s)** a good example. But, this does it at a slightly lower level. It does it at either the warp level, which is groups of 32 threads, or the block level, which is something which executes on one of those streaming multiprocessors we saw earlier. Now, we have lots of material on programming for GPUs. And so, I won't go into too much more detail than that. But, it it basically allows you to take advantage of this hierarchy of parallelism available on the GPU and to do it very very efficiently. So, again, see on the left there some of the different operations which are supported at that the different scales available to us on the GPU. So, something which is really pretty new we've come out with is this idea of tile programming. So, this comes from um

**[19:11](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1151s)** the rise of of tensors as being a pretty much fundamental structure of data used nowadays in computer uh especially AI. Everything is is tensors. So, tensors are just, you know, multi-dimensional matrices. Uh and this that's so commonly used now as a data structure, we actually have built into our hardware um special uh bits of the hardware to deal with matrices. So, we've I we've already mentioned the tensor cores, they do matrix multiply operations. We've also got a thing called the tensor memory accelerator, which can quickly load like whole chunks of data rather than just individual values or lines of data, it actually loads blocks of data. Now, all these things are quite hard to program for and if we're thinking or can be um and if we're thinking, you know, what's the sort of steps we go, well, first of all, you know, the example

**[19:58](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1198s)** here, we've got a picture of a daisy, we'd first of all break that into blocks uh and then we'll break that further down into what each individual thread is going to each individual element of those blocks, right? That's the kind of way we think about these things. Um so, in terms of the user responsibility in that that single instruction multiple thread model, which uh which that represents, um this is how we how we go. We go through the the main picture, break it down into blocks, break it down into individual threads, but we have to think about these other things as well. Uh the memory movement strategy, when do we want to move things around? Where do we want to move them to? The compute strategy, what is the best part of the GPU to use to make sure this works as quickly as possible. Um the tile memory layout, that can be important depending on what way we're issuing the

**[20:45](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1245s)** instructions, the way that's laid out in memory, that may be less efficient. So, we need to think about that as well. And also the storage, where do we want to keep it? Do we want to keep it in shared memory? Do we want it in the main memory? Is it too large? Do we have to keep it somewhere else? So, we have to think about all these things and that's fine. We're clever people, we can do this. Um but it's such a common pattern um that we've actually introduced this idea of um CUDA tile. And that changes the responsibility. You the user just have to break things down into the blocks, divide your work into what we call these tiles, and then all the other things which we talk about the system will take care of. Now, that has a couple of advantages. First of all, it makes your life a lot easier, but it also allows the compiler to do more aggressive optimizations um because it has a better understanding of the structure and what needs to be done because you've given it

**[21:33](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1293s)** more information about that. We've said this is a tile. This is not just an individual element. So, it can make different choices about how it compiles that up. Uh and that can have significant performance improvements. Um So, this is a nice new way of doing I've just got a example here. So, this is what it looks like. So, this is just a a softmax function. Import CUDA tile, then we do we load the tile in, we do a max, we do an exponential, we do a sum, uh then we store it. And for those of you working in the world of NumPy and things, this should look very familiar to you. You know, this quite often the way we deal with arrays in NumPy is very very similar to this. So, it's a natural way of programming in Python, I think. And also the the CUDA tile is available in is C++ as well. Um

**[22:20](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1340s)** What does that So, in terms of performance, it will perform as, you know, very well, but give an example in terms of programming. On the left there is a sort of naive implementation of a particular set of operations we want to perform on this this this block of data, this construct that we have. And on the right, um what it would look like if we wrote it in CUDA tile. So, obviously a bit simpler, a bit cleaner. Um Now, the next slide I think shows the real story of why this is good. So, this is what speed of light C++ implementation would look like. This is trying to get the maximum absolute performance out of the GPU. You can see it's very very involved. Lots of small steps happening in there. Same code on the right, this will perform just as well as that. Okay? So, again, easier for you, but also making sure you

**[23:08](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1388s)** still get the same super fast performance that you come to expect. Okay. We still make mistakes, even the AI's make mistakes, so we need to debug our codes. Again, we've added additional support into our debuggers like CUDA DBG, um to allow you to do that within the world of Python. So, you can have break points, um you know, uh step through the code, variable inspection, all that sort of thing. That's entirely supported in Python. Um so, you can either just use the CUDA GDB directly, or the it's supported in the inside Visual Studio Code Edition. Also, in terms of performance analysis, so this is really key um when we're using GPUs. It's quite easy to get stuff running on GPUs nowadays, but it's quite easy to get

**[23:56](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1436s)** stuff running on GPUs inefficiently. So, how do we know if it's running inefficiently? Well, we have to do some performance analysis. It's also easy to spend a lot of time trying to optimize something uh when that's not the thing that's being optimized. You know, you may be worried about your data movement, whereas it's not the thing which is which is slowing you down. So, to understand all that, you need to do your performance analysis, and of course, we have lots of different tools for analyzing CUDA code. Uh and again, we're bringing the uh they already support Python code, and but what we're doing now is bringing in annotations within your Python code, so directly from your Python, you can actually do some analysis as well. So, we've got the example there. So, this is I think it's just out in beta now. I've not even tried it myself yet. Um so, again, uh a nice thing to try out. Okay, so what are the next steps? Um so,

**[24:46](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1486s)** I've got a couple of QR codes here for you. Uh in terms of lots of examples which go through some of the different techniques I've shown to you today, uh the accelerated cute computing hub on the left here is the place to go to. Um it's got a few tutorials on there. It's got worked examples. And all the different uh technologies I talked about today, CUDA core, CUDA libraries, Q tile, and so on are all available on there. Um On the right uh here, um we're going to be back in Berlin in October uh for our GTC event. Um so the GPU Technology Conference usually runs twice a year. Typically once in uh San Jose in April, uh then somewhere else later in the year. This year it's going to be Berlin. So uh be lovely to see you all here for that. And again, uh a QR code to allow you to register. Uh there is an early

**[25:35](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1535s)** bird discount if you register before the I think it's the 15th of July. So if you are interested in coming to GTC Berlin, come there. Um if you can't attend, um please just uh you can sign up for virtual registration as well, which allows you access to uh um a lot of the talks after the event's finished as well. So uh please do that. Uh it just remains for me to say thank you. Hope that was of interest. Uh we have a stand uh over just next door. Uh if you want to come and chat to me more about CUDA Python or all things GPU, please come and find me there. Thank you very much. >> [applause] >> Paul, thank you so much for a really good talk. Um we have one question. And it's quite a long one, so I brought the tablet with me.

**[26:21](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1581s)** So how efficiently can one GPU run and share resources across multiple parallel machine learning training tasks? And how can the performance degradation be measured? I can repeat if you want. >> Okay, so you're basically asking, I want to run several things on the same GPU. And it might mess with the performance of that GPU because you're running so many different things. Is that what they're asking? >> I think so. >> Yeah. Okay. So I mean we have a few technologies for actually dividing the GPU up. Um so on the data center cards we've got a thing called make short for multi-instance GPU and that means you can split the GPU into up to seven different instances and they they have hardware isolation from each other so you can sort of guarantee level performance for each of those each of those instances. So that's one

**[27:09](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1629s)** way of guaranteeing some level of performance. We've got various other tools for balancing out the load on the GPU things like MPS multi-process service but if you just set off a load of tasks on the GPU there's a GPU scheduler which decides how to work those tasks. It works out what's the what's going to be the most performant way to finish all of those tasks in the shortest amount of time and it will not prioritize one task over the other. Okay it will just work out what's the most efficient way to finish all of the tasks eventually and it so it works it's about throughput on the GPU. So there are performance tools for measuring what's going on the GPU so you can measure any degradation you may think of but yeah the GPU scheduler is usually pretty good at deciding. It knows what it is it knows how quickly you can get through the work you've given it and it will make choices based on what you've given it.

**[27:57](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1677s)** >> Thank you. This question was from our amazing audience. >> Thank you my amazing audience. >> Yeah and the next question is from me. NumPy has been with us for decades I could say right now and now this new library and it's it sounds really interesting but to be fair when when we propose it to our teammates they're a little bit of worried with some sort of vendor lock. >> Some sort of >> vendor lock so that I see that it's compatible right now with Nvidia and AMD that's a good sign but we can expect more and more providers to take place and who is maintaining it and how can we be sure in the future that it will not be limited to Nvidia and AMD only? >> That's It's good question. I'm not sure

**[28:46](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1726s)** I have the answer to that. Um you know, CuPy itself has been around for a long time as well. CuPy is not necessarily new. It's compatibility with other things is new. Okay? Um so, CuPy has existed for a long time and it has been safe, you know, no one's bought it up or anything like that. So, it may well continue like that. We tend to be community led in these efforts. It's why we put all this effort in Python because there's so many people in the community using Python and so we better make sure that our stuff works well with Python. That's That's the philosophy we have on these things. So, yeah. I'm not sure that answers your question, but it's it's the best I have for you. >> Absolutely. It's promising and I will definitely be in the NVIDIA event in late October and I look forward asking you more questions if I catch you on the

**[29:33](https://www.youtube.com/watch?v=8bLOMUInqYM&t=1773s)** stage. Thank you very much again for today and thank you everyone for your questions and enjoy. >> Thank you. >> [applause]
