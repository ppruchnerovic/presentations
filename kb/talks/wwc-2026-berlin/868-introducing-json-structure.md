---
id: 868
title: "Introducing JSON Structure"
slug: introducing-json-structure
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Backend & APIs"
type: "Keynote/Talk"
stage: "Stage 7"
tags: ["Data", "JSON"]
speakers: ["Clemens Vasters"]
speaker_companies: ["Microsoft"]
day: 2
starts_at: 2026-07-10T08:20:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=heaA5E4vkQw
video_id: heaA5E4vkQw
session_page: https://app.wearedevelopers.com/events/16/session/868
transcript: true
---

# Introducing JSON Structure

**Clemens Vasters (Principal Architect — Microsoft)**

`Track: Backend & APIs` · `Type: Keynote/Talk` · `Stage: Stage 7`

`#Data` `#JSON`

[Watch the recording](https://www.youtube.com/watch?v=heaA5E4vkQw) · [Session page](https://app.wearedevelopers.com/events/16/session/868)

## Abstract

JSON Structure, an IETF Internet Draft, is a strictly typed data definition language for code generation and database mapping. Unlike JSON Schema, it enforces strict types and determinism. It adds extended primitives (e.g., int32) and compound types to ensure precise data modeling for modern polyglot applications. It's a better JSON schema than JSON Schema while retaining the familiar structure.

In this session by the primary author of the specs, you will learn about the JSON Structure core and extension specs and the SDK tools that come with it.

## Speakers

### Clemens Vasters

*Principal Architect — Microsoft*

Clemens Vasters is Lead Architect in Microsoft’s Azure Messaging team that builds and operates a fleet of hyper-scale messaging services, including Event Grid, Service Bus, Event Hubs, Stream Analytics and Microsoft Fabric Eventstreams. Clemens represents Microsoft in messaging standardisation in OASIS (AMQP, MQTT) and CNCF (CloudEvents, xRegistry) and writes too much code for being an "Architect". He looks back at nearly 30 years in professional software development and has seen the same fashion come and go a few times.

## Transcript

*4,479 words · source: kome (en)*

**[0:00](https://www.youtube.com/watch?v=heaA5E4vkQw&t=0s)** Hello everybody. I have the pleasure to uh present a specification to you. Um if you scan this QR code, it leads you to the landing page of JSON Schema.org. Um JSON Schema is a schema language. Um and I will talk about schema languages in the beginning a little bit and why that matters and what you use them for and why you should be interested in them. And I will also talk a bit about JSON Schema, which you might be familiar with. Um and why that is uh problematic and some of the story of us. Um I got already got introduced. Let me just say a few things about why I do this. Um I'm the standards architect. I'm the product architect at Microsoft for all the messaging services, Event Hubs, Service Bus, Event

**[0:57](https://www.youtube.com/watch?v=heaA5E4vkQw&t=57s)** Grid, Stream Analytics, and for the streaming real-time capabilities inside of Microsoft Fabric under the name of Event Streams. So that's my product portfolio and my standards portfolio is OASIS MQTT, where I'm a member of the working group. Uh and I'm I've been doing contribute contributions significantly to MQTT 5. Um I am the co-chair of the OASIS AMQP technical committee and I've been writing a number of companion specs for this. I'm one of the architects of CNCF CloudEvents. I am also one of the architects of the relatively new CNCF X Registry um project, which is a universal metadata registry with a focus on a messaging and eventing. And so JSON Schema is part of the story because what we need for our products is a way to formally define what is the data that flows in data pipelines. What we try to do is we try to create effectively schema like for a database, but for a

**[2:04](https://www.youtube.com/watch?v=heaA5E4vkQw&t=124s)** hole in the wall. Right? Data schema for data that you don't have, but that that you don't have as you have in a database, but data for a schema for data that you don't have yet, that's still coming at you. And what makes it really expensive to build real-time data pipelines, think of Kafka, uh things of event hubs, think of these sorts of of real-time pipelines, what makes them really hard is that currently it requires a team from end to end to understand this and to have effectively exchange information about data structures with tribal knowledge or by sharing code. But as soon as you scale this up, then this currently breaks down because we don't really have a good metadata story for all this. So, that's what we're trying to do with cloud events, with X registry, and now this

**[3:04](https://www.youtube.com/watch?v=heaA5E4vkQw&t=184s)** piece is JSON structure. It's a formal What we need generally in a schema is we need to have a formal description of data types. Names descriptions shapes constraints semantics relations and units of data. What schemas today, when you look through the landscape, JSON schema, Avro, uh Protobuf, you know, all of these schema languages which kind of define data structures in a programming language neutral way, is that they're mostly bolted to a very particular serialization format. If you define If you define data structures in proto, they are bolted into the proto ecosystem and you can only use the proto compiler and the translators kind of for this, and you kind of constrained by this, and you can't really break out and use that same with XML. So, that's a problem, right? You have

**[4:01](https://www.youtube.com/watch?v=heaA5E4vkQw&t=241s)** data definitions, and they're kind of locked into a particular technology, and that's not a good thing. Applications are polyglot. Your data, the data structures that you move around, land in a database, they land in a queue, and they need to be exchanged from an app that has been written in JavaScript to the application that's being written in Rust. You have an API that's being called by a language X, and you have an a server that's another language. And the core, funny that we had a just had a talk about, you know, how do you design your APIs, the most complicated aspects of all of this are the data structures. Right? And the semantics of them, and what is contained in what. And we have at most miserable ways of of exchanging them in a polyglot

**[4:59](https://www.youtube.com/watch?v=heaA5E4vkQw&t=299s)** way. So, that was the problem that we were faced with when we were building, when we are building, as we are today, Fabric Event Streams for the Microsoft Data Platform. So, the default way of cur- how people currently define data structures in web applications is JSON Schema. JSON Schema is popular because it's pretty simple in the simplest case, right? You say, type object, properties, and you list the properties with their type, and that's very intuitive, and so, that's what people like. However, JSON Schema has been built as a document validation language, and it's not a data definition language. It's also very constrained in terms of what the type model is. The type model is exactly that of JSON, but doesn't go beyond that. So, it's inadequate, and that's a whole talk by itself. I can

**[5:57](https://www.youtube.com/watch?v=heaA5E4vkQw&t=357s)** go and rant about JSON schema for 2 hours. Um but it's simple and that what makes it popular. And if you use Swagger, if you use Open API, um you already use a subset of half typo, you use a subset of JSON schema. So, JSON structure is an IETF draft or a set of IETF draft. I'm the author of them. And um those are taking JSON schema, the look of it, and turn this into turn this shape into a data definition language from a pure validation language to make that useful. There's a few goals that I have or that we have with this. First, we want to be able to define data structures. We want to give everything a name. We want to have support for name spaces. Something that JSON schema kind of you can hack it into, but it's not a it's not a first part first uh um concept. We want to have a pro a proper type

**[7:06](https://www.youtube.com/watch?v=heaA5E4vkQw&t=426s)** system. We want to have dates, time, decimals, uh in in 32, like all the types that you have in proper languages. Um because the goal of schema is to drive code generation. Right? It's to also make AI agents understand what the data structures are that you're dealing with so they can go and generate that code for you with precision. And then I want to be able to have compound types that go beyond object and beyond array. Because there's much more interesting there's many more interesting things. If you want to go and define a map in JSON schema, you define an object which with additional properties. And then you define kind of that's how you define but it's not a map, right? It's a weird way of defining this because it's not a type definition

**[8:03](https://www.youtube.com/watch?v=heaA5E4vkQw&t=483s)** language. So, here's how this looks. This looks if you know JSON schema, you're like, this is so cool. There's a few things that are noteworthy here. So, first, um the name is required. So, you must define a name for every type. Not required in JSON schema. Date is a proper data type. So, it's no longer a string with a format, but it's a validatable RFC 3339 date. The type for each schema is required. That is not the case in JSON schema. So, it is the same shape, but it's tightening the rules. In for the most part. So, if you know how to write JSON schema, you can also write JSON structure. Then, there are So, there's a list of the extended what are called the extended primitive types. So, when you need to go and define

**[9:02](https://www.youtube.com/watch?v=heaA5E4vkQw&t=542s)** a data structure and you need to have a proper date and you need to have a proper time, you can go and do that in this model. Um and then there are um uh proper mappings of all those things of the serialization into um into JSON. For instance, integers in JSON are only safe up to 53 bits. It is impossible to represent a 64-bit number without loss in JSON uh in JSON serialization. That is the And that is the case because the JSON specification says in section four that it's okay to represent numbers with a double. But a IEEE double only preserves 53 bits of integer precision. Which means that is lossy. Now, certain implementations can certainly give you better precision. But the thing you can rely on, and I'm a standards person, is only 53 bits. So, in these extended primitive types, in the case for instance for uh decimal

**[10:06](https://www.youtube.com/watch?v=heaA5E4vkQw&t=606s)** and for all the integer values that are more than 64-bit that are 64 bits or more the encoding that is prescribed in this data data definition language is straight for JSON. Encoding of the same schema in Avro or in Proto which JSON schema JSON structure can be transcribed into will then be properly in 64 or int 128, whatever that system uh supports. You'll also see that the decimal type has precision scale as you would expect it. So, it's a scalable um uh value where you can go and define these things as you can do for instance in a database. So, the wisdom that we have in databases in terms of of decimal support, that is here in um JSON structure as well. And nevertheless it still looks familiar. Right? One of my one of my my my problems with some other language schema

**[11:08](https://www.youtube.com/watch?v=heaA5E4vkQw&t=668s)** languages was that they're not expressed in JSON. I like the fact that I don't need to have an extra parser for this, right? If you take you can standardize on proto, but proto requires you to have an extra proto parser. The proto parser, the proto language is not go, is its own thing. And it's also completely incompatible with you between proto 2 and proto 3. So, you have to have a bespoke parser to go and decipher this. And so, you're basically dependent on the Google tool chain to even get make a sense out of what's in your schema. And so, that's what makes this nice, and that's also what I like about Avro. Avro schema is also JSON, so you can very easily read this and you can go very easily evaluate this.

**[12:05](https://www.youtube.com/watch?v=heaA5E4vkQw&t=725s)** Um So then there are sets. So, type set um that basically means it's an this is an array where there's an where there's a guarantee and enforcement that the values are unique. And then there's a map where the keys can be anything and then the type must be string. Very straightforward, right? This requires more circus in uh in JSON in JSON schema. Then there are um tuples. A tuple is a very neat trick. Now, I'm in the business of doing real-time data telemetry and all these things. And one of the one of the problems is that we have is that we want to go and save space on the wire. So, the encoding of a tuple So, a tuple is exactly like an object, but the encoding of a tuple is an array.

**[13:02](https://www.youtube.com/watch?v=heaA5E4vkQw&t=782s)** So, the tuple um a keyword sets the order in which the fields occur in that array. And then if you wire serialize this, then you basically just put the the data on the wire and you leave the metadata in the schema. So, with that you get effectively the same you get the same effect as with Protobuf and with Avro where you leave the type information and the the the field names on the wire, but encode this as as text. And in many cases, that is just as efficient as doing that in proto or in in Avro, depending on the data that you have. If you have string heavy data, all the compression that you get out of Avro or proto will not help you much. And you can also go and compress this. So, it's a it's an encoding trick, but that encoding trick is kind of

**[14:05](https://www.youtube.com/watch?v=heaA5E4vkQw&t=845s)** governed by schema that's overlaid on top of it. Very simple. And that is And and and one more word on this. This is motivated by the fact that we now have schema registries, right? We now have metadata registries where we can go and say, here's a publisher, here's a receiver, and the the publisher and receiver, they can go and negotiate what the schema is because we need this for Avro, we need this for Protobuf. So, if we can also do this for for JSON data or some more universal format, right? Then we can go and use these sorts of of encoding tricks for JSON as well. Um one of the most terrifying keywords in JSON schema is dollar ref. Because dollar ref can occur anywhere and can practically pull any island of of JSON of of JSON schema into that place where

**[15:06](https://www.youtube.com/watch?v=heaA5E4vkQw&t=906s)** it lives. There's no constraint and it can also do external references, which means it can go and pull a file or in the worst case it can go and pull a HTTP destination from somewhere. Many resolvers many validators, many processors of JSON's JSON schema struggle with this. Um and the results are less than predictable and you quickly end up with a massive hairball because you have kind of have these references sitting everywhere. I have effectively cut that feature um from JSON schema and have reintroduced it in a much much more focused form. Dollar ref can only appear in this in this shape. There's a type and that type is a dollar ref to a destination inside of the same document. And it must refer to the definition section um that lives inside the JSON document

**[16:04](https://www.youtube.com/watch?v=heaA5E4vkQw&t=964s)** the JSON structure document. So the the the the JSON structure document has the good old JSON schema draft seven definition section because practically speaking JSON schema stopped evolving for mostly everybody's use with uh draft seven. There have been several drafts between them but adoption has been very spotty. So draft seven is kind of where the meat is. So I've been effectively picking that up. So when you want to have a reusable type that you can use anywhere inside of your definition, you put that into the definition section and then you can go and refer that refer to that from um inside of your document. In the core specification there is no import. There is no external file reference. If you stick to the core specification, all those schemas must be

**[17:00](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1020s)** self-contained, which is a quality if you want to use those schemas for passing messages around for instance. If you want to pass the if you want to use those schemas for, you know, data that lives somewhere inside of isolated networks where you don't where you can't allow access to the network just to evaluate the the schema. So, they're designed to be self-contained similar to Avro. And then there's an extension one of the companion specs that allows then for imports. So, dollar ref has been neutered to no longer be such such a hazard. There's an There's a explicit support for namespaces. If you're using schema languages for as the foundation for code generation, but also to capture code generated structures, you need to have name spacing. And if we imagine a very very large document

**[17:58](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1078s)** that define that that basically represents all of the data structures you have in the application, there will be clashes. Right? There will be enums which will kind of have name classes. These are the the the the most popular ones. So, name spacing as it is in your programming language is a good idea. So, namespaces are explicitly defined in JSON structure as a concept. They're schemas which have no type in them and they must live in definitions and then you can go and organize all of your all of your types and then you can go and refer to them. Import is an explicit act. So, if you want to go and refer to an external file which you can as I just said, um you can't in the core, but as soon as you are adding the import feature, then you can then you can then you can go and and import this. So, how how does this work?

**[19:06](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1146s)** Does this work? This Well, let's do the classic way. So, this is a file self-contained. This is the people.json and this is the second file that's now pulling in the people.json. So, dollar import means load this file. Load this file means make this file appear in this place as if it was local. So, it's literally basically putting a copy in there. And then you can go and refer to this as if it was local. So, it goes and pulls this in, puts this in here, and now you can go and do a type reference as if it was local. So, you don't you don't do a type external type reference in every place. You do this in the reusable type section, pull this in, and then you refer to it. Very clean model, very similar to how programming languages do this. Now, one of the things that people try to do with JSON schema, try to model

**[20:13](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1213s)** in kind of a convention way, is inheritance. So, there's you will find various um uh tools, like the Open API tooling, will use an all of construct to kind of model hierarchies, type hierarchies. Which works to some degree. The reality is if you're if you're designing a Open API document and you only test it with one tool, it will likely only work with that one tool. If you're doing anything that's more than just plain data structures, if you try to do something that's more sophisticated, things break down very quickly, especially if you want to deal with uh type reuse. So, I make this very explicit. You cannot So, one of things I would don't want to force on anybody is true polymorphism. So, I don't want to force you to write

**[21:09](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1269s)** it to use an object-oriented language or principles of object orientation. So, I say we want to be able to reuse definitions, common sets of fields, right? So, I say the address base is abstract. And abstract means there cannot be any instances of this type. You cannot instantiate it. Which means that now all the extends references will go and copy those fields effectively, and they will create a um uh a full object, which is then embedding these fields. And then you see address that's the last one here. And so, that's the normal-looking one. That's the normal-looking address that's just having these fields. When you have a code generator, you can you now have choices. So, you can either go and just implement the nest copies as you would do in C, in C#, in C++, in Java, you can still go and do this as a hierarchy, and the way how you would do this is you would go

**[22:17](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1337s)** and create an an interface with property definitions, and you will go and and and implement this. And it's allowed actually to go and inherit from multiple of these definitions. So, this is proper structured reuse without relying on polymorphism. Further enhancements, and these are we're now in the in the in the companion specs, is internationalization aliasing. We often have the have the requirement that we need to have a good label for field. Well, with alt names you can put labels with multiple languages straight into your definitions. >> [snorts] >> Sometimes it's required that your JSON serialization, the field name, is different from the name that you use in the schema. So, you can also go and do an override. So, this alternate name spec allows you to create alternate names for different purposes with your fields and including bringing the labels. Scientific units and currencies, there's a spec that goes and pulls in the entire

**[23:23](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1403s)** SI SI unit system plus the strange unit systems the Americans use Americans use and lets you declare units on those types. Because semantics matter and if you give this to a coding agent if you give a definition like this to AI, it will give you better results because it didn't know what's it knows what the units are, right? It knows what it deals with because you're doing that not in documentation, you're doing this formally inside of the schema. Same for currencies. And then if you love all the validation stuff that isn't JSON schema, which I have cut, well, I brought this back in the fifth companion spec. Um so, all of these all of these validation properties still exist, but you have to add them you have to load them in. Um and the same is true for any of all of one of. So, you can go and turn JSON structure in the end by adding this extension into

**[24:31](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1471s)** effectively JSON schema again by adding all these pieces. So, you think you can think of JSON structure as a refactoring in a way, right? Taking these toxic validation pieces out but adding them later and then adding a proper type system. So now you're going to say, "Huh? Okay. Why is he doing all these things? What is that all good for?" Um So first there's this website. So that's the JSON structure website where you can go and find all these specs. Let's go and look at one of them. So um one one thing that I that I paid attention to is that it's a good spec. And that it's actually from the start readable and gets straight to the point. This is what a schema is. This is what meta schemas are. This is

**[25:29](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1529s)** what data types are and you go into straight in the primitive types. Um in the JSON in the JSON schema spec, you don't get the types until page whatever 10. So it's an easy-to-read spec and AI agents understand this really well. They understand that spec and all the companion specs really well. So I've been trying to write to write this in a way that it's easy to understand. So here's the units and symbols and units scientific units and currencies. And these have all been submitted to the IETF, which means the IETF technically owns them, which means that use them as you like. Microsoft can't charge you for them. Another tool and that's a something that you like is Avro Tize and Structure Ize. Avro Tize and Structure Ize are the same tool just with different names. Avro Tize is

**[26:28](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1588s)** a tool that converts schema over Avro and Structure Ize is a tool that converts schema over structure. Converting schema means you can write a schema and then you can go and create code with it. And I created so that I don't have to go and do this you know run this in front of you, I created a gallery and if you go to avrotize.com, um the gallery is right there and I'm going to just going to, my demo will be showing you the the the gallery. So, we'll start with, I'm going to do, of course, Structurize today. Um, we're going to do Avro to JSON structure. So, here's an Avro schema. Type record, fields, event, event ID, um, and then it has an event source. So, and then JSON structure

**[27:24](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1644s)** kind of picks up these definitions. You see there's a name space name space definition here. We kind of put the data into the same name space, same organization, and you see that all the different types, uh, the different concepts here are converting. So, one of the thing, one of the things here, one of the qualities of of Structurize is if you start with a schema practically in any language, you in any definition language that you have, let's say, here's your XML schema. You can always go to, here in this case it's Avro. Let's go back to Structurize. Uh, we're going to go to Thrift. So, you now have a Thrift definition. You can always go then to, um, JSON structure and capture your, um, your data definitions there. It's intended so that you can go and and

**[28:23](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1703s)** rake up all of the data definitions that you have in your in your environment and to standardize them on one on one schema language. The choice here is to use either Avro, which is great in standardization well tool supported, however, limited in expressiveness and type system, or JSON structure, which is more, um, expressive and kind of more familiar if you know JSON, um schema. And then, you know, given time um the other thing you can do in with schema format, you can code generate. So, you can go from JSON structure to Python. And you can go and look at all these things, and it will go and generate all the data classes for you. It will do this for Python. It will do this for um uh Python, it will do this for C#, it will do this for Java, uh TypeScript, plain JavaScript, uh Go, Rust, um and uh C++.

**[29:28](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1768s)** So, you can go and and and all of these languages all support self-serializa- uh of the the the objects that are being generated sup- um support self- self-serialization. So, they will self-serialize into JSON, they will self-serialize into Avro, they will self-serialize into MessagePack. So, that you can go and use this toolkit, advertise, together with JSON structure as a universal way to go and um define uh to uh do data uh conversions or um also um you know, manage your data structures. And then, there's also a further section that you can go and look at, uh which I don't have time for anymore, which does the definition conversions into a database schemas. Well, I'm going to click one of them. Into MySQL. So, you define a data structure, and then it drops out the create tables the create table um statement. So, that's what I brought for today. JSON structure, a new way to think about

**[30:34](https://www.youtube.com/watch?v=heaA5E4vkQw&t=1834s)** schemas in a very familiar way. There's a whole SDK for validation that's uh that's associated with that, also in a bunch of languages. There's a CLI for it too. And then Avro Tize is a companion tool which allows you to do schema conversion through and with Avro and JSON structure. Thank you. >> [applause]
