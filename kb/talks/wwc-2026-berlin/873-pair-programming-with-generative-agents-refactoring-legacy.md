---
id: 873
title: "Pair Programming with Generative Agents: Refactoring Legacy Android at Speed"
slug: pair-programming-with-generative-agents-refactoring-legacy
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Frontend, Web & Mobile"
type: "Keynote/Talk"
stage: "Stage 12"
tags: ["AI Coding Assistants", "AI Models", "Agents", "Agentic AI", "Android", "Best Practices", "Gemini"]
speakers: ["Ahmed Tikiwa"]
speaker_companies: ["Disney"]
day: 2
starts_at: 2026-07-10T08:20:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=MpDqIaIgPrM
video_id: MpDqIaIgPrM
session_page: https://app.wearedevelopers.com/events/16/session/873
transcript: true
---

# Pair Programming with Generative Agents: Refactoring Legacy Android at Speed

**Ahmed Tikiwa (Senior Software Engineer — Disney)**

`Track: Frontend, Web & Mobile` · `Type: Keynote/Talk` · `Stage: Stage 12`

`#AI Coding Assistants` `#AI Models` `#Agents` `#Agentic AI` `#Android` `#Best Practices` `#Gemini`

[Watch the recording](https://www.youtube.com/watch?v=MpDqIaIgPrM) · [Session page](https://app.wearedevelopers.com/events/16/session/873)

## Abstract

We've all heard the promise of AI affecting how we code, but what does it look like in the trenches of a real-world refactor? In this talk, I walk through the transformation of "Upnext," an established Android codebase, achieved through deep collaboration with an advanced AI agent ("Antigravity").

We didn't just generate boilerplate. We tackled complex architectural shifts: migrating to Type-Safe Navigation 3, implementing Adaptive layouts with ListDetailPaneScaffold, and modernizing a broken CI/CD pipeline. I will demonstrate how agentic AI can act as a true pair programmer—debugging obscure Hilt/KSP conflicts, writing robust Fastlane scripts, and essentially "un-breaking" the build. Come see the future of workflow, where the developer directs the symphony and the AI handles the heavy lifting of modernization.

## Speakers

### Ahmed Tikiwa

*Senior Software Engineer — Disney*

Ahmed Tikiwa is a Senior Software Engineer at Disney and a Google Developer Expert for Android with over 12 years of software engineering experience and possesses a strong passion for developing world-class applications that can help make a difference. He has experience in frontend and backend systems, where he has built enterprise solutions for major telecommunication companies. He is currently a Senior Software Engineer based in Utrecht, The Netherlands, and an official contributor to Google Developers Dev Library.

## Transcript

*5,059 words · source: kome (en)*

**[0:00](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=0s)** Hi everyone and uh thank you so much for um for joining my talk. Um so this particular talk I've titled pair programming with generative agents refactoring legacy Android at speed and as I've been introduced I am um Ahmed and once again I am grateful that you took the time to attend my talk. Now, just a little bit about me. So, as she said, I'm a senior software engineer. Um, I work at the Walt Disney Company. Um, I'm part of the Disney entertainment division, particularly working on the Disney Plus app. I've been with Disney now for a year now, just over a year. Um, I am based in the Netherlands in a city called Utre. Uh if none of you have heard of it, it's about half an hour or so um outside of Amsterdam. Um yeah, so I'm also a Google Developer Expert uh or GDE in short. And what this is is basically it's a program that Google started 2011 that recognizes other

**[1:01](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=61s)** developers from around the world who try to commun uh contribute to the communities in some way or the other. So you could be giving talks, you could be uh doing YouTube videos, um podcasts and so forth. So I'm one of those. If any of you are interested in the GDE program, feel free to ask me afterwards. I'm happy to share some knowledge on how you can actually be a part of it. I also founded a company called Tequila Digital. Um, which is basically a a company that helps founders that turn turn beautiful ideas into apps as well as websites from a single codebase. And um on X you can find me at Ahmed Tequa and on LinkedIn you can just search for my name Ahmed Tequa and you can uh find me there. So in terms of the agenda, this is um how we're going to frame it. So I've basically put it into three acts. Um so here's a road map for the next 30 minutes. So we'll actually go through

**[2:05](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=125s)** this in three acts. The first is setting the stage. So I'll introduce the app, the technical debt we were uh sitting on, then the decision to pair program with an AI agent. So why did I actually do that? And then critically how we codified our architectural rules into agent skills. That way the agent could actually follow um our conventions. Then I'm going to cover the architectural um shifts. So this will be resolving a three-way dependency lock, migrating to navigation 3, which is the latest navigation uh library from um from Google. Then building adaptive app layouts for phones and tablets. And then I'll touch on fully automating the CI/CD pipeline as well as the deployment for my app. And then finally, I'll touch on the verification and results, how the agent actually proved its own work through tests, and then I'll give a before and after transformation of the codebase. So let's get started. So first let me introduce the subject.

**[3:05](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=185s)** So this subject here is actually called up next. And up next is a comprehensive TV tracking app built entirely in Jetack compose. So it you when I first created it, it was a m uh it was originally in XML. In fact, it was even in Java. Then I converted it to Cotlin. Then I had to convert it from XML to Compose. So right now the app is 100% compose, zero XML views, and then it has a multimodule architecture. So it's got um some main screens. So the first one being the dashboard. So there are two parts as well. So users can actually authenticate as well as just view it as a guest. So the dashboard will actually show you specific personalized shows for you from tracked or it'll just give you a generalized um um uh view. And then you've got the schedule which will be showing the shows that aired yesterday, today as well as tomorrow. Then there's a search screen so you can search for uh

**[4:07](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=247s)** any of your favorite shows. There's an explore screen which will show you most anticipated uh trending shows as well as popular shows. So these are also coming from the tracked uh API. Then you've got an account screen. So right now it is tied to tracked. So if you have a tracked account you can sign in and then you can actually keep track of your of your apps. And then so the different screens that you see there um the last one there being the detail screen. So if you're viewing a particular show that's what it looks like. The middle screen is the explore screen and then the one on the far left is actually the the dashboard screen. So in terms of architecture there's a clean multiodule uh architecture. So the app module handles the main application layer. Then the core data actually gives the room and retrofit with u our dependent uh repositories. Then the domain layer holds the models as well as the use cases. Then we have the shared common module for utilities.

**[5:13](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=313s)** Then there's a dedicated design system module for tokens as well as design components. Then we have a baseline profile module for runtime performance. So I actually do run baseline profiles uh within the app to make sure that it actually going to start up uh quickly. Then up next was running. Users were using it. They still are actually. So a bit of background. I built this app in 2015. I've been maintaining it ever since. It started off as a passion project of mine. It's got over 2,000 uh downloads. I'm still proud of it. It is also on the um on GitHub so and open source. So if any of you actually want to contribute to it, I have a URL at the end. Um so you are free to actually contribute to the app if you if you want to. So under the hood there were years of incremental decisions as well as accumulated um issues that turned into

**[6:12](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=372s)** technical debt. For navigation, I was actually using compos destinations, which is the third party library that solved boiler pre problems in vanilla compose navigation. It actually worked well. I actually had no issues with it. No complaints whatsoever. But it meant that every screen composible carried a library specific destinations navigator and we were always one dependency layer away from the official jetack navigation stack. So I was actually planning on navigating to um or migrating to the official jetack compose navigation once it actually caught up and with navigation 3 I actually feel it finally did. Deployment was entirely manual. So every release that I was doing to the Google Play Store uh me meant that I had to build it locally. I had to sign the AAB the bundle file. I had to go to the Play Console and then I had to upload it by hand. So you can imagine this is quite a

**[7:09](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=429s)** tedious process. Took me about half an hour every single time that I had to upload a new version. Then the CI checks were minimal at best. And then there was a dependency situation. So the Hilt KSP as well as the Cotlin um dependencies were all locked in this version triangle where upgrading any one of them would break the other two. This was the legacy trap. So it wasn't broken XML. It wasn't a Java to Cotlin um language migration. It was just accumulated architectural debt in a modern codebase that I didn't really want to touch. So I made the decision instead of continuing to chip away at this debt manually, one screen at a time, one period at a time, afraid of breaking something, I decided to pair program with an advanced AI agent called anti-gravity. So anti-gravity is is from Google. So it's one of the um the softwares out there and it is actually agent first. Initially when they released it last year in I think November. So I've been

**[8:13](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=493s)** using it since then actually. So it started off you'd have an IDE option and then you have an um agent option. So the IDE if you're um if you're familiar with VS Code you'll kind of get um used to it easily. um if not it will take a bit of uh onboarding to try and understand it but for me my use case I was always using the agent mode I never use the IDE I would use Android Studio as my IDE so this is how the interface actually looks so on the left hand side you will have um a list of the different projects that you can actually create within within anti-gravity and then within each project you can have sub projects as well or subtasks as part of that and on the right hand side if If you're familiar with chatting with Claude or Gemini and so forth, this is how the interface would look. Now, for me, this is typically how I would do things. I

**[9:16](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=556s)** would create a project and then I would have multiple uh tasks inside it so that it can tackle different things as part of my project. And it is capable of doing this. The better your your laptop, the the better it can actually perform at this. So, I actually had an Intel laptop when I started this. it unfortunately didn't handle it properly but when I upgraded to this uh M5 here then things actually started working uh working smoothly. So yes there is a need for for for hardware um in order for you to actually get the best out of these um Aentic uh software that are that are out there. So and when I say pair programming okay I actually want to be clear about what I mean by this. I'm not talking about GitHub copilot where it tells you or it autocompletes the next um code part of your code and so forth. No, this is not what I'm what I'm referring to.

**[10:17](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=617s)** Anti-gravity operates as an autonomous autonomous collaborator. So I define the target architecture and then the constraints as well. It proposes a full implementation plan. So every time I actually interact with it and I tell it, okay, I want us to try and work on this. I want us to build this particular thing. It will go ahead and create an implementation plan which is basically an outline of everything that it thinks it's going to do for that particular task. Then it will ask you to review that and if you're happy then it will start um with with the implementation. So I review I adjust it as well as I approve it. Then it goes and implements the changes. It compiles the code, reads the error output and then if something fails, it will try to fix it. It debugs the failures by reading the compiler output and then it iterates on the solution. So until it's reached a a solution that is successful, in other

**[11:18](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=678s)** words, things are running correctly, then it will actually um stop working on that particular task. So all of this happens in a branch isolation. So never touching the main branch directly. So my role shifted from being the one writing every line of code to being the architect, the reviewer as well as the director of the work. So you can see the collaboration loop on the right hand side. So the developer defines, the agent proposes, the developer reviews, the agent implements and tests, the agent debugs and then the developer does the final review and then merge the code into the codebase. So, but here's something I learned very quickly. You can't just point an agent um to your codebase and say, "Refactor this." If you do that, it's going to follow whatever patterns that it thinks of, sometimes even from the internet, which are probably not your patterns. So, it's not going to do it the way you want it to. So, this is also the thing

**[12:20](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=740s)** that you also need to understand as well, that there are things you're going to need to be specific about. And I'm going to show you what that actually means. So I actually needed a way to codify the specific architectural rules. And this is where agent skills came into play for me. So these are portable skill.md files which are marked down with YAML formattered that you can commit directly to your git repository in a folder called aagent/skills. Then the industry standard for AI customization is by using the skills. So it's also supported by over 16 tools including Gemini CLI, claude code, cursor, GitHub copilot as well as anti-gravity itself. The key design principle here is progressive disclosure. So the agent only loads the skills name and description at startup. So into into memory. then it only pulls the full instructions into its context window only when it encounters a task that matches that particular thing that you're asking for it to do. So I'll give

**[13:21](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=801s)** you a concrete example. So I had a rule that said screens must never receive framework objects. No destinations navigator, no nav controller, nothing from the navigation library. So instead they expose stateless lambda uh callbacks on navigate which takes a destination sealed class on back is a simple unit lambda. Then that simple rule defined once in a skill.mmd file and then it guided anti-gravity as it refactored over 10 screens consistently. So every screen ended up with the exact same clean contract because the agent knew the target pattern. So I told it in its own language in the skill and then you can see the actual skill.md file on the right hand side. So it's the name description as well as the rules. So the agent reads this and then it applies this consistently. I don't have to repeat this. It already has this now in memory. So before I could start with any real refactoring, we had to deal with something that I think many of you have

**[14:23](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=863s)** experienced, the dependency hell. But this wasn't your typical version mismatch. In this case, there was a three-way coupling chain where KSP, which is a Cotlin compiler plugin, its version has to actually align semantically with the Cotlin compiler itself. And then Hilt generates all of its dependency injection code through KSP. So you have this contract Cotlin, KSP and Hilt, they all have to agree or your annotation processing will just silently fail and then you'll actually get errors that are actually not even really useful. Um it'll just say hilt error and you won't be able to really know what is actually going on unless you actually then employ um an agent to try and debug for you. So what I found valuable here is that anti-gravity didn't just try some random version combinations until something actually compiled. It actually traced the dependency graph. It defined that even though I was using cotling 2.2 hilt needed the language level to be pinned back to 1.9. So for compatibility then it found that the hilt testing act

**[15:28](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=928s)** artifact was actually on a completely different release cadence than the core hilt which is 2.56 versus 2.59 and then it explained why the mismatch actually existed. So it systematically validated the pin against the actual compiler output then ended up with 14 dependencies which were blocked from automatic renovate updates but each one was blocked for a documented understood reason. So you can actually see here that the version catalog on the right the cotlin ksp aligned as well as the hilt pinned and then the language version explicitly set to 1.9. So this could have been a full day of trial and error but it became a systematic reasoned process. So with the build stabilized I then I could start the major architectural shift which was migrating to navigation 3. And this is a genuine paradigm shift. So in the old world navigation was managed by a blackbox nav controller. You would call navigate with a string route and then the framework would

**[16:28](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=988s)** handle everything internally. You wouldn't really know what's going on unless you really look at the code. You didn't own the state. And then in navigation three, the backstack is just a snapshot state list of type any. You now own it. So navigating forward is literally just using backstack.add. You push a destination onto the list and then the framework will render it. Navigating back is just backstack. Lost. You inspect it, you modify it, you clear it. It's a regular composed state list. Then lamp display observes that list and then it renders the appropriate screen for whatever destination um is on the top. Then now we have one thing we hit immediately which was jarring the visual effects during the screen transitions. So the default size transform was causing screens to collapse during pop animations. It didn't look nice. So the fix was actually adding using now to transition spec and then you can see it

**[17:25](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1045s)** highlighted in the code. So this disables that transform completely. Then I also had to temporarily disable um uh predictive back on an OS level as well just so that the navigation could also work well. Then this is where I want to be honest about the pair programming with an agent and how it actually looks when things go wrong. So we've now migrated to navigation 3. The code compiled and then the app immediately crashed. Every detail screen was dead on arrival. So what happened in the old navigation world? When you navigate to a screen, the framework stores the arguments in a safe state handle. But in navigation 3, the destinations are just cotlin objects in a plain list. They're not roots anymore. So when the view model tried to read from safe handle, there was nothing there. So here's the code that actually fixed it. The solution is called assisted injection. So instead of hilt automatically extracting arguments from the safe state

**[18:26](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1106s)** handle, we explicitly pass a root object from the composable into the view model constructor. Then the view model says I need this argument handled to me at creation time. So that's actually the the it assisted annotation that you see there. And then in the composible we use a creation callback to call the factory and then pass the root object inside of it. Then one more piece when a screen leaves the backstack its view model needs to die with it and that's what view model store nav entry decorator actually does. So it scopes the view model life cycle to the backstack entry. Then this pattern is now what is used across every detail screen within up next. So what I found valuable actually is that the agent didn't just try random things. It traced the conceptual mismatch, proposed the pattern and then applied it consistently across every single screen. This was with anti-gravity. So the second major shift was adaptive layouts. So if you look at these two screenshots

**[19:28](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1168s)** here, so this is actually the same app, same codebase on the left up next which is on a phone which has um the bottom navigation bar. Then on the right hand side it's a tablet with a navigation rail as well as a full list detail split. So the one on the left is single pane. The one on the right is um list detail. But how did we get here? So I'm going to show you the architecture. So I solved this using two layers of adaptation. The first layer which handles the overall app chrome. So navigation suite scaffold automatically switches between a bottom bar on the phones and then a navigation rail on medium screens and then it gives a full drawer as well as on large screens. So inside that we use navigation list details SP pane scaffold for the side by side layout. Then you can see the wiring in the codebase on the on the right hand side. Then the second layer is where it

**[20:31](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1231s)** get interesting. Our entire navigation three uh graph the nav display we just talked about it lives inside the detail pane. So when you navigate on a tablet the list stays visible on the left hand side and then you get the detail on the right hand side. So that's the app navigation call highlighted in the code. Then when you tap on a show in the list, we push a destination um onto the navigation 3 backstack and then a single line of reactive state derived state off which is watching the back stack and then it autoreveals the detail pane. So two independent systems bridge by one line of code. But adaptation doesn't stop at the scaffold level. So individual screens also need to respond to the screen size. So, show detail screen branches between an expanded layout and a compact layout based on the available width. So, even when you rotate your phone, it also becomes a new window size. When you put it back to portrait, it's another type of window

**[21:34](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1294s)** size. And this this scaffoldier takes care of that. It takes care of adapting to both of them. And then for landscape phones, which technically have wide screens but very limited height, we force the single pane mode to prevent a cramped split view. So these details are what adaptive layouts feel polished versus just technically correct. And then this is exactly the kind of work where having a pair programmer actually pays off. So the agent actually applied the patterns consistently across every single screen. The third major shift was my entire CI/CD pipeline and then deployment automation. So before this work it was entirely manual. Now we have three GitHub um action workflows. the pull request workflow which runs Katy lint detect lint unit test assembles both the builds and then it spins up an API of version 30 emulator for the full instrumented um test suite then the deploy workflow it runs daily on a chron at 2 a.m. But it uses a smart stateless

**[22:35](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1355s)** um staleness gate. So with which basically means that if I don't push anything to the repository, the cron is not going to run. Um that way I'm actually saving up on GitHub minutes as well. So it calls fast lane uh deploy internal which handles the entire release um life cycle. Then we also have a smoke test workflow for ondemand app. Smoke test runs on a headless emulator. And then for versioning I use what is called calver which is calendar versioning which uses the month uh the year the month as well as the count format. So it bumps the version it builds the release bundle uploads it to the play store internal track autocommits the version bump with a skip CI tag creates a git tag and then generates a GitHub release. I didn't have to do this manually. AI anti-gravity created this for me. Then also have renovate for automated dependency updates and then pre- push uh hook that blocks the direct pushes to

**[23:36](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1416s)** main because sometimes the AI actually gets it wrong. It will directly push to uh to the main branch unless you explicitly put rules on your GitHub or if you try and create rules within your repository as well to prevent it from doing that. So it may skip your entire branch flow completely. So you need to make sure that you keep an eye on that so that it doesn't push to main directly. If you rely on using pull requests in order to ma to merge into into main then the PR checks on every pull request daily automated deploys as well as on demand smoke test it also did that as well. Then how do you actually trust an AI agent didn't break everything the rooting of your application for example during a massive refactor. So you actually let it prove its work. So one of the valuable things anti-gravity did was actually write its own verification layer. So the app smoke test is an instrumented test with six methods that traverse the full back

**[24:41](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1481s)** stack on a headless emulator. So it bypasses automatically the onboarding. It navigates to the dashboard screen, the show details, the seasons, episodes, person details, all of that by itself. And then it verifies each screen that it renders. Then all of this runs on a headless emulator in CI on an API 30 on a pixel 6 profile. Then beyond that we now have 39 unit test files that it created for view models for the repositories for the workers as well as the network layers and then 16 in instrumented tests for navigation screenshots and screens. So that's 55 test files forming a safety net that gives me genuine confidence when I merge agent generated PR and I make sure that it needs to run these every time it makes a change. You need to ask it did you verify the changes and then it'll go through this suite to make sure that everything is running properly. Then we look at the transformation. So a

**[25:41](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1541s)** third party navigation library became officially type safe using navigation 3. A single pane layout became a full adaptive list detail pane scaffold. Manual play store uploads became daily automated fast lane deployments. The fragile CI became a multi-stage pipeline with instrumental tests. There were almost zero automated verifications, but this became 55 additional test files. So this didn't happen over months of manual refactoring. It happened through structured collaboration with the AI pair programmer. And I want to leave you with with four things. First, you need to codify your context. So you need to commit the agent skills to your repository. It's the industry standard supported by over 16 tools. Second, the pitfalls are the proof. So the hill scoping crash that I mentioned earlier, it taught me more about Navigation 3's internals than any clean migration could have. So those moments where the agent and I had to debug together were the most valuable part of this entire journey of collaborating with with the agent. Then third, let the

**[26:44](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1604s)** agent prove its work not through trust but through compilers, tests as well as pipelines. If it can't compile and pass the test, it's not done. There are times when I was out, it would say it's it's done with a with a feature and then I'm like, did you actually test it? And it's like, oh, you're absolutely right. Which is which is three things I hate. I don't like it when it says that you're absolutely right when I have to remind it. But then if you keep an eye on it, so you really can't let the agent just go, which is called yolo. So there's YOLO mode where you can just say I approve everything you're going to do. That is actually dangerous. If you really want a repository that's going to be great, a repository that you want to ship to your users without any issues and so forth, it's important that you keep an eye on whatever the agent is

**[27:44](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1664s)** actually doing. So the nice thing about anti-gravity is that every time it finishes a task, it will notify you. It will ask you to actually review the changes before. So after it's done the implementation plan, it creates what is called a walk through and it'll tell you this is these are the things that I did. You can also inspect the walkthrough and see did it actually cover everything correctly. So it's also important that you as the architect as the person actually reviewing this um this thing that the the agent is basically doing to also see whether it actually did the tests, did it do the verifications. Sometimes even um code static analysis also fails as well. So you need to also ask it did you do the entire verification sweet check? That's what I do every time. Sometimes it does it automatically. Then um and fourth AI is a collaborator not a replacement. So you set the direction, you define the architecture,

**[28:44](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1724s)** you review the output, you direct the symphony. Thank you for your time. >> [applause] >> Thank you Ahmed. Um we are a bit tight with time so I will just read one question. Um why anti-gravity does it in your experience does better job for Android in comparison to other platform? [laughter] >> That is a good question. So um I had I actually g had some before I started using anti-gravity someone gave me a trial um of Claude's code and then um I asked Claude to actually build me a feature within up next. So I was busy working on a particular feature that a user most user requested feature and I said Claude go ahead and build this feature for me and then it tried to build it. It reached I think 70% before it said my tokens had run out and then I had to wait I think 5 days before it it renewed or something like that. I gave

**[29:44](https://www.youtube.com/watch?v=MpDqIaIgPrM&t=1784s)** the same task to anti-gravity. It built it it built the the feature fine. It even built the end toend tests fine. It added additional tests as well. It did the running. I didn't run out of any tokens or whatever it is. So that's actually when I realized that Claude might be good at at coding, but it it has its limitations in in terms of being able to get my task finished from end to end. So I found that anti-gravity actually made things easier for me and and build the things uh correctly. I do switch from time to time within anti-gravity from Gemini to claude because you can change your model if you want to. But um anti-gravity for me so far has been has been great. >> Thank you very much.
