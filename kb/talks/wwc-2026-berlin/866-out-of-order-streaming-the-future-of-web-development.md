---
id: 866
title: "Out-of-Order Streaming — The Future of Web Development"
slug: out-of-order-streaming-the-future-of-web-development
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Frontend, Web & Mobile"
type: "Keynote/Talk"
stage: "Stage 5"
tags: ["Node.js", "React", "Web Performance"]
speakers: ["Julian Burr"]
speaker_companies: ["Sonar"]
day: 2
starts_at: 2026-07-10T08:20:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=WjoGKMgEO4c
video_id: WjoGKMgEO4c
session_page: https://app.wearedevelopers.com/events/16/session/866
transcript: true
---

# Out-of-Order Streaming — The Future of Web Development

**Julian Burr (Senior Developer — Sonar)**

`Track: Frontend, Web & Mobile` · `Type: Keynote/Talk` · `Stage: Stage 5`

`#Node.js` `#React` `#Web Performance`

[Watch the recording](https://www.youtube.com/watch?v=WjoGKMgEO4c) · [Session page](https://app.wearedevelopers.com/events/16/session/866)

## Abstract

As the pendulum of web development swings back towards the server, streaming has become increasingly popular. Specifically, out-of-order streaming through features like Suspense, one of the magical powers behind React Server Components.

Let's build our very own simplified version to explore how it works, what problems we are trying to solve, and what this future of web development looks like.

## Speakers

### Julian Burr

*Senior Developer — Sonar*

Senior Developer from Germany with over 15 years of experience building web apps and leading frontend teams, currently living and working down under in Australia. Pragmatic perfectionist, always looking for opportunities to learn and grow.

## Transcript

*5,461 words · source: yt (en)*

**[0:02](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=2s)** Thank you. Um as a warning up front, this is going to be more like a classic talk. I know a lot of talks here at this conference are about AI. I'm not going to mention AI at all. I'm sorry about that. If you're >> [applause] >> I hope it's still going to be interesting because we we we're basically just going to talk about a few fundamentals that I think are especially right now really important to understand and it really important to know why we're talking about these things. What I want to talk about is web rendering. And when you talk about web rendering very quickly, especially in the last couple of years, the conversation quickly goes to server-side rendering because we moved back from client-side first to like back to the server to increase performance. And when you talk about server-side rendering, the conversation quickly becomes about streaming.

**[0:50](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=50s)** Now what I want to talk about is out of order streaming. To understand what out of order streaming is, I think we need to understand what is the problem that we're trying to solve? How did we get to the point where we are today? How did the web essentially evolve? Um and then once we understand what is the problem that we're trying to solve, we're going to have a look at how does it actually work? Uh and why might it be the way we're building web applications in the future? So to get started and I will try to actually make this clicker work. The clicker will not work, so we'll do it this way. Um to understand what problem we're trying to solve, I think we have to take a quick look at the history of how rendering on the web actually works.

**[1:39](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=99s)** And we have to start at the very beginning. This might be me sharing my age. I'm quite old. Um but I see some of you in the audience, you might also be from that era where we were actually writing HTML, plain HTML, to build websites. Good old times that were much simpler. Um I'm mentioning in here. It could be any other server-side language. The idea is you have some server-side language that generates your PHP, so you can dynamically create your websites. Um, but essentially everything happens on the server. And what that means from a rendering perspective is uh, we're making or the user makes a request uh, in the browser with the URL. The browser requests the page from the server. All the server does is run that server-side PHP script. Returns the HTML. The browser renders

**[2:29](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=149s)** that, and that's it. Simpler times. That's all that happens, like. Um, that's great for building dynamic websites. It's great for the user experience of like you get all your HTML up front. Um, the user experience starts suffering when you start doing a lot of stuff on the server. So, once you start say querying stuff from a database, or you do other stuff where you're not really in control of it might take a long time. With this approach, you might just stare at a blank page and you see the browser spinner until the page is there. Um, and especially the more dynamic your application get, that's not great. The other thing is, talking about dynamic, you can't build dynamic dynamic applications. Like, again, we're JavaScript developers here, or I hope at least most of us are.

**[3:17](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=197s)** You can't build like user interactive applications with just HTML and PHP. So, naturally what we did is we introduced JavaScript. So, again, this is the early days. We're talking jQuery, uh, we're talking Ajax. So, jQuery, um, for those of you who are not as old as I am, um, is famous for making JavaScript more accessible to developers. It allowed us to be, uh, more easily cross-browser, and it just helped JavaScript become as popular as it is today. What that means is we can start using things like event listeners and local state on the client side to actually do stuff more dynamically in our websites. And what Ajax allowed us to do is to make network requests even after the page has already been loaded. So before,

**[4:06](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=246s)** we always had to go back to the server. So with the PHP um HTML approach, when you click on a link, you do the whole loop again. You go back to the server. The whole HTML has to load again. Ajax allows us to be much more specific about what we want to load. So instead of having to go back to the server for everything, we can say, "Okay, the navigation actually sticks around. So we need We don't need to load that from the server again. We only want to load the content." And because we're fully in control on the client side, what that means is A, we can load less um from the server, making that server request faster. And we can create a much better user experience because instead of just using the browser going blank and showing you the loading spinner, we can show a loading state on the client side while we're loading the new content via Ajax.

**[4:56](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=296s)** So theoretically, this was great. From a request perspective, it's still very similar. So we request the page from the server. The server responds with the HTML. We still have some templating, some server side happening there. But additionally, we're now shipping a JavaScript file. And that JavaScript file very selectively says, "Okay, these elements need to be interactive." So again, event listeners or whatever whatever you want. Uh and they very selectively inject that dynamic stuff into the page. And again, like the big benefit here is when we navigate around, we can be much more specific about which parts we actually need to reload from the server instead of loading the whole page. So from a user experience perspective, this got better, but the developer experience at the time,

**[5:43](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=343s)** again, for those of you who were around at the time, jQuery is famous for the spaghetti code era. And that's for a reason because there weren't many guardrails to help you build large complex applications with jQuery alone. To fix that, we started using frameworks. So, this is the early days uh when there was Knockout JS as a famous one, but then we quickly have Angular, Vue.js, React.js, whatever you use. The idea is what if we took it to the extreme? What if we take the server completely away? The server just basically renders an empty shell. And then we have a front-end framework that deals with rendering the website. And that fixes the problem of um

**[6:32](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=392s)** the dynamic part of like the spaghetti code. So, now we have frameworks that can deal with the whole application life cycle and make it much easier at a larger scale to build dynamic applications. Um and that's fun, but what it means is when we look at the request flow, we're now requesting the page from the server. Like I said, the server just returns an empty shell, so the user still doesn't see anything. And then we load the JavaScript. The JavaScript has to be downloaded, executed. Once it's executed, it will inject all the HTML into the page. But once it's there, it's there, and everything is technically dynamic. You can have event listeners and and and dynamic parts in anywhere in your page. So, that was like the boom of single page applications, SPAs, and those frameworks is because again, in modern era, you want to have a lot of state, you want to

**[7:20](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=440s)** have a lot of interactivity, uh user interactivity. Front-end frameworks were great for that. But the bad part is what we saw here, like that initial load. Again, especially at larger scale, the more complex your application get, the more JavaScript you actually need to load, the longer that initial load will actually take. And that isn't great. So, we went to the full extreme. Uh we went all the way client-side and realized, "Okay, maybe we went a little bit too far." And this is where for the last five or so years, we've seen the trend going back to the server. So, we have frameworks like Nuxt for Vue.js or Next for React.js uh and a lot of other frameworks that try to bring us back to a lot of the best practices that we had previously with server-side rendering. So, how can

**[8:08](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=488s)** we fix that initial load specifically? The idea is we still want to keep those frameworks. Like React is great. Vue is great. Like we want to keep the developer experience of those frameworks. We want to keep the ability to build dynamic applications easily from those frameworks. But we want to fix that first render. So, with Node.js, um the idea is just, "Okay, let's render the page that we're trying to render on the client on the server first, have that initial HTML, and then the client-side takes over. And basically what that means is, from a rendering perspective, we're requesting the page from the server. The server returns static HTML. Again, HTML itself doesn't have any concept of dynamic parts. It doesn't have any concepts of event listeners or anything. So, this page will load and it

**[8:56](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=536s)** will look like it's loaded, but it will not be interactive. For that, we need the front-end framework on the client-side to take over to actually inject all the dynamic parts, and then the user can click around on the carousel and do stuff with it. Um so, this is basically trying to get the best of both worlds. We get the quick first render, and then we inject all the dynamic parts. Technically, we're not making the website load faster in the sense of like faster to interactive, but it feels faster. So, there's also an element of perceived performance that we're trying to achieve here. Um but there are several problems with that. So, the first thing is what that process is called is hydration. You might have heard it because it's it's been one of the

**[9:43](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=583s)** booming words. The process of injecting the dynamic parts means most front-end web frameworks, what they do is you have to render twice. You have to render the same thing twice, once on the server to get the static HTML, and then doing the same thing again to build a virtual DOM that you can then use to inject the event listeners and local state and all of that stuff. You're rendering the same thing again on the client. So, hopefully that's clear. Like that is not very efficient. Again, at larger scale with really complex applications, this hydration thing can become the new bottleneck. So, yeah, we improved the initial render, but the time to interactive is still really slow. There's two other problems with this. One is with the server-side rendering, we're

**[10:32](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=632s)** kind of reintroducing problems that we previously had. So, when I said in the PHP HTML era, when you do a lot of stuff server-side, that initial rendering can be really slow, and that's just the browser showing you a loading spinner, we're back to that. Because again, for that initial render to get that snapshot, we need to render the whole page. So, if that page needs to go and fetch some data or needs to do some other stuff that's that's going to external sources, if that render is slow, the initial page load will be really slow. How can we solve that? So, you might have heard something uh that's called server components. So, React um kind of made the term popular, but

**[11:21](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=681s)** a they they weren't even the first framework to do it, uh and there's a lot of other frameworks doing very similar things. The idea is what if we don't have to hydrate everything? If we look back at our hydration thing here, not everything on that page is actually dynamic. Not everything on that page needs JavaScript. So, what if we could tell the bundler which parts of our code base are actually dynamic and need to be rendered on the client and which parts only need to be rendered once on the server and then are done. And that's the idea behind like React server components. You have the use server directive where you can tell in the file, okay, if this is use server, this will not be shipped to the client. Um and in other frameworks, you have similar ways to just again give the bundler a hint, this code doesn't need to be executed on the client, exclude it

**[12:11](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=731s)** from the client-side bundle. And what that does is two things. For one, we can reduce the client-side uh bundle size in a real-world large-scale application dramatically. Because again, you don't need to include stuff that only needs to be rendered once. So, the flow is still the same. We request the page from the server. We get that initial HTML. And then we only need to hydrate the parts that really need JavaScript. And you might notice this looks very similar to what we had before with like PHP and Ajax. We're very specific now about, okay, which parts are dynamic, which parts actually need to run client-side code. And this is where all the memes start of like, okay, isn't that just PHP again in a nutshell? Like, what's what's the deal

**[12:59](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=779s)** here really? Um and I mainly mention this here because it's like this is something that I feel a lot in the uh like technology like in in our in our community when we come up with new concepts, that there's the idea of uh it takes ideas from old systems or from old paradigms, it can't be innovative or it can't be like groundbreaking because it's an old idea. I think this is very wrong. What you see here as an example and what you see in a lot of other places, especially around web development, uh is let's learn from the last 20 years of where the web came from and where we are going and let's pick what we learned from it, the best parts from the server-side render era, the best parts from the client-side render era, and

**[13:48](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=828s)** bring it all together and actually make it work with the new technology that we have. And I think that's really important to understand. Yes, it feels like we're coming back full loop of like, okay, isn't that just like Ajax? But there's so much more that happened since then. We have all these great frameworks now. We have application frameworks that help us build large-scale applications much, much easier. And we get the benefit of like server-side rendering and all this all the stuff around it. All of that being said, this doesn't solve the problem with that initial render. If I'm fetching stuff from the database, that initial render will still be slow. So, how can we fix that? This is finally where out of order stream comes in. So, I hope this gave you an idea of how we came full circle from like full-on just static rendering, server-side rendering,

**[14:37](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=877s)** then full-on client-side rendering, to now back to like trying to get the performance benefits of server-side rendering. What we're now going to do is to see, okay, how does classical server-side rendering work? How can we improve the performance, especially of the initial render, with streaming or with traditional streaming? And then we're going to look at, okay, how does out of order streaming work? What is it? And how does it fix the problem that we have? To do that, we're going to do a little bit of live coding. Uh we're going to write some code um because it makes me feel alive on stage. So, I have a demo application here. And uh this is a Node.js server.

**[15:25](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=925s)** It could be any other language. It doesn't need to be Node.js, but I hope this is easy to follow. I'm also using JSX with like the React renderer. Again, this could be any other framework. The framework doesn't really matter. The important part is here. We're trying to render an application and this is kind of the output that we're getting. What this application is, it's kind of like the movie app because that's what people do in their demos. And it shows like the title of the movie, some meta information about the movie, and then the actors in the movie and some related movies that we might want to watch. And what it looks like in the code is we have several components. The header component is rendering the logo here. Then we have the title, which is rendering that information about the

**[16:12](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=972s)** the movie details and similar. Hopefully self-explanatory. If we look at those components, if we just open up details for a moment, you can see, if you're familiar with React, they're fairly standard React components. So, they're just a function that returns some JSX. The interesting part here is that it's an asynchronous function. So, this is part of the idea of server components. If we know that this component will only ever run on the server once and doesn't have to run on the client, we can make it an asynchronous function because on the server we can just await. And therefore we can do stuff like fetching data from a database or in this case, we're fetching the movie details from an external API. We can do that inside of our render, which is neat, and

**[17:02](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1022s)** we don't have to deal with like life cycles and anything else. So, for the sake of this demo, treat all the components that we have as server components. So, everything that happens happens on the server. We're trying to not ship anything to the client side. So, what that looks like in the actual render here is we take that JSX and then in classical server-side rendering, what you do is you just start accumulating the HTML that you want to return. So, again, this is the problem that I tried to explain in the beginning. If you do a lot of work, it going to take a long time for the server to actually have all of that HTML ready to then actually send it back to the browser, so you might just be looking at a blank screen for a while. Um but what that means in practice is that we start getting the doctype ready,

**[17:50](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1070s)** then we loop through all our components um that we're in here. So, JSX gives us like a nice children that we can loop over. Um we render that to an HTML string. Then we put all of those components that we just rendered together in order. Uh and then we close the HTML. And at the end, we set the header to text HTML and return that to the browser. And this is literally what you see here. If I click on another thing, what you can see is I clicked, you might have not seen that, but it takes a while for the page to actually load. So, again, this is the problem with classical server-side rendering. The server has to finish all the work before it sends the stuff back to the browser. And from a user experience perspective, this is awful because there's no real user feedback that something is

**[18:37](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1117s)** happening while I click this. And then after a while, it just snaps in. So, this isn't great. So, what can we do about it? I kind of already said that. The first step how we can improve that is streaming. So, streaming has been around basically forever, ever since we have the web. And the idea is that you don't have to wait for everything to be ready. The moment you start having the first HTML parts ready, you can start sending them to the browser. But you tell the browser, "Hey, this is not everything. I will keep sending you stuff." And then once you know that you're done, you tell the browser you're done. Uh and how you do that in Node is also very simple. Instead of just accumulating everything and sending it at the end, we can actually use response.write. Um and that will

**[19:28](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1168s)** do exactly what I just described. It will tell the browser, "Hey, here's some HTML that I'm I've read for you, but keep the connection open because I'm going to send you even more." Um and we can keep doing that with everything else as well. So, now that we start sending immediately, we also want to send the header immediately. Um and let's do that. Since they're returning, let's just write that to the stream. Don't need that. And instead of sending at the end, we just tell the browser, "Okay, this is this is finished." After everything is sent after everything is written to the stream, we just call response.end, and

**[20:17](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1217s)** that tells the browser, "Okay, the connection can be closed." And if we now reload the page, what you're going to see is we immediately see the logo, which is great. Um and then it looks a bit weird. And now like all the sections are out of order because before we were trying to optimize it, we were loading the different components at the same time. Um but if we're writing to a stream, we can't really do that because if you're writing to a stream, the HTML that you send will be rendered in the order that you send it. So, there's several ways how you could fix that. I'm going to do the lazy one. Uh so, this is not the optimal way of doing it, but uh for the sake of this demo, I'm just going to loop through the children one by one and await each of those individually. So, what that means is again I can

**[21:05](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1265s)** refresh the page, I see the logo immediately, and then I see one section at a time as it's ready. Hopefully. Cool. So, this is closer. It's still not great, because I'm I'm still If the logo wasn't there, I would still be looking at a blank black screen. Um but from a user experience, at least now I get immediate feedback. I clicked on the thing, and the page at least did something immediately. But what I really want is kind of like that single page application feeling, right? Where because it's all on the client, I can just render loading states and and and and fancy stuff. How I would usually do that in frameworks like React or Vue is do something like suspense boundaries, right? So, the idea of I have a boundary here.

**[21:57](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1317s)** Uh let's call it title for a skeleton. And what suspense boundaries do is it's literally just a container that says, "If there's anything asynchronous happening in this container, render the fallback until the asynchronous thing has resolved." So, that could be like API calls like we're doing. That could literally just like code splitting or other asynchronous asynchronous things that you want to do. So, if we put that in here, um and so on the client side, that's easy because again, we're in full control. But how would it work on the server side? Let's implement it. If we go to our suspense file, all we really want is in the first step, I just want to render the fallback, right? Just as a starting point. Uh nope, that is not helpful.

**[22:59](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1379s)** Um so, So we saw to a suspense component, you pass in the children. That is the asynchronous stuff that might take some time and you pass in the fallback that we want to render while it's doing that. And I'm going to just do the fallback for now. A bit of CSS trivia. Um if you give your uh your container a display of content I think it's content. Uh it will ignore this container for layout purposes. So if this is within like a grid or within a flex layout, it will skip this and still render your flex layout within your children as you would expect it to. Um but what we really want is just say, okay, cool. I want to render this fallback. This is also not a TypeScript

**[23:47](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1427s)** talk, so this any is totally fine. And this is not working. That's great. Oh. Uh suspense is not defined. Yes, that makes sense. So we actually need to import this. Okay, let's try that again. And what we see is kind of what we expect. So this is like the loading state. Yeah, you can see that. Uh this is the loading state that we want. And right now we're not actually doing anything smart. Like we're not actually dealing with the suspension of stuff. We're just rendering the fallback state. So how can we then take the components, the asynchronous stuff that we suspended? We kind of want to have a map to keep track of all of those.

**[24:35](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1475s)** So let's have a suspended map. And then let's have a UUID. And then in here we just assign the children to that suspended map. Again. Don't judge me for my TypeScript here. But the idea is, okay, we're rendering the fallback, but we're keeping track of the children that we haven't rendered. And then what we're going to do in the actual server-side rendering is after we render the initial page, let's actually check if we have suspended components. We want to loop through those components

**[25:29](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1529s)** and render them. So again, this object contains all the React components that we haven't rendered yet. And then we kind of do what we were doing before of just taking them uh getting the type of it and render to string. And we're just writing that to the stream for now. And if we do that, hopefully still see the loading state of the title. That is not doing what I was hoping that

**[26:24](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1584s)** it would do. All right, let's take the escape route. What are we doing wrong? contents children contents >> Let's see if this does the right thing. Live coding.

**[27:21](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1641s)** Okay, I'm just going to talk through it. The idea is that we're taking those suspended components, we're rendering them um after the fact if there are suspended components. Um and what would happen usually is that they get appended to the document at the end. So, this is the fundamental problem with classical uh uh server-side streaming is that you can only append to your document. So, again, we can't really have that effect that we want is uh uh swapping things out. To actually swap things out, we need to introduce a little bit of magic. And this is kind of like the thing uh that I showed here. Going to talk about this in a second. But basically, what we have here is we

**[28:07](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1687s)** introduce a template tag. Uh and what the template tag does in HTML, it's basically the the browser will just ignore it. So, for us, it's just a place to put in the content that we actually want to render. Um and then we need a little bit of JavaScript um to actually swap that out. And again, I won't actually have time for that. But so, what we're actually doing here, I'm I'm mixing three things together. So, we have the template that renders like the thing that we want to swap into the loading state once it's ready. Um and then we have two options to actually Jesus Christ. Ah, this is why it wasn't working. Okay. You live and you learn.

**[28:58](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1738s)** It's not suspended. Export. Jesus. Okay. So, what we can see here now is kind of like the end state that I wanted to get to. We have multiple suspense boundaries, so we can control like the individual parts. This is still not loading. This is a disaster. But, the idea is, okay, we we can without of order streaming, you can control, okay, we're not just appending to the document. We we have two ways to tell the document to swap out the initial loading state with the actual

**[29:45](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1785s)** content that has been loaded. One is to use JavaScript, so this is what React uses for React suspense on the server or view with the it's experimental, but view suspense on the server. Um, which is just having a little bit of JavaScript that says, okay, I know my container because there's an ID on the container. I'm going to swap that out, um, with the actual content once it's ready. The alternative is what I wanted to talk about. Again, I'm running out of time here. There's a new proposal, and I think I have a link for it. I don't have a link for it. I will have a link in for it in the slides, uh, when I share them. Um, but Chrome started shipping a proposal for declarative, um, HTML that allows you to define those out of order streaming boundaries

**[30:33](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1833s)** where you have the templates uh, that tells you, okay, this is my suspense ID. And then you can define this is the start and the end of where I want to put the content and you can do it in pure HTML of defining okay these are my boundaries and this is where the content will eventually come in and you don't need any client-side JavaScript at all anymore. So I apologize that the demo hasn't worked for most of the parts but I hope somewhat assured there are ways how to take the idea of streaming take it to the next step and actually make it a user experience that feels like client-side rendered apps where you are in full control of the loading states and be able to swap out content on the fly while doing most or not potentially even all of your work on the server. These are some resources if any of this

**[31:22](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1882s)** is interesting to you and you actually want to look at demos that do also work that I highly recommend just start reading on about it and these go then deeper into other resources talking about again react suspense on the server and the motivation behind that etc. This is the repo to the demo again you can actually look at the code that should have been working. There's a link to the slides that have all the resources and some code examples in it. And a link to some socials and this is all I have time for. Thank you very much. >> [applause] >> Thank you so much for your presentation. We have couple of questions one of them. Where does Islands architecture fit into

**[32:11](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1931s)** this story? >> What architecture? >> Islands architecture. >> Yes. >> Island architecture. >> Island architecture. Oh, yeah that that is Island architecture is exactly that idea of server components. So especially in view that's that's where they talk about Islands. That's the idea of having um specific parts that you can define as I know this only needs to render once Um, if you can tell the bundle of that, it can avoid sending that to the client and reduce the client-side bundle size and speed up your rendering, essentially. >> Yeah, that explains very well. And, um, can you bundle application from both static HTML and server components? Is it similar to >> Sorry. Both static HTML and server components. Um,

**[33:01](https://www.youtube.com/watch?v=WjoGKMgEO4c&t=1981s)** you you could do that. The idea of server components is that you still can use your client-side frameworks, so that you wouldn't write static HTML, but you have a way to write React or Vue, but don't have to worry about it bloating up your client side. So, the attempt is to try to get away with not having to go back to write static HTML, but there's also nothing stopping you. You could basically we could go into micro frontends or whatever else. It's the you could mix it if you wanted to. The benefit of sticking to the framework and just make letting the framework do its work is that you get everything else around it for free, essentially. >> Yeah, that explains architectural decisions. >> Yeah. >> Thank you so much. >> Okay. >> [applause]
