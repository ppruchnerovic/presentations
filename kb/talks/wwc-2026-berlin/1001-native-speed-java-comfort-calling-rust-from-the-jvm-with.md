---
id: 1001
title: "Native Speed, Java Comfort: Calling Rust from the JVM with Project Panama"
slug: native-speed-java-comfort-calling-rust-from-the-jvm-with
event: "World Congress 2026 Berlin"
event_slug: wwc-2026-berlin
track: "Languages & Runtimes"
type: "Keynote/Talk"
stage: "Stage 8 - powered by Red Hat"
tags: ["Java", "JVM", "Performance", "Rust"]
speakers: ["Gonzalo Ortiz Jaureguizar"]
speaker_companies: ["Startree"]
day: 2
starts_at: 2026-07-10T13:40:00+00:00
duration_min: 30
recording_url: https://www.youtube.com/watch?v=KS6JBr3pAvk
video_id: KS6JBr3pAvk
session_page: https://app.wearedevelopers.com/events/16/session/1001
transcript: true
---

# Native Speed, Java Comfort: Calling Rust from the JVM with Project Panama

**Gonzalo Ortiz Jaureguizar (Performance Engineer — Startree)**

`Track: Languages & Runtimes` · `Type: Keynote/Talk` · `Stage: Stage 8 - powered by Red Hat`

`#Java` `#JVM` `#Performance` `#Rust`

[Watch the recording](https://www.youtube.com/watch?v=KS6JBr3pAvk) · [Session page](https://app.wearedevelopers.com/events/16/session/1001)

## Abstract

The JVM is one of the most impressive pieces of software engineering, making Java incredibly fast for most workloads. But sometimes, "fast" isn't enough. For domains like scientific computing, AI, or processing massive volumes of text, we need to call highly-optimized native libraries written in C++ or Rust to gain a critical performance edge or access functionality not available on the JVM. For decades, this meant using the Java Native Interface (JNI)—a powerful but notoriously complex and unsafe bridge to the native world.

Enter Project Panama. With the Foreign Function & Memory (FFM) API, Java finally has a safe, supported, and elegant way to call native code, eliminating the need for brittle glue code and manual memory management. This talk puts it to the test with a classic Java challenge: the regular expression engine.

Join me for a practical, hands-on session where we will replace Java's capable but often-outperformed regex engine with Rust's highly optimized regex crate. We will walk through two implementations side-by-side: the "old way" with JNI and the "new way" with Project Panama. You will see firsthand how Panama simplifies interfacing with native code and improves safety. We'll cap it off with live benchmarks to compare the performance of both approaches against standard Java regex, helping you understand not just how to call native code, but also when it's truly worth the effort.

## Speakers

### Gonzalo Ortiz Jaureguizar

*Performance Engineer — Startree*

I am a software engineer specialized in developing databases in Java. I love understanding how libraries and frameworks work under the hood and to design and implement high-performance systems. I have worked on prototypes such as ToroDB, the first Spanish database unicorn Devo and since 2022 I'm working at StarTree as an Apache Pinot contributor.

## Transcript

*5,001 words · source: kome (en)*

**[0:00](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=0s)** Good afternoon everyone. Welcome to the fifth talk of the day. Fifth talk of the afternoon. I am Ninad Nete. I'm a moderator for this talk. I'm a senior software engineer at Portugon. And at Portagon, we connect financial financial uh institutions which are fragmented to enable investments into private markets. Something that I truly love to understand is how stuff works under the hood. And that's the reason I am really excited for the next talk. And just a reminder, I would I would like to ask everyone to ask questions in the congress app. And if we have some time towards the end, our speaker will be more than happy to answer it. As you know, the JVM is incredibly fast, but for workarounds like scientific computing, AI, and processing massive volumes of text, sometimes even the standard Java simply isn't fast enough. We often need to tap into highly optimized native libraries written into C++ or Rust. Our next speaker is here to show us a much better way. He will

**[1:01](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=61s)** demonstrate how to safely and elegantly bridge Java and native code. Our next speaker is a performance engineer at Star Tree. As a software engineer who loves to specialize uh he he specializes in developing databases in Java. He loves digging into how libraries and frameworks work under the hood and to design high performance systems. In the past, he's worked on building database prototypes like Toro DB. He's also worked with Spanish database unicorn Devo. and since 2022 he has been bringing his database performance expertise to star tree as a core apache pino contributor. Please help me in welcoming Gonzalo Ortiz on stage. Please give him a big round of applause. Hear me? Um yeah, as he say I'm Gonzal Ortiz. I'm um staff engineer at Starry which is a company that provides support for Apache. I'm also a contributor and committer of Apache Pino which is a all up database written in Java. Okay. And one of the things we are trying to do or

**[2:01](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=121s)** doing in the latest you know last six months is trying to migrate some parts of the Java code we have into native code. You may ask why would you do that right? And basically there are three different reasons. Most of you have been already think about performance, right? At least we write anything in a native language is going to be faster and I'm not so sure about that. Uh because the GBM is super fast, the just in time compiler is great and the garbage collector is also very good until it isn't. But in my opinion, the two main reasons why you would like to write some code in Rust or use some code in Rust or any other native language are the two first, the ecosystem and the ergonomics. The ecosystem means that there are tons of libraries around that are already written in a native language. Libraries like machine learning or compression libraries, crypto libraries, things like that. Or maybe you want to

**[3:01](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=181s)** call some API that is you know um some OS API that is not available in the GVM right maybe you want to use some iOS ergonomics means that you don't want sometimes and in penote we have to do that a lot you have to work with offhe memory or you you can do that in Java but it feels like you are fighting against the language you are not being helpful held by the language, you are fighting against it. There are tons of protections in Java that don't let you do the things you want to do, right? And sometimes it's just easier to write the code in a different language and then call it from from Java. And in Java, we have two ways to do that. since almost the beginning of Java 30 years ago 29 in fact uh Java 1.1 introduced Java native interface or GNI which is 30 years old I mean it's probably older than some of you and then two years ago

**[4:01](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=241s)** in Java 22 uh Panama project Panama was released okay and this two ways to call Java to call native code from Java and Java from native code they follow two different completely different um philosophies in GNI. The idea was okay everything is going to be easier from the Java side and then you are going to have almost full access to the GVM on the native code but you're going to need to write a lot of glue code on the native code right and that works but it's pretty difficult to write code that way for the let's say that you are a maintainer of a native library you don't want to be writing uh connectors for each language which in the world right so in the end what happened is that most libraries provide just a [snorts] Cabi or FFI interface almost all languages can call and you can call them from Pino from from sorry um Python from Go from Node from

**[5:01](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=301s)** whatever language you you care but not from Java it's very difficult to call it from Java and that's why one of the reasons why project Panama was created as a way inside the GDK as a way to make it easy to called all these libraries that are around right and the main difference is that in G the has to know that is being called by a Java Java virtual machine and it has abilities to talk with the Java virtual machine. You can send objects to to the native code while in Panama the native library doesn't know anything about the caller. It's just a normal native library. Okay, here is an example and I don't I guess the pointer is not very useful here but the this is an example of a very simple call adding two integers in both written in both GNI and Panama. Okay, on the left hand side you can see GNI and you can see that the Ras code is pretty

**[6:01](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=361s)** verbose. We have a very strange name, right? this java_com example leave add and we have these two integers we want to add but we also have more stuff we have two parameters we don't care about while in Panamai is super simple it's just a standard Rust code adding two functions okay but on the Java side we have it's just the other way around right on the Java side in GNI we have okay a little bit of extra code saying okay load this native library and there is a native method called add that accepts two integers. Okay, the GBM is going to use a convention and assume that there is or look for a method in the native library that is called Java the name of your package and then the name of the method plus the arguments and it has to include this two static arguments there. Once you have that, the GBM is able to find the method. Then you can call it as a

**[7:02](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=422s)** normal static method in in Java as always. But in in Panama is just the opposite. Here's where we have all the glue as I said before. Right here we are saying, okay, load the library again and then look for this specific method that is called add and accepts two integers and blah blah blah blah blah. Okay, once we have that, we have to also load the library and once we have that, we can call it. Okay, it's a little bit uglier in Java, but is quite better in Rust. Okay, so far so good. That's a very simple example. Let's try to do something a little bit more complex. Let's send let's try to use regular expressions, right? All the examples are going to be based on regular expressions. And let's simply have a very simple method where we send two strings. one is the regular expression and the other one is the hat we want to verify and it returns a boolean value.

**[8:01](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=481s)** This is how this function could be written in rest. Okay, again we have the names the strange names blah blah blah. But the important part here is that we have the strings in GNI. We receive a string that is a J string which is basically saying okay this is a pointer to a string in Java. Okay, it's literally a pointer to the string on the Java heap. In Panama, we don't have that ability. As I said, we don't know we are being called by a GBM. So, we need to express that somehow. We need to express that we need two strings as arguments. We could use the rush string as argument, but we need something that is not rushed specific. So, in this case, I'm just using a very simple pointer plus uh size to represent that. Okay. But we want to send that information to the regular expression library. We are using Rust and that regular expression library is using a strings strings from

**[9:02](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=542s)** Rust. So we need to transform them. Okay. And that's for example why in in in GNI we have this uh method sorry this uh attribute parameter m which is basically a reference to the GVM itself and some methods you have there and you can use that to ask hey please GVM transform this Java string into a another representation in this case an UTF8 uh codification of the same information right which means it has to copy all the data and encode that in a different format. on the right hand side on Panama the thing we are saying is okay assume that this pointer is actually an UTF encoded string and that is going to be a string in in rust and we are going to use it okay in in Java again calling that it's completely different than GNI and Panama okay in GNI is going to be again very simple and as you can see here we are just calling the method with two strings

**[10:02](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=602s)** that's it we don't pair. Okay. But Panama is quite more complex. In Panama, as we say, we need to send byes codified in UTF8. So we need to do something like this. Here we are saying, okay, let's give me that string, but get me the bytes that represent the same information in UTF8. I don't want an array and bite array in Java because I cannot send something on the heap to rest because some things on the heap can be moved can be removed by the and the GC could decide that it's not reachable anymore and they can relate it. So we need to have a copy of the data. Okay, the same way we have a we had to do a copy in G inside the native code to transform the code if the way it's encoded we need to do the same thing but in this case in um in Java. Okay. And that memory is memory that is of heat and we need to collect that memory. We need to the GVM or the GC is not going

**[11:07](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=667s)** to take care of all the off heat memory we are using. This is an advantage and a disadvantage. And in this case the thing we have to do is to be sure that that memory is collected and project Panama provides different types of arenas and arena is basically an object you can use to allocate memory. And there are four different types of arenas I think and I think you can create your owns. Uh this one specifically the confine arena means that it's a single thread arena can only be used by the thread that created that and it's also closable which with the property that when you close it all the memory you allocated is going to be released right which is pretty convenient. There are other arenas for example there are one are alive for the whole lifetime of your application. There are others that are alive as long as the garbage collector can see that it's been referenced. So it follows the

**[12:07](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=727s)** same life cycle of our Java objects. So yeah, and there are others that are concurrent. Okay. [snorts] Okay. Now we know that we can call and obtain primitive values from Rust. We can send objects like strings. But it would be cool if instead of having to compile the regular expression each time we are calling that imagine that we are want to verify whether something is an email or not. We don't want to compile the regular expression each time we want to do that. So sometimes it's useful to allocate something on the native code obtain the reference use it for a while and then release it. Okay. And that's what we are going to do here. Okay. Um here we have again something pretty similar. We are going to we are going to have three three functions three methods here. One is want to create the regular expression compile it which is okay. The important part here is probably the R side is

**[13:06](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=786s)** pretty chaotic and not super important and that this specific slide the Java side is more important because what we have here is that we are creating a new object. Then when we create the object we call the rust and ask to create a new regular expression compile it and then we keep the reference and we keep that handle attribute and that is going to be there with us for a sometime. Okay as long as we are using that handle the object is going to be alive in or the strct is going to be alive in rust. We can use it several times calling this uh is match method. And as you can see we are using a different arena here. The arena we are using in line 17 is different than the one we are using on line 9. Okay. Because we want to call it and we don't want to keep the reference of all the strings we have created for a very long time. And finally when you know we finish executing that we have to

**[14:11](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=851s)** close it and we have to release the information. It's important to, for example, close it in that specific order. You have to first destroy, call the destroy method, and then you have to release the arena. Otherwise, you're going to be leaking information. Okay. Another pretty common pattern. It's something that we don't use that much in Java, but it's pretty common in other languages, which is to return complex objects that contain multiple information inside. For example, you can have an either in something like um functional programming or in this case we are going to try to find a match for some specific group on the regular expression. So ideally we are going to retrieve one value saying whether there is a match or not and then one two indexes where the expression starts and ends. Okay. So we are going to receive something like that we have in Rust. Okay, whether it's found and the positions but in order to access that we have to

**[15:11](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=911s)** declare in Java that that extractor exist. I mean we can just read it as a bunch of bites and do that by ourselves but project Panama provides tons of utilities you can use to make it a little bit easier still very hard but a little bit easier to deal with these kind of structures. So here we have a strct that says okay we have a boolean some space in the middle and two integers. Okay. And then we have these bar handlers which are basic basically a way to access the whatever element for example the start element or the end element on a better way than just saying okay this is the pointer to my structure and then add eight bytes to read whatever we want to read. Right? Then we can once we obtain one of these objects we can just use the get method sending as the first argument the record we have read and then how many this is the co is being used for if you have an

**[16:14](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=974s)** array you can read end positions right but this is like super tedious nobody wants to be writing things like that okay and we know that or the people in the GDK know that and this is tedious and errorprone as I said nobody wants to write this or most of the stuff I show you for so on the open GDK they created a tool called JS extract. JS extract is a tool that you can uh use to transform any uh h or header file in C and you can transform that into stuff you can read from Java. Okay, that's if you are using project Panama, gestact is the tool you have to use to generate all that you know garbage and very repetitive code to be able to interact with your system. But we don't have header files if we are using rust right we have rust source code we don't have C rust uh C header files and that's something that the people from Mozilla know about and that's given that not

**[17:16](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1036s)** just Java but other languages may want to call Rust and they you know the lingua frana we have is this C um header files they created Cinden which is another tool that reads Ras code and generate these header files, right? So the process if you have a large library you want to trans you want to call from from Java is first install this tool but basically provide the crate you want to transform or want to call and this is automatically going going to generate this header file for you. Once you have that then you can use gstract and JS extract again you say okay this is the header files and it's going to generate all these files for you. Okay, that's uh the nice way to do this and you only need to do that only once. So usually unless you are modifying the native library. So cool. So far we have seen different ways to call rust from Java. But sometimes you would like to do the opposite. You

**[18:18](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1098s)** would like to be able from Rust to call Java. Okay, this is a place where Panama is not so powerful as GNI. In GNI you can do tons of things. You can even start a new GVM from using GV well using GNI. Okay. But in Panama you don't have that ability. The thing you can do is to have some kind of callbacks, right? You start in Java call some uh RA method and that RA method may call you back. Okay. For example, let's say that we have we want to do something like okay, we have a regular expression compile regular expression and we want to find all the matches on that regular expression and do something with that right there is a lambda there. So I want to this is very a very trivial example but let's say that we have um CSV with numbers and I have a regular expression which match with a number and then I want to print all of them. Okay. So we can use something like like this. Okay, but in

**[19:23](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1163s)** our case, we will need to call the find all method in Rust and we need to provide a lambda or a function to do something with that. And Rust has to call this function each time we find a match. Okay. So in R the code could be something like this. we have a a function basically and then we have this um method implemented where we receive this call back that we are going to be calling. I suppressed all the Ras code because probably it's not super interesting as this is just normal Ras code. Okay. And the Java part is the one that is a little bit more interesting. Here we have um we have obviously the method we want to call. Okay. In this case, I'm going to define an again a a very simple on match method that is going to do whatever. And here we have to this bridge part is important because here we are saying okay give me the shape of this Java uh

**[20:24](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1224s)** method you are going to call. Okay. And on the right hand side we are using Panama to say okay you know this method I have here please register that as a something that can be called from the native code. Okay. Once we have that and we have this stuff there, um we can call the same method we had before passing the pointer to that function we have. Okay. And that's it. The code is going to the Rust is going to be processing everything and it's going to see the same function you show in RS code. It's going to see that and it's going to call it without any any issue. Okay, wrapping it up a little bit. Um, we have enough time. Um, we have seen like five different patterns and in my experience, this is most of the things you are going to be doing if you are calling a native library. Uh, sometimes you are not going to need this

**[21:24](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1284s)** callback. Sometimes you're not going to need but you usually the other four specifically they are pretty common. Okay, as I said before, just you need to to take one thing from here is that the main reason to move to Rust, go to Rust or to call Rust is shouldn't be to be very fast because the GBM is very fast. But you know this ergonomics and the in fact the ability I mean you want to call some library that runs on the GPU either you have some kind of library that is calling native code or you have to do that yourself. Okay, the if you really care about performance then you have to understand that each GNI your Panama call is expensive when you are jumping from one side to the other is very expensive and in GNI is orders of magnitude more expensive because Panama okay you have to be copying information if you have like

**[22:21](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1341s)** complex structure you have to copy them off heap which is expensive and in GNI you can share pointers but once you start sharing pointers s you start doing things on the Rust or native code in general that is super problematic. you may end up pinning the GC and then the GC cannot run on the Java code and there are tons of issues there and especially you have to do a check um safe safe point is time you call GNI okay and that's very expensive in general if you can write a single call that uh represent or you know a single code instead of multiple narrow calls is going to be better obviously and if you can share memory it's better and sending copies all the time. For example, we in in Penote, we are using Apache Arrow which is a data format being used on multiple databases created mainly to share information structural information like SQL tables memory that can be divided you know in tables like columns and rows between different languages and between

**[23:25](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1405s)** different programs even right without copying information. So that's super useful. Probably not going to be your specific case. What do you need? But but it's usually best idea to keep everything off heap if you are going to be using a lot of native calls. The other one is to not use Java strings. In general, Java strings are very are very efficient for the things we want to do, but they are very, you know, the GDK is very paranoid and don't let you do a lot of things like actually reading the bites you have there. It has a very good reason to do that. For example, they changed the way the bytes are represented in different versions of Java to be more efficient. But for us, it's problematic because we would like to just point to these bites and it's not possible. And the other very very important part is to profile and run your code, verify the performance and in order to do that in Java, we have mostly

**[24:26](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1466s)** two tools. One is GFR included in the GDK and the other one is assign profiler which is not included in the GDK but it's very good and GFR has the problem of not being able to read native code or when you profile with GNR you're only going to have information from your stack uh Java stack not the native one okay I think they are changing that in the latest versions but still I think it's only supported in Linux and still they are working on that. So for now at least let's continue use assign profiler which is a very very cool tool to to that and almost the last thing I want to say is that the tooling is not great. Okay there are some plugins from both Maven and Gradel that compile the native code in this case Rust and package that inside your J your jar file. But for example, debugging is problematic because you as far as I know there is no debugger you can use to go inside from Java code to native code

**[25:31](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1531s)** under the hood. You can use two different debuggers. In the previous talk they were talking about GDB. You can use that but you are going to be debugging the GBM itself and your native code. Right? So it's a little bit tricky. I have to say that one advantage is that you can use AI to do that. AI is pretty good debugging code if you provide the MCPS. Um another thing is logging. Login is problematic because you have like two different words running on the same process, right? And your logs are not magically going to appear in your I mean if you are using lo forj or slf for java to log you are not going to have your native logs there. Okay, you can create a bridge but we are again with the same issue right we are logging is something that we do very frequently. So that means that we are going to have a lot of GNI calls and we are using a string

**[26:32](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1592s)** which means we need to copy information right so it's not a good idea in our case for example what we do is to just have two different logs one with the code written in Java one with the code written in Rust IDs are a little bit problematic you lose some of the features ID provides and you are using intellig you have to usually pay two licenses because you don't have full support of Rust and you need to pay twice and also the all the profiling tools and all the metrics you have the the GVM has a lot of metrics talking about the amount of memory you are allocated how is your heap how is even your native memory used by the GVM all of that is based on what the Java is doing but you are going to have your own heap native heap that you are not going to have any metric provided by the GBM you have to pro use

**[27:31](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1651s)** your own okay so that's another another important I usually I was when I was working on that I found some cases where we've had an issue and we were leaking information we were leaking memory and I tried to use AI to verify that or and and it was not very smart because it was focused on all the metrics from the GBM and it's like no no wait there are other you know thing around there and you have to explain that very carefully and finally this is the the last slide I want to go back again here In my opinion, one of the reasons why uh for example, Python is so useful right now is because it's super it's a language that is super easy to use, but it's also super easy to call very efficient native code, right? So when you are called you are um uh data science for you is very easy to write

**[28:28](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1708s)** Python and maybe it's not the fastest language in the world but all the heavy stuff is going to be delegated to native code which is going to be very fast and I think Java was enab It was very difficult to do something like that 5 10 years ago with Panama. I hope we are not too late and we still have time to catch up and make it easier to call all these very cool libraries we have around without having to wait until someone creates a wrapper using GNI to be able to call these libraries. Right. And yep, that's it. >> [applause] >> Thank you so much, Gonzalo. Unfortunately, we do not have enough time for Q&A, but Gonzalo would be hanging around near the stage area and he would be more than happy to answer any of your questions. And coming up shortly is a really interesting talk about co routines. But until then, let's give a big round of applause to Gonzalo. Thank you. [applause] We'll be taking a short break for 10

**[29:31](https://www.youtube.com/watch?v=KS6JBr3pAvk&t=1771s)** minutes and then see you later.
